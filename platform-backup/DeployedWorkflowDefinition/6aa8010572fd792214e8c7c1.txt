{
	"changesetType":"IMPORT",
	"createdTime":1789395205559,
	"deleted":false,
	"deployedWorkflowNotes":"Deploy cloud-storage file fix (uploadToCloudStorage=true, single-step, matches getAgentConfig pattern) to this environment - was already in draft",
	"deployedWorkflowVersion":1,
	"deploymentSource":{
		"changesetRequestId":"6aa7fffcb91cc6613f66322e",
		"changesetType":"IMPORT"
	},
	"edges":[
		{
			"fromNodeId":"n_v4sZv",
			"id":"next@n_v4sZv@n_Cegc9",
			"priority":0,
			"skip":false,
			"toNodeId":"n_Cegc9",
			"type":"next"
		},
		{
			"fromNodeId":"n_Cegc9",
			"id":"next@n_Cegc9@n_eE59d",
			"priority":0,
			"skip":false,
			"toNodeId":"n_eE59d",
			"type":"next"
		},
		{
			"fromNodeId":"n_eE59d",
			"id":"next@n_eE59d@n_Be9QU",
			"priority":0,
			"skip":false,
			"toNodeId":"n_Be9QU",
			"type":"next"
		},
		{
			"fromNodeId":"n_Be9QU",
			"id":"next@n_Be9QU@n_FPOkF",
			"priority":0,
			"skip":false,
			"toNodeId":"n_FPOkF",
			"type":"next"
		},
		{
			"fromNodeId":"n_FPOkF",
			"id":"next@n_FPOkF@n_pgCfg",
			"priority":0,
			"skip":false,
			"toNodeId":"n_pgCfg",
			"type":"next"
		},
		{
			"fromNodeId":"n_pgCfg",
			"id":"next@n_pgCfg@n_cnFNH",
			"priority":0,
			"skip":false,
			"toNodeId":"n_cnFNH",
			"type":"next"
		},
		{
			"fromNodeId":"n_cnFNH",
			"id":"next@n_cnFNH@n_APnyI",
			"priority":0,
			"skip":false,
			"toNodeId":"n_APnyI",
			"type":"next"
		},
		{
			"fromNodeId":"n_APnyI",
			"id":"next@n_APnyI@n_VYOw1",
			"priority":0,
			"skip":false,
			"toNodeId":"n_VYOw1",
			"type":"next"
		},
		{
			"fromNodeId":"n_VYOw1",
			"id":"next@n_VYOw1@n_rZz5h",
			"priority":0,
			"skip":false,
			"toNodeId":"n_rZz5h",
			"type":"next"
		},
		{
			"fromNodeId":"n_rZz5h",
			"id":"next@n_rZz5h@n_vU2yL",
			"priority":0,
			"skip":false,
			"toNodeId":"n_vU2yL",
			"type":"next"
		},
		{
			"fromNodeId":"n_vU2yL",
			"id":"loop@n_vU2yL@n_8lzWG",
			"priority":0,
			"skip":false,
			"toNodeId":"n_8lzWG",
			"type":"loop"
		},
		{
			"fromNodeId":"n_8lzWG",
			"id":"next@n_8lzWG@n_ZxueP",
			"priority":0,
			"skip":false,
			"toNodeId":"n_ZxueP",
			"type":"next"
		},
		{
			"fromNodeId":"n_ZxueP",
			"id":"if@n_ZxueP@n_LTs8U",
			"name":"yes",
			"priority":0,
			"skip":false,
			"toNodeId":"n_LTs8U",
			"type":"if"
		},
		{
			"fromNodeId":"n_LTs8U",
			"id":"next@n_LTs8U@n_2oX47",
			"priority":0,
			"skip":false,
			"toNodeId":"n_2oX47",
			"type":"next"
		},
		{
			"fromNodeId":"n_ZxueP",
			"id":"next@n_ZxueP@n_2oX47",
			"name":"no",
			"priority":0,
			"skip":false,
			"toNodeId":"n_2oX47",
			"type":"next"
		},
		{
			"fromNodeId":"n_2oX47",
			"id":"next@n_2oX47@n_zKUfl",
			"priority":0,
			"skip":false,
			"toNodeId":"n_zKUfl",
			"type":"next"
		},
		{
			"fromNodeId":"n_zKUfl",
			"id":"next@n_zKUfl@n_moIvM",
			"priority":0,
			"skip":false,
			"toNodeId":"n_moIvM",
			"type":"next"
		},
		{
			"fromNodeId":"n_moIvM",
			"id":"branch@n_moIvM@n_moIvM@1",
			"name":"1",
			"priority":0,
			"skip":false,
			"toNodeId":"n_moIvM@1",
			"type":"branch"
		},
		{
			"fromNodeId":"n_moIvM@1",
			"id":"next@n_moIvM@1@n_5NnK0",
			"priority":0,
			"skip":false,
			"toNodeId":"n_5NnK0",
			"type":"next"
		},
		{
			"fromNodeId":"n_5NnK0",
			"id":"next@n_5NnK0@n_Oad4Z",
			"priority":0,
			"skip":false,
			"toNodeId":"n_Oad4Z",
			"type":"next"
		},
		{
			"fromNodeId":"n_moIvM",
			"id":"branch@n_moIvM@n_moIvM@2",
			"name":"2",
			"priority":0,
			"skip":false,
			"toNodeId":"n_moIvM@2",
			"type":"branch"
		},
		{
			"fromNodeId":"n_moIvM@2",
			"id":"next@n_moIvM@2@n_KZZsd",
			"priority":0,
			"skip":false,
			"toNodeId":"n_KZZsd",
			"type":"next"
		},
		{
			"fromNodeId":"n_KZZsd",
			"id":"if@n_KZZsd@n_k02Aq",
			"name":"yes",
			"priority":0,
			"skip":false,
			"toNodeId":"n_k02Aq",
			"type":"if"
		},
		{
			"fromNodeId":"n_k02Aq",
			"id":"next@n_k02Aq@n_qgM1x",
			"priority":0,
			"skip":false,
			"toNodeId":"n_qgM1x",
			"type":"next"
		},
		{
			"fromNodeId":"n_qgM1x",
			"id":"next@n_qgM1x@n_84rFM",
			"priority":0,
			"skip":false,
			"toNodeId":"n_84rFM",
			"type":"next"
		},
		{
			"fromNodeId":"n_84rFM",
			"id":"next@n_84rFM@n_VgcsB",
			"priority":0,
			"skip":false,
			"toNodeId":"n_VgcsB",
			"type":"next"
		},
		{
			"fromNodeId":"n_VgcsB",
			"id":"next@n_VgcsB@n_yc5sd",
			"priority":0,
			"skip":false,
			"toNodeId":"n_yc5sd",
			"type":"next"
		},
		{
			"fromNodeId":"n_yc5sd",
			"id":"next@n_yc5sd@n_IFllm",
			"priority":0,
			"skip":false,
			"toNodeId":"n_IFllm",
			"type":"next"
		},
		{
			"fromNodeId":"n_IFllm",
			"id":"if@n_IFllm@n_rfqMg",
			"name":"yes",
			"priority":0,
			"skip":false,
			"toNodeId":"n_rfqMg",
			"type":"if"
		},
		{
			"fromNodeId":"n_upFil",
			"id":"next@n_upFil@n_bGet4",
			"priority":0,
			"skip":false,
			"toNodeId":"n_bGet4",
			"type":"next"
		},
		{
			"fromNodeId":"n_IFllm",
			"id":"next@n_IFllm@n_bGet4",
			"name":"no",
			"priority":0,
			"skip":false,
			"toNodeId":"n_bGet4",
			"type":"next"
		},
		{
			"fromNodeId":"n_bGet4",
			"id":"next@n_bGet4@n_mPkXD",
			"priority":0,
			"skip":false,
			"toNodeId":"n_mPkXD",
			"type":"next"
		},
		{
			"fromNodeId":"n_mPkXD",
			"id":"next@n_mPkXD@n_BZnmR",
			"priority":0,
			"skip":false,
			"toNodeId":"n_BZnmR",
			"type":"next"
		},
		{
			"fromNodeId":"n_BZnmR",
			"id":"next@n_BZnmR@n_DNslO",
			"priority":0,
			"skip":false,
			"toNodeId":"n_DNslO",
			"type":"next"
		},
		{
			"fromNodeId":"n_DNslO",
			"id":"loop@n_DNslO@n_cvTol",
			"priority":0,
			"skip":false,
			"toNodeId":"n_cvTol",
			"type":"loop"
		},
		{
			"fromNodeId":"n_cvTol",
			"id":"next@n_cvTol@n_t3ANJ",
			"priority":0,
			"skip":false,
			"toNodeId":"n_t3ANJ",
			"type":"next"
		},
		{
			"fromNodeId":"n_t3ANJ",
			"id":"next@n_t3ANJ@n_Wr6cd",
			"priority":0,
			"skip":false,
			"toNodeId":"n_Wr6cd",
			"type":"next"
		},
		{
			"fromNodeId":"n_Wr6cd",
			"id":"next@n_Wr6cd@n_zFiVb",
			"priority":0,
			"skip":false,
			"toNodeId":"n_zFiVb",
			"type":"next"
		},
		{
			"fromNodeId":"n_zFiVb",
			"id":"next@n_zFiVb@n_IFtol",
			"priority":0,
			"skip":false,
			"toNodeId":"n_IFtol",
			"type":"next"
		},
		{
			"fromNodeId":"n_IFtol",
			"id":"if@n_IFtol@n_A2tYc",
			"name":"yes",
			"priority":0,
			"skip":false,
			"toNodeId":"n_A2tYc",
			"type":"if"
		},
		{
			"fromNodeId":"n_upTol",
			"id":"next@n_upTol@n_cFcDR",
			"priority":0,
			"skip":false,
			"toNodeId":"n_cFcDR",
			"type":"next"
		},
		{
			"fromNodeId":"n_IFtol",
			"id":"next@n_IFtol@n_cFcDR",
			"name":"no",
			"priority":0,
			"skip":false,
			"toNodeId":"n_cFcDR",
			"type":"next"
		},
		{
			"fromNodeId":"n_cFcDR",
			"id":"next@n_cFcDR@n_dwr7W",
			"priority":0,
			"skip":false,
			"toNodeId":"n_dwr7W",
			"type":"next"
		},
		{
			"fromNodeId":"n_dwr7W",
			"id":"next@n_dwr7W@n_DNslO",
			"name":"loopback",
			"priority":0,
			"skip":false,
			"toNodeId":"n_DNslO",
			"type":"next"
		},
		{
			"fromNodeId":"n_DNslO",
			"id":"next@n_DNslO@n_XJ345",
			"priority":0,
			"skip":false,
			"toNodeId":"n_XJ345",
			"type":"next"
		},
		{
			"fromNodeId":"n_XJ345",
			"id":"next@n_XJ345@n_mcJ3K",
			"priority":0,
			"skip":false,
			"toNodeId":"n_mcJ3K",
			"type":"next"
		},
		{
			"fromNodeId":"n_moIvM",
			"id":"branch@n_moIvM@n_moIvM@3",
			"name":"3",
			"priority":0,
			"skip":false,
			"toNodeId":"n_moIvM@3",
			"type":"branch"
		},
		{
			"fromNodeId":"n_moIvM@3",
			"id":"next@n_moIvM@3@n_kYScV",
			"priority":0,
			"skip":false,
			"toNodeId":"n_kYScV",
			"type":"next"
		},
		{
			"fromNodeId":"n_kYScV",
			"id":"next@n_kYScV@n_FHsWj",
			"priority":0,
			"skip":false,
			"toNodeId":"n_FHsWj",
			"type":"next"
		},
		{
			"fromNodeId":"n_moIvM",
			"id":"branch@n_moIvM@n_moIvM@4",
			"name":"4",
			"priority":0,
			"skip":false,
			"toNodeId":"n_moIvM@4",
			"type":"branch"
		},
		{
			"fromNodeId":"n_moIvM@4",
			"id":"next@n_moIvM@4@n_UlhJE",
			"priority":0,
			"skip":false,
			"toNodeId":"n_UlhJE",
			"type":"next"
		},
		{
			"fromNodeId":"n_UlhJE",
			"id":"next@n_UlhJE@n_kxKO5",
			"priority":0,
			"skip":false,
			"toNodeId":"n_kxKO5",
			"type":"next"
		},
		{
			"fromNodeId":"n_Oad4Z",
			"id":"next@n_Oad4Z@n_vU2yL",
			"name":"loopback",
			"priority":0,
			"skip":false,
			"toNodeId":"n_vU2yL",
			"type":"next"
		},
		{
			"fromNodeId":"n_mcJ3K",
			"id":"next@n_mcJ3K@n_vU2yL",
			"name":"loopback",
			"priority":0,
			"skip":false,
			"toNodeId":"n_vU2yL",
			"type":"next"
		},
		{
			"fromNodeId":"n_KZZsd",
			"id":"next@n_KZZsd@n_vU2yL",
			"name":"no",
			"priority":0,
			"skip":false,
			"toNodeId":"n_vU2yL",
			"type":"next"
		},
		{
			"fromNodeId":"n_FHsWj",
			"id":"next@n_FHsWj@n_vU2yL",
			"name":"loopback",
			"priority":0,
			"skip":false,
			"toNodeId":"n_vU2yL",
			"type":"next"
		},
		{
			"fromNodeId":"n_kxKO5",
			"id":"next@n_kxKO5@n_vU2yL",
			"name":"loopback",
			"priority":0,
			"skip":false,
			"toNodeId":"n_vU2yL",
			"type":"next"
		},
		{
			"fromNodeId":"n_moIvM",
			"id":"next@n_moIvM@n_vU2yL",
			"name":"default",
			"priority":0,
			"skip":false,
			"toNodeId":"n_vU2yL",
			"type":"next"
		},
		{
			"fromNodeId":"n_vU2yL",
			"id":"next@n_vU2yL@n_QOhaa",
			"priority":0,
			"skip":false,
			"toNodeId":"n_QOhaa",
			"type":"next"
		},
		{
			"fromNodeId":"n_QOhaa",
			"id":"next@n_QOhaa@n_chkGt",
			"priority":0,
			"skip":false,
			"toNodeId":"n_chkGt",
			"type":"next"
		},
		{
			"fromNodeId":"n_chkGt",
			"id":"next@n_chkGt@n_IFchk",
			"priority":0,
			"skip":false,
			"toNodeId":"n_IFchk",
			"type":"next"
		},
		{
			"fromNodeId":"n_IFchk",
			"id":"if@n_IFchk@n_mm9mc",
			"name":"yes",
			"priority":0,
			"skip":false,
			"toNodeId":"n_mm9mc",
			"type":"if"
		},
		{
			"fromNodeId":"n_mm9mc",
			"id":"loop@n_mm9mc@n_jrFAb",
			"priority":0,
			"skip":false,
			"toNodeId":"n_jrFAb",
			"type":"loop"
		},
		{
			"fromNodeId":"n_jrFAb",
			"id":"next@n_jrFAb@n_icOtM",
			"priority":0,
			"skip":false,
			"toNodeId":"n_icOtM",
			"type":"next"
		},
		{
			"fromNodeId":"n_GnI26",
			"id":"next@n_GnI26@n_Tm8Ns",
			"priority":0,
			"skip":false,
			"toNodeId":"n_Tm8Ns",
			"type":"next"
		},
		{
			"fromNodeId":"n_Tm8Ns",
			"id":"next@n_Tm8Ns@n_mm9mc",
			"name":"loopback",
			"priority":0,
			"skip":false,
			"toNodeId":"n_mm9mc",
			"type":"next"
		},
		{
			"fromNodeId":"n_mm9mc",
			"id":"next@n_mm9mc@n_WB2iD",
			"priority":0,
			"skip":false,
			"toNodeId":"n_WB2iD",
			"type":"next"
		},
		{
			"fromNodeId":"n_IFchk",
			"id":"next@n_IFchk@n_WB2iD",
			"name":"no",
			"priority":0,
			"skip":false,
			"toNodeId":"n_WB2iD",
			"type":"next"
		},
		{
			"fromNodeId":"n_WB2iD",
			"id":"next@n_WB2iD@n_Xq7FL",
			"priority":0,
			"skip":false,
			"toNodeId":"n_Xq7FL",
			"type":"next"
		},
		{
			"fromNodeId":"n_rfqMg",
			"id":"next@n_rfqMg@n_upFil",
			"priority":0,
			"skip":false,
			"toNodeId":"n_upFil",
			"type":"next"
		},
		{
			"fromNodeId":"n_A2tYc",
			"id":"next@n_A2tYc@n_upTol",
			"priority":0,
			"skip":false,
			"toNodeId":"n_upTol",
			"type":"next"
		},
		{
			"fromNodeId":"n_icOtM",
			"id":"next@n_icOtM@n_GnI26",
			"priority":0,
			"skip":false,
			"toNodeId":"n_GnI26",
			"type":"next"
		}
	],
	"id":"6aa8010572fd792214e8c7c1",
	"lastModifiedBy":-1,
	"lcName":"agent optimiser | getsingleturntimeline",
	"modifiedTime":1789395205561,
	"name":"Agent Optimiser | GetSingleTurnTimeline",
	"nodes":[
		{
			"context":{
				"appName":"callables",
				"resourceVersion":2756,
				"resourceName":"callables_from_automation"
			},
			"debug":false,
			"dirty":false,
			"fallbackMode":"STOP",
			"groupId":"_JAI60-1",
			"id":"n_v4sZv",
			"index":1,
			"inputs":{
				"result":{
					"additionalProperties":false,
					"properties":{
						"result":{
							"additionalProperties":false,
							"properties":{},
							"title":"Result",
							"type":"object"
						}
					},
					"required":[
						"result"
					],
					"type":"object"
				},
				"setup":{
					"additionalProperties":false,
					"properties":{
						"case_id":{
							"title":"Case Id",
							"type":"string"
						},
						"limit":{
							"title":"Limit",
							"type":"integer"
						},
						"message_id":{
							"title":"Message Id",
							"type":"string"
						},
						"offset":{
							"title":"Offset",
							"type":"integer"
						}
					},
					"required":[
						"case_id",
						"message_id",
						"limit",
						"offset"
					],
					"type":"object"
				}
			},
			"skip":false,
			"subTitle":"Callable",
			"title":"Trigger via automation",
			"trigger":{
				"type":"CALLABLE"
			},
			"type":"START"
		},
		{
			"context":{
				"appName":"variable_by_unifyapps",
				"resourceVersion":0,
				"resourceName":"variable_by_unifyapps_create_list",
				"type":"APPLICATION"
			},
			"debug":false,
			"dirty":false,
			"fallbackMode":"STOP",
			"groupId":"_JAI60-1",
			"id":"n_Cegc9",
			"index":2,
			"inputSchema":{
				"type":"SCHEMA_AND_LAYOUT",
				"dynamic":false,
				"layout":{
					"createList":{
						"items":{
							"ui:description":"Set the initial values of the list (defaults to empty)",
							"ui:title":"Initial list items"
						},
						"ua:type":{
							"ui:widget":"hidden"
						},
						"ui:order":[
							"*",
							"items"
						],
						"ui:title":""
					},
					"ua:title":{
						"ui:widget":"hidden"
					},
					"ua:list_type":{
						"ui:widget":"hidden"
					}
				},
				"schema":{
					"allOf":[
						{
							"if":{
								"properties":{
									"ua:list_type":{
										"const":"OBJECT"
									}
								}
							},
							"then":{
								"properties":{
									"createList":{
										"properties":{
											"items":{
												"properties":{},
												"type":"object"
											}
										},
										"type":"object"
									}
								}
							}
						},
						{
							"if":{
								"properties":{
									"ua:list_type":{
										"const":"STRING"
									}
								}
							},
							"then":{
								"properties":{
									"createList":{
										"properties":{
											"items":{
												"type":"string"
											},
											"source":{
												"format":"ua:source",
												"title":"List of String",
												"type":"string"
											}
										},
										"type":"object"
									}
								}
							}
						},
						{
							"if":{
								"properties":{
									"ua:list_type":{
										"const":"INTEGER"
									}
								}
							},
							"then":{
								"properties":{
									"createList":{
										"properties":{
											"items":{
												"type":"integer"
											},
											"source":{
												"format":"ua:source",
												"title":"List of Integers",
												"type":"string"
											}
										},
										"type":"object"
									}
								}
							}
						},
						{
							"if":{
								"properties":{
									"ua:list_type":{
										"const":"NUMBER"
									}
								}
							},
							"then":{
								"properties":{
									"createList":{
										"properties":{
											"items":{
												"type":"number"
											},
											"source":{
												"format":"ua:source",
												"title":"List of Numbers",
												"type":"string"
											}
										},
										"type":"object"
									}
								}
							}
						},
						{
							"if":{
								"properties":{
									"ua:list_type":{
										"const":"BOOLEAN"
									}
								}
							},
							"then":{
								"properties":{
									"createList":{
										"properties":{
											"items":{
												"type":"boolean"
											},
											"source":{
												"format":"ua:source",
												"title":"List of Booleans",
												"type":"string"
											}
										},
										"type":"object"
									}
								}
							}
						}
					],
					"format":"ua:list",
					"type":"object",
					"properties":{
						"createList":{
							"properties":{
								"items":{
									"additionalProperties":false,
									"properties":{},
									"type":[
										"object",
										"string",
										"number",
										"boolean",
										"integer"
									]
								},
								"source":{
									"format":"ua:source",
									"title":"List Item Source",
									"type":"string"
								},
								"ua:type":{
									"default":"mappedArray",
									"type":"string"
								}
							},
							"type":"object"
						},
						"ua:list_type":{
							"default":"OBJECT",
							"oneOf":[
								{
									"const":"OBJECT",
									"title":"Object"
								},
								{
									"const":"STRING",
									"title":"String"
								},
								{
									"const":"NUMBER",
									"title":"Number"
								},
								{
									"const":"INTEGER",
									"title":"Integer"
								},
								{
									"const":"BOOLEAN",
									"title":"Boolean"
								}
							],
							"title":"List Type",
							"type":"string"
						},
						"ua:title":{
							"type":"string"
						}
					}
				}
			},
			"inputs":{
				"createList":{
					"ua:type":"mappedArray"
				},
				"ua:title":"Input Guardrails",
				"ua:list_type":"OBJECT"
			},
			"outputSchema":{
				"type":"SCHEMA",
				"dynamic":false,
				"schema":{
					"type":"object",
					"properties":{
						"items":{
							"disableActions":true,
							"items":{
								"properties":{},
								"type":"object"
							},
							"title":"Input Guardrails",
							"type":"array"
						}
					}
				}
			},
			"skip":false,
			"subTitle":"Variable",
			"title":"Input Guardrails",
			"type":"ACTION"
		},
		{
			"context":{
				"appName":"variable_by_unifyapps",
				"resourceVersion":0,
				"resourceName":"variable_by_unifyapps_create_list",
				"type":"APPLICATION"
			},
			"debug":false,
			"dirty":false,
			"fallbackMode":"STOP",
			"groupId":"_JAI60-1",
			"id":"n_eE59d",
			"index":3,
			"inputSchema":{
				"type":"SCHEMA_AND_LAYOUT",
				"dynamic":false,
				"layout":{
					"createList":{
						"items":{
							"ui:description":"Set the initial values of the list (defaults to empty)",
							"ui:title":"Initial list items"
						},
						"ua:type":{
							"ui:widget":"hidden"
						},
						"ui:order":[
							"*",
							"items"
						],
						"ui:title":""
					},
					"ua:title":{
						"ui:widget":"hidden"
					},
					"ua:list_type":{
						"ui:widget":"hidden"
					}
				},
				"schema":{
					"allOf":[
						{
							"if":{
								"properties":{
									"ua:list_type":{
										"const":"OBJECT"
									}
								}
							},
							"then":{
								"properties":{
									"createList":{
										"properties":{
											"items":{
												"properties":{},
												"type":"object"
											}
										},
										"type":"object"
									}
								}
							}
						},
						{
							"if":{
								"properties":{
									"ua:list_type":{
										"const":"STRING"
									}
								}
							},
							"then":{
								"properties":{
									"createList":{
										"properties":{
											"items":{
												"type":"string"
											},
											"source":{
												"format":"ua:source",
												"title":"List of String",
												"type":"string"
											}
										},
										"type":"object"
									}
								}
							}
						},
						{
							"if":{
								"properties":{
									"ua:list_type":{
										"const":"INTEGER"
									}
								}
							},
							"then":{
								"properties":{
									"createList":{
										"properties":{
											"items":{
												"type":"integer"
											},
											"source":{
												"format":"ua:source",
												"title":"List of Integers",
												"type":"string"
											}
										},
										"type":"object"
									}
								}
							}
						},
						{
							"if":{
								"properties":{
									"ua:list_type":{
										"const":"NUMBER"
									}
								}
							},
							"then":{
								"properties":{
									"createList":{
										"properties":{
											"items":{
												"type":"number"
											},
											"source":{
												"format":"ua:source",
												"title":"List of Numbers",
												"type":"string"
											}
										},
										"type":"object"
									}
								}
							}
						},
						{
							"if":{
								"properties":{
									"ua:list_type":{
										"const":"BOOLEAN"
									}
								}
							},
							"then":{
								"properties":{
									"createList":{
										"properties":{
											"items":{
												"type":"boolean"
											},
											"source":{
												"format":"ua:source",
												"title":"List of Booleans",
												"type":"string"
											}
										},
										"type":"object"
									}
								}
							}
						}
					],
					"format":"ua:list",
					"type":"object",
					"properties":{
						"createList":{
							"properties":{
								"items":{
									"additionalProperties":false,
									"properties":{},
									"type":[
										"object",
										"string",
										"number",
										"boolean",
										"integer"
									]
								},
								"source":{
									"format":"ua:source",
									"title":"List Item Source",
									"type":"string"
								},
								"ua:type":{
									"default":"mappedArray",
									"type":"string"
								}
							},
							"type":"object"
						},
						"ua:list_type":{
							"default":"OBJECT",
							"oneOf":[
								{
									"const":"OBJECT",
									"title":"Object"
								},
								{
									"const":"STRING",
									"title":"String"
								},
								{
									"const":"NUMBER",
									"title":"Number"
								},
								{
									"const":"INTEGER",
									"title":"Integer"
								},
								{
									"const":"BOOLEAN",
									"title":"Boolean"
								}
							],
							"title":"List Type",
							"type":"string"
						},
						"ua:title":{
							"type":"string"
						}
					}
				}
			},
			"inputs":{
				"createList":{
					"ua:type":"mappedArray"
				},
				"ua:title":"Agent Execution",
				"ua:list_type":"OBJECT"
			},
			"outputSchema":{
				"type":"SCHEMA",
				"dynamic":false,
				"schema":{
					"type":"object",
					"properties":{
						"items":{
							"disableActions":true,
							"items":{
								"properties":{},
								"type":"object"
							},
							"title":"Agent Execution",
							"type":"array"
						}
					}
				}
			},
			"skip":false,
			"subTitle":"Variable",
			"title":"Agent Execution",
			"type":"ACTION"
		},
		{
			"context":{
				"appName":"variable_by_unifyapps",
				"resourceVersion":0,
				"resourceName":"variable_by_unifyapps_create_list",
				"type":"APPLICATION"
			},
			"debug":false,
			"dirty":false,
			"fallbackMode":"STOP",
			"groupId":"_JAI60-1",
			"id":"n_Be9QU",
			"index":4,
			"inputSchema":{
				"type":"SCHEMA_AND_LAYOUT",
				"dynamic":false,
				"layout":{
					"createList":{
						"items":{
							"ui:description":"Set the initial values of the list (defaults to empty)",
							"ui:title":"Initial list items"
						},
						"ua:type":{
							"ui:widget":"hidden"
						},
						"ui:order":[
							"*",
							"items"
						],
						"ui:title":""
					},
					"ua:title":{
						"ui:widget":"hidden"
					},
					"ua:list_type":{
						"ui:widget":"hidden"
					}
				},
				"schema":{
					"allOf":[
						{
							"if":{
								"properties":{
									"ua:list_type":{
										"const":"OBJECT"
									}
								}
							},
							"then":{
								"properties":{
									"createList":{
										"properties":{
											"items":{
												"properties":{},
												"type":"object"
											}
										},
										"type":"object"
									}
								}
							}
						},
						{
							"if":{
								"properties":{
									"ua:list_type":{
										"const":"STRING"
									}
								}
							},
							"then":{
								"properties":{
									"createList":{
										"properties":{
											"items":{
												"type":"string"
											},
											"source":{
												"format":"ua:source",
												"title":"List of String",
												"type":"string"
											}
										},
										"type":"object"
									}
								}
							}
						},
						{
							"if":{
								"properties":{
									"ua:list_type":{
										"const":"INTEGER"
									}
								}
							},
							"then":{
								"properties":{
									"createList":{
										"properties":{
											"items":{
												"type":"integer"
											},
											"source":{
												"format":"ua:source",
												"title":"List of Integers",
												"type":"string"
											}
										},
										"type":"object"
									}
								}
							}
						},
						{
							"if":{
								"properties":{
									"ua:list_type":{
										"const":"NUMBER"
									}
								}
							},
							"then":{
								"properties":{
									"createList":{
										"properties":{
											"items":{
												"type":"number"
											},
											"source":{
												"format":"ua:source",
												"title":"List of Numbers",
												"type":"string"
											}
										},
										"type":"object"
									}
								}
							}
						},
						{
							"if":{
								"properties":{
									"ua:list_type":{
										"const":"BOOLEAN"
									}
								}
							},
							"then":{
								"properties":{
									"createList":{
										"properties":{
											"items":{
												"type":"boolean"
											},
											"source":{
												"format":"ua:source",
												"title":"List of Booleans",
												"type":"string"
											}
										},
										"type":"object"
									}
								}
							}
						}
					],
					"format":"ua:list",
					"type":"object",
					"properties":{
						"createList":{
							"properties":{
								"items":{
									"additionalProperties":false,
									"properties":{},
									"type":[
										"object",
										"string",
										"number",
										"boolean",
										"integer"
									]
								},
								"source":{
									"format":"ua:source",
									"title":"List Item Source",
									"type":"string"
								},
								"ua:type":{
									"default":"mappedArray",
									"type":"string"
								}
							},
							"type":"object"
						},
						"ua:list_type":{
							"default":"OBJECT",
							"oneOf":[
								{
									"const":"OBJECT",
									"title":"Object"
								},
								{
									"const":"STRING",
									"title":"String"
								},
								{
									"const":"NUMBER",
									"title":"Number"
								},
								{
									"const":"INTEGER",
									"title":"Integer"
								},
								{
									"const":"BOOLEAN",
									"title":"Boolean"
								}
							],
							"title":"List Type",
							"type":"string"
						},
						"ua:title":{
							"type":"string"
						}
					}
				}
			},
			"inputs":{
				"createList":{
					"ua:type":"mappedArray"
				},
				"ua:title":"Response Guardrail",
				"ua:list_type":"OBJECT"
			},
			"outputSchema":{
				"type":"SCHEMA",
				"dynamic":false,
				"schema":{
					"type":"object",
					"properties":{
						"items":{
							"disableActions":true,
							"items":{
								"properties":{},
								"type":"object"
							},
							"title":"Response Guardrail",
							"type":"array"
						}
					}
				}
			},
			"skip":false,
			"subTitle":"Variable",
			"title":"Response Guardrail",
			"type":"ACTION"
		},
		{
			"context":{
				"appName":"variable_by_unifyapps",
				"resourceVersion":3835,
				"resourceName":"variable_by_unifyapps_create_list",
				"type":"APPLICATION"
			},
			"debug":false,
			"dirty":false,
			"fallbackMode":"STOP",
			"groupId":"_JAI60-1",
			"id":"n_FPOkF",
			"index":5,
			"inputSchema":{
				"type":"SCHEMA_AND_LAYOUT",
				"dynamic":false,
				"layout":{
					"createList":{
						"items":{
							"ui:description":"Set the initial values of the list (defaults to empty)",
							"ui:title":"Initial list items"
						},
						"ua:type":{
							"ui:widget":"hidden"
						},
						"ui:order":[
							"*",
							"items"
						],
						"ui:title":""
					},
					"ua:title":{
						"ui:widget":"hidden"
					},
					"ua:list_type":{
						"ui:widget":"hidden"
					}
				},
				"schema":{
					"allOf":[
						{
							"if":{
								"properties":{
									"ua:list_type":{
										"const":"OBJECT"
									}
								}
							},
							"then":{
								"properties":{
									"createList":{
										"properties":{
											"items":{
												"properties":{},
												"type":"object"
											}
										},
										"type":"object"
									}
								}
							}
						},
						{
							"if":{
								"properties":{
									"ua:list_type":{
										"const":"STRING"
									}
								}
							},
							"then":{
								"properties":{
									"createList":{
										"properties":{
											"items":{
												"type":"string"
											},
											"source":{
												"format":"ua:source",
												"title":"List of String",
												"type":"string"
											}
										},
										"type":"object"
									}
								}
							}
						},
						{
							"if":{
								"properties":{
									"ua:list_type":{
										"const":"INTEGER"
									}
								}
							},
							"then":{
								"properties":{
									"createList":{
										"properties":{
											"items":{
												"type":"integer"
											},
											"source":{
												"format":"ua:source",
												"title":"List of Integers",
												"type":"string"
											}
										},
										"type":"object"
									}
								}
							}
						},
						{
							"if":{
								"properties":{
									"ua:list_type":{
										"const":"NUMBER"
									}
								}
							},
							"then":{
								"properties":{
									"createList":{
										"properties":{
											"items":{
												"type":"number"
											},
											"source":{
												"format":"ua:source",
												"title":"List of Numbers",
												"type":"string"
											}
										},
										"type":"object"
									}
								}
							}
						},
						{
							"if":{
								"properties":{
									"ua:list_type":{
										"const":"BOOLEAN"
									}
								}
							},
							"then":{
								"properties":{
									"createList":{
										"properties":{
											"items":{
												"type":"boolean"
											},
											"source":{
												"format":"ua:source",
												"title":"List of Booleans",
												"type":"string"
											}
										},
										"type":"object"
									}
								}
							}
						}
					],
					"format":"ua:list",
					"type":"object",
					"properties":{
						"createList":{
							"properties":{
								"items":{
									"additionalProperties":false,
									"properties":{},
									"type":[
										"object",
										"string",
										"number",
										"boolean",
										"integer"
									]
								},
								"source":{
									"format":"ua:source",
									"title":"List Item Source",
									"type":"string"
								},
								"ua:type":{
									"default":"mappedArray",
									"type":"string"
								}
							},
							"type":"object"
						},
						"ua:list_type":{
							"default":"OBJECT",
							"oneOf":[
								{
									"const":"OBJECT",
									"title":"Object"
								},
								{
									"const":"STRING",
									"title":"String"
								},
								{
									"const":"NUMBER",
									"title":"Number"
								},
								{
									"const":"INTEGER",
									"title":"Integer"
								},
								{
									"const":"BOOLEAN",
									"title":"Boolean"
								}
							],
							"title":"List Type",
							"type":"string"
						},
						"ua:title":{
							"type":"string"
						}
					}
				}
			},
			"inputs":{
				"createList":{
					"ua:type":"mappedArray"
				},
				"ua:title":"Knowledge Retrieval",
				"ua:list_type":"OBJECT"
			},
			"outputSchema":{
				"type":"SCHEMA",
				"dynamic":false,
				"schema":{
					"type":"object",
					"properties":{
						"items":{
							"disableActions":true,
							"items":{
								"properties":{},
								"type":"object"
							},
							"title":"Knowledge Retrieval",
							"type":"array"
						}
					}
				}
			},
			"skip":false,
			"subTitle":"Variable",
			"title":"Knowledge Retrieval",
			"type":"ACTION"
		},
		{
			"context":{
				"appName":"code_by_unifyapps",
				"resourceVersion":970,
				"resourceName":"code_by_unifyapps_groovy",
				"type":"APPLICATION"
			},
			"debug":false,
			"dirty":false,
			"fallbackMode":"STOP",
			"groupId":"_JAI60-1",
			"id":"n_pgCfg",
			"index":6,
			"inputs":{
				"output":{
					"additionalProperties":false,
					"properties":{
						"caseId":{
							"title":"Case Id",
							"type":"string"
						},
						"fetchLimit":{
							"title":"Fetch Limit",
							"type":"integer"
						},
						"idCharactersStripped":{
							"items":{
								"type":"string"
							},
							"title":"Id Characters Stripped",
							"type":"array"
						},
						"idsMissing":{
							"title":"Ids Missing",
							"type":"boolean"
						},
						"limit":{
							"title":"Limit",
							"type":"integer"
						},
						"limitClamped":{
							"title":"Limit Clamped",
							"type":"boolean"
						},
						"limitDefaulted":{
							"title":"Limit Defaulted",
							"type":"boolean"
						},
						"maxLimit":{
							"title":"Max Limit",
							"type":"integer"
						},
						"messageId":{
							"title":"Message Id",
							"type":"string"
						},
						"offset":{
							"title":"Offset",
							"type":"integer"
						},
						"offsetDefaulted":{
							"title":"Offset Defaulted",
							"type":"boolean"
						}
					},
					"required":[
						"offset",
						"limit",
						"fetchLimit"
					],
					"type":"object"
				},
				"input":{
					"additionalProperties":false,
					"properties":{
						"caseId":{
							"title":"Case Id",
							"type":"string"
						},
						"limit":{
							"title":"Limit",
							"type":"integer"
						},
						"messageId":{
							"title":"Message Id",
							"type":"string"
						},
						"offset":{
							"title":"Offset",
							"type":"integer"
						}
					},
					"required":[],
					"type":"object"
				},
				"code":"// Resolve the query window ONCE, and make everything the SQL string interpolates safe before it\n// gets there.\n//\n//  1. `limit` / `offset` are normalised, so the statement can never be handed `LIMIT ` (a caller\n//     that omits them, or sends 0) and can never be handed a page bigger than one run can hold.\n//\n//  2. `fetchLimit` is limit + 1 ON PURPOSE. The query asks for one row MORE than the caller wants,\n//     n_APnyI drops it, and its presence is the answer to \"is there another page\" — no COUNT(*),\n//     no second round trip, and no `returned == limit` guess (wrong exactly once per message, on\n//     the page that lands on the boundary, where it reports a nextOffset that returns nothing).\n//\n//  3. caseId / messageId are pasted INTO the SQL text as quoted literals. An id carrying a quote,\n//     a backslash or a newline would break the statement or change its meaning, and the failure\n//     would be a confusing SQL error rather than \"that id is malformed\". e_traces ids are\n//     `e_<hex>`, so anything outside [A-Za-z0-9_-] is dropped here and reported.\ndef asLong = { v, long dflt ->\n    if (v == null) return dflt\n    try { return (v as BigDecimal).longValue() } catch (ignored) { return dflt }\n}\n\ndef DEFAULT_LIMIT = 500L\ndef MAX_LIMIT     = 1000L      \n// one page must stay assemblable in memory; see previewCharsPerSpan\n// A page is the whole universe the relational rules get to reason over, and one of them needs\n// room: a delegation wrapper is recognised by another agent's react loop STARTING inside its\n// window, so the page has to hold the wrapper and that loop's first call at the same time. Below\n// ~25 rows it often does not — measured on a 546-span case, a 105,978 ms InvokeAgent came apart\n// from the loop it opened and was billed as ordinary tool time. Everything else here is decided per\n// row and does not care, so this is the one number that sets the floor.\ndef MIN_LIMIT     = 25L\n\ndef rawLimit  = binding.hasVariable('limit')  ? limit  : null\ndef rawOffset = binding.hasVariable('offset') ? offset : null\n\ndef wanted     = asLong(rawLimit, DEFAULT_LIMIT)\ndef pageLimit  = (wanted <= 0) ? DEFAULT_LIMIT : Math.min(Math.max(wanted, MIN_LIMIT), MAX_LIMIT)\ndef pageOffset = Math.max(0L, asLong(rawOffset, 0L))\n\ndef rawCase = binding.hasVariable('caseId')    ? caseId    : null\ndef rawMsg  = binding.hasVariable('messageId') ? messageId : null\ndef clean = { v -> (v == null) ? '' : ((v as String).trim().replaceAll('[^A-Za-z0-9_-]', '')) }\ndef caseIdSafe = clean(rawCase)\ndef msgIdSafe  = clean(rawMsg)\n\ndef rejected = []\nif (rawCase != null && caseIdSafe != (rawCase as String).trim()) rejected << 'case_id'\nif (rawMsg  != null && msgIdSafe  != (rawMsg  as String).trim()) rejected << 'message_id'\n\ndef out = [\n    offset         : pageOffset,\n    limit          : pageLimit,\n    fetchLimit     : pageLimit + 1,\n    caseId         : caseIdSafe,\n    messageId      : msgIdSafe,\n    // Said out loud, so a caller that sent nonsense can see what was used instead.\n    limitDefaulted : (rawLimit == null || wanted <= 0),\n    limitClamped   : (wanted > MAX_LIMIT),\n    limitRaised    : (wanted > 0 && wanted < MIN_LIMIT),\n    offsetDefaulted: (rawOffset == null),\n    maxLimit       : MAX_LIMIT,\n    minLimit       : MIN_LIMIT\n]\nif (rejected) out.idCharactersStripped = rejected\nif (!caseIdSafe || !msgIdSafe) out.idsMissing = true\nreturn out\n",
				"compile_static":false,
				"captureStdOutput":false,
				"parameters":{
					"caseId":"{{ n_v4sZv.outputs.case_id }}",
					"limit":"{{ n_v4sZv.outputs.limit }}",
					"messageId":"{{ n_v4sZv.outputs.message_id }}",
					"offset":"{{ n_v4sZv.outputs.offset }}"
				}
			},
			"skip":false,
			"subTitle":"Groovy",
			"title":"Resolve Page Window",
			"type":"ACTION"
		},
		{
			"context":{
				"appName":"analytics_by_unifyapps",
				"resourceVersion":2214,
				"resourceName":"analytics_by_unifyapps_execute_query",
				"type":"APPLICATION"
			},
			"debug":false,
			"dirty":false,
			"fallbackMode":"STOP",
			"groupId":"_JAI60-1",
			"id":"n_cnFNH",
			"index":7,
			"inputs":{
				"outputSchema":{
					"additionalProperties":false,
					"properties":{
						"aiAgentId":{
							"title":"aiAgentId",
							"type":"string"
						},
						"billableInputTokens":{
							"title":"billableInputTokens",
							"type":"integer"
						},
						"cacheReadCost":{
							"title":"cacheReadCost",
							"type":"integer"
						},
						"cacheReadInputTokens":{
							"title":"cacheReadInputTokens",
							"type":"integer"
						},
						"cacheWriteCost":{
							"title":"cacheWriteCost",
							"type":"number"
						},
						"cacheWriteInputTokens":{
							"title":"cacheWriteInputTokens",
							"type":"integer"
						},
						"cost":{
							"title":"cost",
							"type":"number"
						},
						"currentType":{
							"title":"currentType",
							"type":"string"
						},
						"executionInstanceId":{
							"title":"executionInstanceId",
							"type":"string"
						},
						"finishReason":{
							"title":"finishReason",
							"type":"string"
						},
						"guardrailViolationDetected":{
							"title":"guardrailViolationDetected",
							"type":"boolean"
						},
						"guardrailViolationReason":{
							"title":"guardrailViolationReason",
							"type":"string"
						},
						"id":{
							"title":"id",
							"type":"string"
						},
						"inputToken":{
							"title":"inputToken",
							"type":"integer"
						},
						"inputTokenCost":{
							"title":"inputTokenCost",
							"type":"number"
						},
						"llmErrorType":{
							"title":"llmErrorType",
							"type":"string"
						},
						"llmTimeTaken":{
							"title":"llmTimeTaken",
							"type":"integer"
						},
						"modelId":{
							"title":"modelId",
							"type":"string"
						},
						"outputToken":{
							"title":"outputToken",
							"type":"integer"
						},
						"outputTokenCost":{
							"title":"outputTokenCost",
							"type":"number"
						},
						"parent":{
							"title":"parent",
							"type":"string"
						},
						"parentExecutionId":{
							"title":"parentExecutionId",
							"type":"string"
						},
						"provider":{
							"title":"provider",
							"type":"string"
						},
						"rootExecutionId":{
							"title":"rootExecutionId",
							"type":"string"
						},
						"startTime":{
							"title":"startTime",
							"type":"integer"
						},
						"success":{
							"title":"success",
							"type":"boolean"
						},
						"toolActionId":{
							"title":"toolActionId",
							"type":"string"
						},
						"toolAppName":{
							"title":"toolAppName",
							"type":"string"
						},
						"toolInvocationCost":{
							"title":"toolInvocationCost",
							"type":"number"
						},
						"toolName":{
							"title":"toolName",
							"type":"string"
						}
					},
					"required":[],
					"type":"object"
				},
				"query":"SELECT\n  id, \n  _pr_executionInstanceId AS executionInstanceId,\n  _pr_parentExecutionId   AS parentExecutionId,\n  _pr_rootExecutionId     AS rootExecutionId,\n  _pr_parent          AS parent,\n  _pr_currentType     AS currentType,\n  _pr_startTime       AS startTime,\n  _pr_llmTimeTaken    AS llmTimeTaken,\n  _pr_success         AS success,\n  _pr_aiAgentId       AS aiAgentId,\n  _pr_provider        AS provider,\n  _pr_modelId         AS modelId,\n  _pr_finish_reason   AS finishReason,\n  _pr_llmErrorType    AS llmErrorType,\n  _pr_cost            AS cost,\n  _pr_inputToken      AS inputToken,\n  _pr_outputToken     AS outputToken,\n  _pr_billableInputTokens  AS billableInputTokens,\n  _pr_inputTokenCost       AS inputTokenCost,\n  _pr_outputTokenCost      AS outputTokenCost,\n  _pr_cacheReadInputTokens AS cacheReadInputTokens,\n  _pr_cacheWriteInputTokens AS cacheWriteInputTokens,\n  _pr_cacheReadCost        AS cacheReadCost,\n  _pr_cacheWriteCost       AS cacheWriteCost,\n  _pr_toolName        AS toolName,\n  _pr_toolAppName     AS toolAppName,\n  _pr_toolActionId    AS toolActionId,\n  _pr_toolInvocationCost AS toolInvocationCost,\n  _pr_guardrailViolationDetected AS guardrailViolationDetected,\n  _pr_guardrailViolationReason   AS guardrailViolationReason\nFROM ENTITY_e_traces\nWHERE _pr_caseId = '{{ n_pgCfg.outputs.result.caseId }}'\n  AND _pr_messageId = '{{ n_pgCfg.outputs.result.messageId }}'\nORDER BY COALESCE(_pr_startTime, 0) ASC, id ASC\nLIMIT {{ n_pgCfg.outputs.result.fetchLimit }}\nOFFSET {{ n_pgCfg.outputs.result.offset }}",
				"record":{
					"case_id":"{{ n_pgCfg.outputs.result.caseId }}",
					"limit":"{{ n_pgCfg.outputs.result.fetchLimit }}",
					"message_id":"{{ n_pgCfg.outputs.result.messageId }}",
					"offset":"{{ n_pgCfg.outputs.result.offset }}"
				},
				"params":{
					"additionalProperties":false,
					"properties":{
						"case_id":{
							"title":"Case Id",
							"type":"string"
						},
						"limit":{
							"title":"Limit",
							"type":"integer"
						},
						"message_id":{
							"title":"Message Id",
							"type":"string"
						},
						"offset":{
							"title":"Offset",
							"type":"integer"
						}
					},
					"required":[],
					"type":"object"
				},
				"group":"ENTITY_REPORTING"
			},
			"skip":false,
			"subTitle":"Analytics by UnifyApps",
			"title":"Execute Analytics SQL Query",
			"type":"ACTION"
		},
		{
			"context":{
				"appName":"code_by_unifyapps",
				"resourceVersion":970,
				"resourceName":"code_by_unifyapps_groovy",
				"type":"APPLICATION"
			},
			"debug":false,
			"dirty":false,
			"fallbackMode":"STOP",
			"groupId":"_JAI60-1",
			"id":"n_APnyI",
			"index":8,
			"inputs":{
				"output":{
					"additionalProperties":false,
					"properties":{
						"currentSpans":{
							"items":{
								"additionalProperties":false,
								"properties":{
									"aiAgentId":{
										"title":"aiAgentId",
										"type":"string"
									},
									"billableInputTokens":{
										"title":"billableInputTokens",
										"type":"integer"
									},
									"cacheReadCost":{
										"title":"cacheReadCost",
										"type":"integer"
									},
									"cacheReadInputTokens":{
										"title":"cacheReadInputTokens",
										"type":"integer"
									},
									"cacheWriteCost":{
										"title":"cacheWriteCost",
										"type":"integer"
									},
									"cacheWriteInputTokens":{
										"title":"cacheWriteInputTokens",
										"type":"integer"
									},
									"cost":{
										"title":"cost",
										"type":"number"
									},
									"currentType":{
										"title":"currentType",
										"type":"string"
									},
									"executionInstanceId":{
										"title":"executionInstanceId",
										"type":"string"
									},
									"finishReason":{
										"title":"finishReason",
										"type":"string"
									},
									"guardrailViolationDetected":{
										"title":"guardrailViolationDetected",
										"type":"boolean"
									},
									"guardrailViolationReason":{
										"title":"guardrailViolationReason",
										"type":"string"
									},
									"id":{
										"title":"id",
										"type":"string"
									},
									"inputToken":{
										"title":"inputToken",
										"type":"integer"
									},
									"inputTokenCost":{
										"title":"inputTokenCost",
										"type":"number"
									},
									"llmErrorType":{
										"title":"llmErrorType",
										"type":"string"
									},
									"llmTimeTaken":{
										"title":"llmTimeTaken",
										"type":"integer"
									},
									"modelId":{
										"title":"modelId",
										"type":"string"
									},
									"outputToken":{
										"title":"outputToken",
										"type":"integer"
									},
									"outputTokenCost":{
										"title":"outputTokenCost",
										"type":"number"
									},
									"parent":{
										"title":"parent",
										"type":"string"
									},
									"parentExecutionId":{
										"title":"parentExecutionId",
										"type":"string"
									},
									"provider":{
										"title":"provider",
										"type":"string"
									},
									"rootExecutionId":{
										"title":"rootExecutionId",
										"type":"string"
									},
									"spanIndex":{
										"title":"spanIndex",
										"type":"integer"
									},
									"startTime":{
										"title":"startTime",
										"type":"integer"
									},
									"success":{
										"title":"success",
										"type":"boolean"
									},
									"toolActionId":{
										"title":"toolActionId",
										"type":"string"
									},
									"toolAppName":{
										"title":"toolAppName",
										"type":"string"
									},
									"toolInvocationCost":{
										"title":"toolInvocationCost",
										"type":"integer"
									},
									"toolName":{
										"title":"toolName",
										"type":"string"
									}
								},
								"required":[],
								"type":"object"
							},
							"title":"currentSpans",
							"type":"array"
						},
						"page":{
							"additionalProperties":false,
							"properties":{},
							"title":"Page",
							"type":"object"
						}
					},
					"type":"object"
				},
				"input":{
					"additionalProperties":false,
					"properties":{
						"messageId":{
							"title":"Message Id",
							"type":"string"
						},
						"pageLimit":{
							"title":"Page Limit",
							"type":"integer"
						},
						"pageOffset":{
							"title":"Page Offset",
							"type":"integer"
						},
						"spans":{
							"items":{
								"additionalProperties":false,
								"properties":{},
								"type":"object"
							},
							"title":"Spans",
							"type":"array"
						}
					},
					"required":[
						"spans",
						"messageId"
					],
					"type":"object"
				},
				"code":"def spans = (binding.hasVariable('spans') && spans != null) ? spans : []\n\ndef asLong = { v ->\n    if (v == null) return 0L\n    try { return (v as BigDecimal).longValue() } catch (ignored) { return 0L }\n}\n\ndef pageOffset = Math.max(0L, asLong(binding.hasVariable('pageOffset') ? pageOffset : null))\ndef pageLimit  = asLong(binding.hasVariable('pageLimit') ? pageLimit : null)\ndef paging     = pageLimit > 0\n\n// the order\n// This comparator MUST be the SQL's ORDER BY, character for character in meaning:\n//\n//     ORDER BY COALESCE(_pr_startTime, 0) ASC, id ASC\n//\n// Two separate reasons, and both are silent when they go wrong.\n//\n//  1. WHICH ROWS ARE IN THE PAGE is decided by the database. `startTime` alone is not a total\n//     order — 174 of one real 546-row case share a millisecond — so without `id` the engine may\n//     return two tied rows in either order per query, and a boundary landing inside a tie repeats\n//     one row on the next page while dropping another.\n//\n//  2. WHICH INDEX EACH ROW GETS is decided here. `spanIndex = offset + position`, so if this node\n//     re-orders the page by anything the database did not order by, the index stops being the\n//     row's position in the whole result and becomes its position in a local reshuffle. The old\n//     comparator had a third key (LLM/Retrieval before Tool) that did exactly that. It is gone:\n//     ownership is resolved from rootExecutionId, not from array adjacency, so nothing needs it.\n//\n// COALESCE is in the SQL because a row with a NULL startTime is real (one exists in the sampled\n// case) and NULL ordering in ASC is engine-defined — first in some engines, last in others. Pinning\n// it to 0 makes the page a row lands on deterministic instead of a property of the database.\ndef keyOf = { s -> [asLong(s?.startTime), ((s?.id ?: '') as String)] }\ndef cmp = { a, b -> (keyOf(a)[0] <=> keyOf(b)[0]) ?: (keyOf(a)[1] <=> keyOf(b)[1]) }\n\ndef arrived = spans.collect { it?.columns ?: it }\ndef ordered = arrived.sort(false, cmp)\n\n// If the rows did not ARRIVE in that order, the SQL's ORDER BY is not what this node assumes and\n// the page boundaries cannot be trusted — so it is reported rather than silently corrected.\ndef orderStable = true\nfor (int i = 0; i < arrived.size(); i++) {\n    if (!arrived[i].is(ordered[i])) { orderStable = false; break }\n}\n\n// The probe row (see n_pgCfg.fetchLimit) never leaves this node.\ndef hasMore = paging && ordered.size() > pageLimit\ndef pageRows = hasMore ? ordered.subList(0, (int) pageLimit) : ordered\n\n// never cut inside a millisecond\n// Rows that share a startTime are one instant of the turn, and several rules downstream pair rows\n// WITHIN that instant:\n//\n//   * a retried LLM call writes two rows with the SAME executionInstanceId and the SAME startTime.\n//     Split them and the failed attempt reads as a real failure on one page and the successful one\n//     as an unexplained duplicate on the next.\n//   * a manager firing tools in parallel starts several loops in one millisecond; the delegation\n//     wrapper and the loop it opened have to be on the same page to be seen as related.\n//\n// So the boundary is pulled back to the last COMPLETE startTime group. The page comes back a few\n// rows short of `limit`, `nextOffset` is `offset + returned` as always, and the pages still tile\n// the result exactly — they just tile it on instant boundaries instead of arbitrary ones. Measured\n// on a 546-span case with 174 tied rows, this is what makes retriedLlmCalls identical at every\n// page size instead of drifting 86 -> 84 as the page shrinks.\ndef tieTrimmed = 0\ndef tieUnavoidable = false\nif (hasMore && pageRows) {\n    def boundaryTime = asLong(pageRows[pageRows.size() - 1]?.startTime)\n    if (asLong(ordered[(int) pageLimit]?.startTime) == boundaryTime) {\n        int cut = pageRows.size()\n        while (cut > 0 && asLong(pageRows[cut - 1]?.startTime) == boundaryTime) cut--\n        if (cut > 0) {\n            tieTrimmed = pageRows.size() - cut\n            pageRows = pageRows.subList(0, cut)\n        } else {\n            // The whole page is one instant — trimming would return nothing and stall the caller.\n            // Deliver it and say the instant was split.\n            tieUnavoidable = true\n        }\n    }\n}\n\n// Global numbering. Every span carries its position in the WHOLE ordered result set, not its\n// position in this page, so page 2 of a 546-span message continues at 300 instead of restarting at\n// 0 and a caller can concatenate pages without renumbering anything. The nested tool / aux rows\n// that n_VYOw1 folds under an owner deep-copy the span, so they inherit it for free — previously a\n// nested row had no index at all.\ndef currentSpans = []\npageRows.eachWithIndex { s, i ->\n    def rec = new LinkedHashMap(s instanceof Map ? s : [:])\n    rec.spanIndex = pageOffset + i\n    currentSpans << rec\n}\n\ndef last = currentSpans ? currentSpans[currentSpans.size() - 1] : null\n\ndef page = [\n    enabled       : paging,\n    offset        : pageOffset,\n    limit         : pageLimit,\n    returned      : currentSpans.size(),\n    hasMore       : hasMore,\n    nextOffset    : hasMore ? (pageOffset + currentSpans.size()) : null,\n    firstSpanIndex: currentSpans ? pageOffset : null,\n    lastSpanIndex : currentSpans ? (pageOffset + currentSpans.size() - 1) : null,\n    // True whenever this page is a WINDOW rather than the whole message. Everything the downstream\n    // nodes derive per-thread — the diff baseline, firstTurnForAgent, who delegated to whom, the\n    // turn's totals — is scoped to the window when this is set.\n    partial       : (pageOffset > 0 || hasMore),\n\n    // Keyset cursor, free of charge. The ORDER BY is already the total order, so a caller that\n    // wants to stop paying OFFSET's linear re-scan on deep pages can switch to\n    //   WHERE (COALESCE(_pr_startTime,0), id) > (nextAfterStartTime, nextAfterId)\n    // with no change to any node here.\n    nextAfterStartTime: hasMore ? asLong(last?.startTime) : null,\n    nextAfterId       : hasMore ? ((last?.id ?: null) as String) : null\n]\n// Only present when something is wrong, so a healthy response carries no diagnostic keys.\nif (!orderStable) {\n    page.orderStable = false\n    page.orderWarning = 'rows did not arrive in (COALESCE(startTime,0), id) order — check the ' +\n                        'ORDER BY in n_cnFNH; page boundaries and spanIndex cannot be trusted'\n}\nif (tieTrimmed > 0) page.tieTrimmed = tieTrimmed\nif (tieUnavoidable) {\n    page.tieSplit = true\n    page.tieWarning = 'this page is a single startTime and had to be split — retry pairing and ' +\n                      'delegation edges spanning the cut cannot be resolved; use a larger limit'\n}\nif (paging && !hasMore && currentSpans.size() == pageLimit) {\n    // The probe row is what distinguishes \"exactly one page left\" from \"the engine capped us\".\n    page.exactlyFull = true\n}\n\nreturn [currentSpans: currentSpans, page: page]\n",
				"compile_static":false,
				"captureStdOutput":false,
				"parameters":{
					"messageId":"{{ n_v4sZv.outputs.message_id }}",
					"pageLimit":"{{ n_pgCfg.outputs.result.limit }}",
					"pageOffset":"{{ n_pgCfg.outputs.result.offset }}",
					"spans":{
						"items":"{{ n_cnFNH.outputs.objects[0] }}",
						"source":"{{ n_cnFNH.outputs.objects }}",
						"ua:type":"mappedArray"
					}
				}
			},
			"skip":false,
			"subTitle":"Code by UnifyApps",
			"title":"Traces (Current Message)",
			"type":"ACTION"
		},
		{
			"context":{
				"appName":"code_by_unifyapps",
				"resourceVersion":0,
				"resourceName":"code_by_unifyapps_groovy",
				"type":"APPLICATION"
			},
			"debug":false,
			"dirty":false,
			"fallbackMode":"STOP",
			"groupId":"_JAI60-1",
			"id":"n_VYOw1",
			"index":9,
			"inputs":{
				"output":{
					"additionalProperties":false,
					"properties":{
						"auxByOwnerIndex":{
							"additionalProperties":false,
							"properties":{},
							"title":"Aux By Owner Index",
							"type":"object"
						},
						"delegationToolIds":{
							"items":{
								"type":"string"
							},
							"title":"Delegation Tool Ids",
							"type":"array"
						},
						"grouping":{
							"additionalProperties":false,
							"properties":{},
							"title":"Grouping",
							"type":"object"
						},
						"ownerMetaByIndex":{
							"additionalProperties":false,
							"properties":{},
							"title":"Owner Meta By Index",
							"type":"object"
						},
						"previewCharsPerSpan":{
							"title":"Preview Chars Per Span",
							"type":"integer"
						},
						"retrievalStepsByOwnerIndex":{
							"additionalProperties":false,
							"properties":{},
							"title":"Retrieval Steps By Owner Index",
							"type":"object"
						},
						"skipIndices":{
							"items":{
								"type":"string"
							},
							"title":"Skip Indices",
							"type":"array"
						},
						"toolsByOwnerIndex":{
							"additionalProperties":false,
							"properties":{},
							"title":"Tools By Owner Index",
							"type":"object"
						},
						"unattributedSteps":{
							"items":{
								"additionalProperties":false,
								"properties":{},
								"type":"object"
							},
							"title":"Unattributed Steps",
							"type":"array"
						}
					},
					"required":[
						"toolsByOwnerIndex",
						"retrievalStepsByOwnerIndex",
						"auxByOwnerIndex",
						"unattributedSteps",
						"ownerMetaByIndex",
						"delegationToolIds",
						"skipIndices",
						"previewCharsPerSpan",
						"grouping"
					],
					"type":"object"
				},
				"input":{
					"additionalProperties":false,
					"properties":{
						"currentSpans":{
							"items":{
								"additionalProperties":false,
								"properties":{},
								"type":"object"
							},
							"title":"Current Spans",
							"type":"array"
						},
						"page":{
							"additionalProperties":false,
							"properties":{},
							"title":"Page",
							"type":"object"
						}
					},
					"required":[
						"currentSpans"
					],
					"type":"object"
				},
				"code":"// Build Trace Owners (by thread)\n//\n// Ownership used to be decided by ADJACENCY in a wall-clock-sorted array: walk forward from an\n// LLM row collecting consecutive Tool rows. That is only correct while every span in the turn is\n// strictly serialized, and a team turn is exactly what it is not.\n//\n// The hierarchy is on every row. Measured against real turns, the three id columns mean:\n//\n//   executionInstanceId  the individual STEP's run — one per row. NOT a react loop, so it is\n//                        useless as a thread key (keying on it made every span a thread start).\n//   parentExecutionId    that step's immediate caller in the workflow runtime. Also per-step: in\n//                        a 4-agent turn NONE of the tool rows' parents resolved to an LLM row.\n//   rootExecutionId      the top of the call chain = THE REACT LOOP. This is the thread key.\n//                        In a 99-row / 4-agent turn it yields 12 threads, no thread ever spans two\n//                        agents, and one agent invoked 8 times in parallel gets 8 distinct threads\n//                        — which aiAgentId alone would have merged into one.\n//\n// Tool rows carry their thread's root too (33/35 and 46/46 on the two sampled turns). The handful\n// that start their own chain fall back to the same agent's nearest preceding LLM call, and only\n// then to adjacency. `attachedVia` on every child and `grouping.mode` on the turn say which rule\n// actually produced the answer, so a fallback is never silent.\n\ndef currentSpans = (binding.hasVariable('currentSpans') && currentSpans != null) ? currentSpans : []\n\n// The page window this call is looking at (n_APnyI.page). A page is a WINDOW on the turn and a\n// react loop does not stop at the window's edge, so several rules below are only sound for a page\n// that starts at 0 and has nothing after it. Where they are not, the OUTPUT says so instead of\n// reporting a windowed answer as a complete one.\ndef page = (binding.hasVariable('page') && page instanceof Map) ? page : [:]\n\ndef str = { v -> (v == null) ? null : (v as String)?.trim() ?: null }\ndef asLong = { v ->\n    if (v == null) return 0L\n    try { return (v as BigDecimal).longValue() } catch (ignored) { return 0L }\n}\n\ndef pageOffset  = Math.max(0L, asLong(page?.offset))\ndef leadingCut  = pageOffset > 0            // rows of this message exist BEFORE this page\ndef trailingCut = (page?.hasMore == true)   // ... and/or AFTER it\ndef windowed    = leadingCut || trailingCut\n\n// span classification\n// Exactly the four categories the BRANCH routes, plus the two child kinds it nests.\n// ANYTHING ELSE is `aux`: PII Mask, Content Filters, Semantic Search, Reranker, Fetch Embeddings,\n// Topic Selection, and any future step type. Aux rows used to be collected and then dropped on the\n// floor (`interstitialsByOwnerIndex` was never read by anything), which cost 122 of the sample\n// turn's 245 rows. They are now attached to the step they belong to and their time is reported\n// under their own currentType.\ndef kindOf = { s ->\n    def p = str(s?.parent)\n    def t = str(s?.currentType)\n    if (p == 'Agent Execution' && t == 'LLM') return 'llm'\n    if (p == 'Agent Execution' && t == 'Tool') return 'tool'\n    if (p == 'Input Guardrails') return 'inputGuardrail'\n    if (p == 'Response Guardrail') return 'responseGuardrail'\n    if (p == 'ReferFromKnowledge' && t == 'Retrieval') return 'retrieval'\n    if (p == 'Retrieval') return 'retrievalStep'\n    return 'aux'\n}\n\ndef n = currentSpans.size()\ndef kinds = (0..<n).collect { kindOf(currentSpans[it]) }\ndef agentIdOf = (0..<n).collect { str(currentSpans[it]?.aiAgentId) }\ndef rootIds = (0..<n).collect { str(currentSpans[it]?.rootExecutionId) }\ndef parentExecIds = (0..<n).collect { str(currentSpans[it]?.parentExecutionId) }\ndef execIds = (0..<n).collect { str(currentSpans[it]?.executionInstanceId) }\n\n// The react loop this row belongs to.\ndef threadOf = (0..<n).collect { i ->\n    rootIds[i] ?: parentExecIds[i] ?: ('__agent:' + (agentIdOf[i] ?: 'unknown'))\n}\ndef idsAvailable = rootIds.any { it != null }\n\n// page boundary\n// The thread holding the window's FIRST row certainly continues before it, and the thread holding\n// its LAST row certainly continues after it. Others may too — with rows ordered by wall clock and\n// several react loops running concurrently, any loop that was open at the cut is split — which is\n// exactly why nothing here tries to guess per-thread completeness. What is reported is the cut\n// itself; the consumers that would otherwise assert something unknowable (n_VgcsB's\n// `firstTurnForAgent`, the delegation edges below, the turn totals in n_WB2iD) read the same flags\n// and degrade to \"unknown on this page\" rather than to a confident wrong answer.\ndef edgeThreads = new LinkedHashSet()\nif (n > 0) {\n    if (leadingCut)  edgeThreads << threadOf[0]\n    if (trailingCut) edgeThreads << threadOf[n - 1]\n}\n\n// Threads that actually ran an LLM call — the only ones a child can be attributed to.\ndef threadRows = new LinkedHashMap()\n(0..<n).each { i -> threadRows.get(threadOf[i], []) << i }\ndef agentRows = new LinkedHashMap()\n(0..<n).each { i -> if (agentIdOf[i] != null) agentRows.get(agentIdOf[i], []) << i }\ndef llmThreads = threadRows.findAll { k, v -> v.any { kinds[it] == 'llm' } }\n\n// owner resolution\ndef ownerKinds = ['llm', 'inputGuardrail', 'responseGuardrail', 'retrieval'] as Set\n\n// The latest owner row in `rows` that started at or before `t`, optionally restricted to a kind.\ndef latestOwnerIn = { List rows, long t, Set wanted ->\n    def best = -1\n    rows.each { i ->\n        if (!wanted.contains(kinds[i])) return\n        if (asLong(currentSpans[i]?.startTime) > t) return\n        if (best < 0 || asLong(currentSpans[i]?.startTime) >= asLong(currentSpans[best]?.startTime)) best = i\n    }\n    return best\n}\n\n// The thread's first owner — for steps that ran before any LLM call in it.\ndef firstOwnerIn = { List rows, Set wanted ->\n    def best = -1\n    rows.each { i ->\n        if (!wanted.contains(kinds[i])) return\n        if (best < 0 || asLong(currentSpans[i]?.startTime) < asLong(currentSpans[best]?.startTime)) best = i\n    }\n    return best\n}\n\n// Pre-projection fallback: nearest preceding owner anywhere in the array. This is the old\n// adjacency rule, kept so rows written before the id projection still group the way they do today.\ndef latestOwnerBefore = { int idx, Set wanted ->\n    for (int j = idx - 1; j >= 0; j--) { if (wanted.contains(kinds[j])) return j }\n    return -1\n}\n\ndef toolsByOwnerIndex = new LinkedHashMap()\ndef retrievalStepsByOwnerIndex = new LinkedHashMap()\ndef auxByOwnerIndex = new LinkedHashMap()\ndef unattributedSteps = []\ndef skipIndices = new LinkedHashSet()\ndef viaCounts = new LinkedHashMap()\ndef bump = { k -> viaCounts[k] = (viaCounts[k] ?: 0) + 1 }\n\ndef attach = { Map bucket, int owner, int child, String via ->\n    def rec = new LinkedHashMap(currentSpans[child] instanceof Map ? currentSpans[child] : [:])\n    rec.attachedVia = via\n    bucket.get(owner.toString(), []) << rec\n    skipIndices << child.toString()\n    bump(via)\n}\n\n(0..<n).each { i ->\n    def kind = kinds[i]\n    // owners route through the BRANCH as before\n    if (ownerKinds.contains(kind)) return\n    def t = asLong(currentSpans[i]?.startTime)\n    def mine = threadRows[threadOf[i]] ?: []\n    def sameAgent = agentIdOf[i] != null ? (agentRows[agentIdOf[i]] ?: []) : []\n\n    def wanted = (kind == 'tool') ? (['llm'] as Set)\n               : (kind == 'retrievalStep') ? (['retrieval'] as Set)\n               : ownerKinds\n    def bucket = (kind == 'tool') ? toolsByOwnerIndex\n               : (kind == 'retrievalStep') ? retrievalStepsByOwnerIndex\n               : auxByOwnerIndex\n\n    // 1. the same react loop\n    def ownerIndex = idsAvailable ? latestOwnerIn(mine, t, wanted) : -1\n    if (ownerIndex >= 0) { attach(bucket, ownerIndex, i, 'threadRoot'); return }\n    // 2. the same agent — for the few rows that start their own chain\n    ownerIndex = idsAvailable ? latestOwnerIn(sameAgent, t, wanted) : -1\n    if (ownerIndex >= 0) { attach(bucket, ownerIndex, i, 'agentId'); return }\n    // 2b. setup steps (workspace sync, conversation load, agent init) run BEFORE the thread's\n    // first LLM call, so no owner precedes them. They belong to the call they were preparing.\n    if (idsAvailable && kind == 'aux') {\n        ownerIndex = firstOwnerIn(mine, wanted)\n        if (ownerIndex >= 0) { attach(bucket, ownerIndex, i, 'threadRootPrefix'); return }\n    }\n    // 3. wall clock, the pre-projection rule\n    ownerIndex = latestOwnerBefore(i, wanted)\n    if (ownerIndex >= 0) { attach(bucket, ownerIndex, i, 'adjacency'); return }\n    // Nothing to hang it on. It is still REPORTED — with its own currentType and its own time —\n    // never silently dropped the way it used to be.\n    unattributedSteps << currentSpans[i]; skipIndices << i.toString(); bump('unattributed')\n}\n\n// delegation\ndef delegationToolIds = new LinkedHashSet()\ndef delegationEdges = []\nif (idsAvailable) {\n    // A delegation wrapper is a tool whose interval CONTAINS the start of another react loop —\n    // an InvokeAgent standing in for a whole delegated run, or a tool that itself runs an agent.\n    // Its duration already contains that loop's LLM and tool time, so adding it to toolTimeMs\n    // counts a team turn twice (one sampled turn reported toolTimeMs 3040137 against latency\n    // 2754308 — more tool time than the turn lasted).\n    //\n    // parentExecutionId is NOT usable for this: it is a per-step workflow-runtime link and none of\n    // the sampled tool rows' parents resolved to an LLM row. Interval containment is what the data\n    // actually supports.\n    def threadStart = [:]\n    def threadAgent = [:]\n    llmThreads.each { k, v ->\n        def first = v.findAll { kinds[it] == 'llm' }.min { asLong(currentSpans[it]?.startTime) }\n        if (first != null) {\n            threadStart[k] = asLong(currentSpans[first]?.startTime)\n            threadAgent[k] = agentIdOf[first]\n        }\n    }\n    (0..<n).each { j ->\n        if (kinds[j] != 'tool') return\n        def s0 = asLong(currentSpans[j]?.startTime)\n        def e0 = s0 + asLong(currentSpans[j]?.llmTimeTaken)\n        if (e0 <= s0) return\n        // A DELEGATION is a hand-off to ANOTHER AGENT. Requiring the covered loop to belong to a\n        // different agent is not cosmetic: without it the rule is page-size sensitive, because on a\n        // small window \"the first call of loop K seen here\" is not loop K's real first call, and a\n        // short tool can appear to contain a loop of its OWN agent that was already running. That\n        // loop's time is already the same agent's, so calling it delegated would move real tool\n        // time into delegationTimeMs. Measured across page sizes 7..1000 on a 546-span case, this\n        // makes delegationTimeMs identical at every size.\n        def mine = agentIdOf[j]\n        def covered = threadStart.findAll { k, ts ->\n            k != threadOf[j] && ts >= s0 && ts <= e0 &&\n            threadAgent[k] != null && mine != null && threadAgent[k] != mine\n        }\n        if (!covered) return\n        delegationToolIds << (str(currentSpans[j]?.id) ?: j.toString())\n        delegationEdges << [\n            viaToolName    : str(currentSpans[j]?.toolName),\n            viaTraceId     : str(currentSpans[j]?.id),\n            fromThread     : threadOf[j],\n            coveredThreads : covered.keySet() as List\n        ]\n    }\n}\n\n// retry classification\n// A retried LLM call is a RECURSION in the no-code runtime: `Call LLM (AI Agent)` calls itself on\n// the error branch (node `5bp5f`, retry_count defaults to 3), and every frame writes rows under its\n// own executionInstanceId. Three kinds of row come out of that, and they must be counted\n// differently:\n//\n//   attempt      success=false — the frame's own failed try. Real wall clock, no charge.\n//   billed       success=true  — the frame that finally got a response. Carries the charge.\n//   retry rollup success=true  — an ANCESTOR frame re-reporting its descendant's result, written\n//                                by the duplicate Capture Trace node `_uMwW1` that sat on the error\n//                                branch until 2026-08-16 (removed in #41489, re-introduced by an\n//                                asset sync, re-removed in #42560). It repeats the winner's cost\n//                                and tokens verbatim and its duration spans the whole chain, so\n//                                billing it charges one call up to four times. On a sampled\n//                                546-span case that was 86 rows, 67.60 of 295.04 — cost 29.7% high.\n//\n// A rollup is identified WITHOUT looking at its neighbours: it is a success row that shares its\n// FRAME (executionInstanceId + startTime, both stamped once per frame from `GQFTz`) with a failed\n// row and ran at least as long. Only `_uMwW1` can write that combination — the frame's own attempt\n// failed, so any success it reports came from a descendant, and its duration runs to the end of the\n// chain because `llmTimeTaken` is evaluated after the recursive call returned.\n//\n// Being frame-local is what makes it survive pagination. n_APnyI never splits rows that share a\n// startTime, so a frame is always whole on one page and the verdict cannot move with the page size\n// — measured identical at page sizes 25 through 546. Traces written after the fix contain no such\n// rows and this finds nothing, which is exactly what it should do.\ndef llmRows = (0..<n).findAll { kinds[it] == 'llm' }\n\ndef frameRows = new LinkedHashMap()\nllmRows.each { i ->\n    frameRows.get((execIds[i] ?: i.toString()) + '@' + asLong(currentSpans[i]?.startTime), []) << i\n}\n\ndef retryRollups = new LinkedHashSet()\nframeRows.each { key, rowsOfFrame ->\n    def failures = rowsOfFrame.findAll { currentSpans[it]?.success == false }\n    if (!failures) return\n    def longestFailure = failures.collect { asLong(currentSpans[it]?.llmTimeTaken) }.max()\n    rowsOfFrame.each { i ->\n        if (currentSpans[i]?.success == false) return\n        if (currentSpans[i]?.llmErrorType == null) return\n        if (asLong(currentSpans[i]?.llmTimeTaken) < longestFailure) return\n        retryRollups << i\n    }\n}\ndef failedAttempts = llmRows.findAll { currentSpans[it]?.success == false } as LinkedHashSet\n\n// One logical LLM call = one `parentExecutionId`. The recursion FORWARDS the value it received\n// rather than rebinding it to its own execution id, so every frame of one chain carries the same\n// one — which is what makes the column useless as a tree pointer (it resolves to a\n// WORKFLOW_EXECUTION row, never to another trace row) and exact as a chain key. Measured on the\n// 546-span case: 306 groups, every one a well-formed chain — zero or more failures, then at most\n// one success, all frames distinct, strictly sequential.\n//\n// The guard is not decoration. A writer that runs the whole react loop in ONE execution stamps the\n// same id in both columns, which would put the entire turn in a single group. When\n// parentExecutionId cannot discriminate — absent, or equal to the row's own executionInstanceId —\n// the frame is the call and the grouping degrades to one call per row rather than to one call per\n// turn.\ndef callIdOf = { int i ->\n    def parent = parentExecIds[i]\n    (parent != null && parent != execIds[i])\n        ? ('p:' + parent)\n        : ('x:' + (execIds[i] ?: i.toString()) + '@' + asLong(currentSpans[i]?.startTime))\n}\n\ndef callRows = new LinkedHashMap()\nllmRows.each { i -> if (!retryRollups.contains(i)) callRows.get(callIdOf(i), []) << i }\ndef answeredCalls = callRows.count { k, v -> v.any { currentSpans[it]?.success != false } }\ndef retriedCalls  = callRows.count { k, v -> v.size() > 1 && v.any { currentSpans[it]?.success != false } }\n\n// per-owner metadata\n// threadId + the previous LLM row of the SAME thread. The diff baseline needs the latter and it\n// stops being derivable the moment two agents interleave.\ndef ownerMetaByIndex = new LinkedHashMap()\ndef lastLlmOfThread = new LinkedHashMap()\n(0..<n).each { i ->\n    if (!ownerKinds.contains(kinds[i])) return\n    def key = threadOf[i]\n    def meta = [\n        threadId           : key,\n        rootExecutionId    : rootIds[i],\n        executionInstanceId: execIds[i],\n        parentExecutionId  : parentExecIds[i],\n        aiAgentId          : agentIdOf[i]\n    ]\n    // Only stamped when true, so a full (unpaged) run carries no extra keys at all.\n    if (leadingCut) meta.baselineOffPage = true\n    if (edgeThreads.contains(key)) meta.threadSpansPageEdge = true\n    if (kinds[i] == 'llm') {\n        meta.callId = callIdOf(i)\n        if (retryRollups.contains(i)) meta.retryRollup = true\n        // A rollup repeats a call that is already in this page and a failed attempt carries no\n        // assistant reply. Neither advances the conversation, so neither may become the diff\n        // baseline for the call that follows — that would leave the real call diffing against a\n        // prompt that was never sent, and report its whole history as new.\n        def carriesReply = !retryRollups.contains(i) && currentSpans[i]?.success != false\n        meta.threadPrevIndex = (lastLlmOfThread[key] == null) ? -1 : lastLlmOfThread[key]\n        if (carriesReply) lastLlmOfThread[key] = i\n    }\n    ownerMetaByIndex[i.toString()] = meta\n}\n\n// preview size decision\n// A tool preview is per span, so a fixed 2000 chars is fine at 20 spans and 1.2 MB at 300. The\n// budget is set ONCE here, from the number of spans that will actually be rendered (the children\n// already folded into skipIndices are not rendered at the top level), and n_cFcDR uses it instead\n// of a hard-coded number. outputChars always carries the FULL length, so a reader can always see\n// how much was cut.\ndef renderedSpans = n - skipIndices.size()\ndef previewCharsPerSpan = (renderedSpans <= 20) ? 2000\n                        : (renderedSpans <= 50) ? 1000\n                        : (renderedSpans <= 150) ? 400\n                        : 200\n\n// Stamp the verdict onto the tool rows THEMSELVES, in the buckets they were just filed into.\n// It used to travel as a separate `delegationToolIds` list that n_t3ANJ had to be wired to read\n// and then copy across — a three-hop dependency where any missing wire silently produced a turn\n// whose toolTimeMs double-counted every delegated run. The row now carries its own answer, and\n// n_WB2iD re-derives the same verdict independently as a backstop.\n// The unattributed list gets the same stamp. A wrapper whose own LLM call is in an earlier page\n// arrives with no owner to nest under, and it used to lose its verdict here and be billed as\n// ordinary tool time — 105,978 ms of another agent's whole run, on a page boundary that happened to\n// fall between the wrapper and its caller.\ndef stampDelegation = { rowsToStamp ->\n    rowsToStamp.each { r ->\n        def rid = (r?.id ?: null) as String\n        if (rid != null && delegationToolIds.contains(rid)) r.isDelegation = true\n    }\n}\ntoolsByOwnerIndex.each { owner, rowsOfOwner -> stampDelegation(rowsOfOwner) }\nstampDelegation(unattributedSteps)\n\ndef agentIds = agentIdOf.findAll { it != null }.unique()\ndef mode = !idsAvailable ? 'adjacency'\n         : ((viaCounts['adjacency'] || viaCounts['agentId']) ? 'mixed' : 'threadRoot')\n\nreturn [\n    toolsByOwnerIndex         : toolsByOwnerIndex,\n    retrievalStepsByOwnerIndex: retrievalStepsByOwnerIndex,\n    auxByOwnerIndex           : auxByOwnerIndex,\n    unattributedSteps         : unattributedSteps,\n    ownerMetaByIndex          : ownerMetaByIndex,\n    delegationToolIds         : delegationToolIds as List,\n    skipIndices               : skipIndices as List,\n    previewCharsPerSpan       : previewCharsPerSpan,\n    grouping                  : [\n        mode                : mode,\n        idsAvailable        : idsAvailable,\n        spanCount           : n,\n        renderedSpans       : renderedSpans,\n        previewCharsPerSpan : previewCharsPerSpan,\n        threadCount         : llmThreads.size(),\n        agentCount          : agentIds.size(),\n        attachedByThreadRoot: (viaCounts['threadRoot'] ?: 0) + (viaCounts['threadRootPrefix'] ?: 0),\n        attachedByAgentId   : (viaCounts['agentId'] ?: 0),\n        attachedByAdjacency : (viaCounts['adjacency'] ?: 0),\n        unattributed        : (viaCounts['unattributed'] ?: 0),\n        // Counted on facts that are true of a ROW, so every one of these adds cleanly across\n        // pages. `retriedLlmCalls` and `failedLlmCalls` are properties of a CHAIN, and a chain the\n        // page cut in half is seen by both pages — pageMath says so rather than pretending.\n        llmSpanCount        : llmRows.size(),\n        billedLlmCalls      : llmRows.size() - retryRollups.size() - failedAttempts.size(),\n        failedLlmAttempts   : failedAttempts.size(),\n        retryRollupSpans    : retryRollups.size(),\n        retriedLlmCalls     : retriedCalls,\n        logicalLlmCalls     : callRows.size(),\n        failedLlmCalls      : callRows.size() - answeredCalls,\n        delegationEdges     : delegationEdges,\n        // What this page can and cannot answer. `windowed` false is the old, whole-message case\n        // and every rule above holds exactly as it did before pagination existed.\n        window              : [\n            windowed        : windowed,\n            offset          : pageOffset,\n            leadingCut      : leadingCut,\n            trailingCut     : trailingCut,\n            edgeThreadCount : edgeThreads.size(),\n            // A retry pair (two rows, same executionInstanceId + startTime) split by the cut reads\n            // as a plain failure on one page and an orphan success on the next; a delegation\n            // wrapper on one side of the cut cannot see the loop it opened on the other. Both are\n            // properties of the window, not of the turn.\n            note            : windowed\n                ? 'thread grouping, retry pairing and delegation edges are resolved WITHIN this page only'\n                : null\n        ]\n    ]\n]",
				"compile_static":false,
				"captureStdOutput":false,
				"parameters":{
					"currentSpans":{
						"items":"{{ n_APnyI.outputs.result.currentSpans[0] }}",
						"source":"{{ n_APnyI.outputs.result.currentSpans }}",
						"ua:type":"mappedArray"
					},
					"page":"{{ n_APnyI.outputs.result.page }}"
				}
			},
			"skip":false,
			"subTitle":"Code",
			"title":"Build Trace Owners (by execution)",
			"type":"ACTION"
		},
		{
			"context":{
				"appName":"variable_by_unifyapps",
				"resourceVersion":0,
				"resourceName":"variable_by_unifyapps_create_variables",
				"type":"APPLICATION"
			},
			"debug":false,
			"dirty":false,
			"fallbackMode":"STOP",
			"groupId":"_JAI60-1",
			"id":"n_rZz5h",
			"index":10,
			"inputSchema":{
				"type":"SCHEMA_AND_LAYOUT",
				"dynamic":false,
				"layout":{},
				"schema":{
					"additionalProperties":false,
					"type":"object",
					"properties":{
						"previousLlmInput":{
							"additionalProperties":false,
							"properties":{},
							"title":"Previous LLM Input",
							"type":"object"
						}
					},
					"required":[]
				}
			},
			"inputs":{},
			"outputSchema":{
				"type":"SCHEMA",
				"dynamic":false,
				"schema":{
					"additionalProperties":false,
					"type":"object",
					"properties":{
						"previousLlmInput":{
							"additionalProperties":false,
							"properties":{},
							"title":"Previous LLM Input",
							"type":"object"
						}
					},
					"required":[]
				}
			},
			"skip":false,
			"subTitle":"Variable",
			"title":"Create variables",
			"type":"ACTION"
		},
		{
			"context":{
				"appName":"loop",
				"resourceVersion":483,
				"resourceName":"loop_for_each",
				"type":"APPLICATION"
			},
			"debug":false,
			"dirty":false,
			"fallbackMode":"STOP",
			"groupId":"_JAI60-1",
			"id":"n_vU2yL",
			"index":11,
			"inputs":{
				"repeatMode":"SINGLE",
				"listSource":"{{ n_APnyI.outputs.result.currentSpans }}",
				"captureIterations":true
			},
			"skip":false,
			"subTitle":"Loop",
			"title":"For each Trace",
			"type":"LOOP"
		},
		{
			"context":{
				"appName":"code_by_unifyapps",
				"resourceVersion":0,
				"resourceName":"code_by_unifyapps_groovy",
				"type":"APPLICATION"
			},
			"debug":false,
			"dirty":false,
			"fallbackMode":"STOP",
			"groupId":"n_vU2yL@_JAI60-1@l",
			"id":"n_8lzWG",
			"index":12,
			"inputs":{
				"output":{
					"additionalProperties":false,
					"properties":{
						"shouldSkip":{
							"title":"Should Skip",
							"type":"boolean"
						}
					},
					"required":[
						"shouldSkip"
					],
					"type":"object"
				},
				"input":{
					"additionalProperties":false,
					"properties":{
						"index":{
							"title":"Index",
							"type":"integer"
						},
						"skipIndices":{
							"items":{
								"type":"string"
							},
							"title":"Skip Indices",
							"type":"array"
						}
					},
					"required":[
						"skipIndices",
						"index"
					],
					"type":"object"
				},
				"code":"def skipIndices = (binding.hasVariable('skipIndices') && skipIndices != null) ? skipIndices : []\ndef index = (binding.hasVariable('index') && index != null) ? index : -1\n\nreturn [\n    shouldSkip: skipIndices.contains(index.toString())\n]",
				"compile_static":false,
				"captureStdOutput":false,
				"parameters":{
					"index":"{{ n_vU2yL.outputs.index }}",
					"skipIndices":{
						"items":"{{ n_VYOw1.outputs.result.skipIndices[0] }}",
						"source":"{{ n_VYOw1.outputs.result.skipIndices }}",
						"ua:type":"mappedArray"
					}
				}
			},
			"skip":false,
			"subTitle":"Code",
			"title":"Skip this Trace?",
			"type":"ACTION"
		},
		{
			"context":{
				"appName":"if_else",
				"resourceVersion":61,
				"resourceName":"if_else_condition",
				"type":"APPLICATION"
			},
			"debug":false,
			"dirty":false,
			"fallbackMode":"STOP",
			"groupId":"n_vU2yL@_JAI60-1@l",
			"id":"n_ZxueP",
			"index":13,
			"inputs":{
				"filters":[
					{
						"filter":{
							"operator":"EQUAL",
							"value":"True"
						},
						"property":"{{ n_8lzWG.outputs.result.shouldSkip }}"
					}
				],
				"operator":"AND"
			},
			"skip":false,
			"subTitle":"Condition",
			"title":"Condition",
			"type":"IF_ELSE"
		},
		{
			"context":{
				"appName":"continue",
				"resourceVersion":61,
				"resourceName":"continue",
				"type":"APPLICATION"
			},
			"debug":false,
			"dirty":false,
			"fallbackMode":"STOP",
			"groupId":"n_ZxueP@n_vU2yL@_JAI60-1@l@y",
			"id":"n_LTs8U",
			"index":14,
			"inputs":{
				"loop":"n_vU2yL"
			},
			"skip":false,
			"subTitle":"Continue",
			"title":"Continue",
			"type":"CONTINUE"
		},
		{
			"context":{
				"appName":"variable_by_unifyapps",
				"resourceVersion":0,
				"resourceName":"variable_by_unifyapps_create_variables",
				"type":"APPLICATION"
			},
			"debug":false,
			"dirty":false,
			"fallbackMode":"STOP",
			"groupId":"n_vU2yL@_JAI60-1@l",
			"id":"n_2oX47",
			"index":15,
			"inputSchema":{
				"type":"SCHEMA_AND_LAYOUT",
				"dynamic":false,
				"layout":{},
				"schema":{
					"additionalProperties":false,
					"type":"object",
					"properties":{
						"subTurn":{
							"additionalProperties":false,
							"properties":{},
							"title":"Sub Turn",
							"type":"object"
						},
						"subTurnFile":{
							"additionalProperties":false,
							"properties":{},
							"title":"Sub Turn File",
							"type":"object"
						}
					},
					"required":[
						"subTurn",
						"subTurnFile"
					]
				}
			},
			"inputs":{},
			"outputSchema":{
				"type":"SCHEMA",
				"dynamic":false,
				"schema":{
					"additionalProperties":false,
					"type":"object",
					"properties":{
						"subTurn":{
							"additionalProperties":false,
							"properties":{},
							"title":"Sub Turn",
							"type":"object"
						},
						"subTurnFile":{
							"additionalProperties":false,
							"properties":{},
							"title":"Sub Turn File",
							"type":"object"
						}
					},
					"required":[
						"subTurn",
						"subTurnFile"
					]
				}
			},
			"skip":false,
			"subTitle":"Variable",
			"title":"Create variables",
			"type":"ACTION"
		},
		{
			"context":{
				"appName":"code_by_unifyapps",
				"resourceVersion":0,
				"resourceName":"code_by_unifyapps_groovy",
				"type":"APPLICATION"
			},
			"debug":false,
			"dirty":false,
			"fallbackMode":"STOP",
			"groupId":"n_vU2yL@_JAI60-1@l",
			"id":"n_zKUfl",
			"index":16,
			"inputs":{
				"output":{
					"additionalProperties":false,
					"properties":{
						"currentType":{
							"title":"Current Type",
							"type":"string"
						},
						"parent":{
							"title":"Parent",
							"type":"string"
						}
					},
					"required":[
						"parent",
						"currentType"
					],
					"type":"object"
				},
				"input":{
					"additionalProperties":false,
					"properties":{
						"currentSpan":{
							"additionalProperties":false,
							"properties":{},
							"title":"Current Span",
							"type":"object"
						}
					},
					"required":[
						"currentSpan"
					],
					"type":"object"
				},
				"code":"def currentSpan = (binding.hasVariable('currentSpan') && currentSpan != null) ? currentSpan : [:]\n\ndef parent = currentSpan?.parent\ndef currentType = currentSpan?.currentType\n\nreturn [\n    parent: parent,\n    currentType: currentType\n]",
				"compile_static":false,
				"captureStdOutput":false,
				"parameters":{
					"currentSpan":"{{ n_vU2yL.outputs.item }}"
				}
			},
			"skip":false,
			"subTitle":"Code by UnifyApps",
			"title":"Get Parent",
			"type":"ACTION"
		},
		{
			"context":{
				"appName":"branch",
				"type":"APPLICATION"
			},
			"debug":false,
			"dirty":false,
			"fallbackMode":"STOP",
			"groupId":"n_vU2yL@_JAI60-1@l",
			"id":"n_moIvM",
			"index":17,
			"inputs":{
				"branches":[
					{
						"id":"1",
						"inputs":{
							"conditions":{
								"filters":[
									{
										"filter":{
											"operator":"EQUAL",
											"value":"Input Guardrails"
										},
										"property":"{{ n_zKUfl.outputs.result.parent }}"
									}
								],
								"operator":"AND"
							},
							"name":"Input Guardrails"
						}
					},
					{
						"id":"2",
						"inputs":{
							"conditions":{
								"filters":[
									{
										"filter":{
											"operator":"EQUAL",
											"value":"Agent Execution"
										},
										"property":"{{ n_zKUfl.outputs.result.parent }}"
									}
								],
								"operator":"AND"
							},
							"name":"Agent Execution"
						}
					},
					{
						"id":"3",
						"inputs":{
							"conditions":{
								"filters":[
									{
										"filter":{
											"operator":"EQUAL",
											"value":"Response Guardrail"
										},
										"property":"{{ n_zKUfl.outputs.result.parent }}"
									}
								],
								"operator":"AND"
							},
							"name":"Response Guardrail"
						}
					},
					{
						"id":"4",
						"inputs":{
							"conditions":{
								"filters":[
									{
										"filter":{
											"operator":"EQUAL",
											"value":"ReferFromKnowledge"
										},
										"property":"{{ n_zKUfl.outputs.result.parent }}"
									},
									{
										"filter":{
											"operator":"EQUAL",
											"value":"Retrieval"
										},
										"property":"{{ n_zKUfl.outputs.result.currentType }}"
									}
								],
								"operator":"AND"
							},
							"name":"Knowledge Retrieval"
						}
					},
					{
						"id":"default"
					}
				]
			},
			"skip":false,
			"subTitle":"Branch",
			"title":"",
			"type":"BRANCH"
		},
		{
			"context":{
				"appName":"branch_condition",
				"resourceVersion":0,
				"resourceName":"branch_condition",
				"type":"APPLICATION"
			},
			"debug":false,
			"dirty":false,
			"fallbackMode":"STOP",
			"groupId":"n_moIvM@n_vU2yL@_JAI60-1@l@1",
			"id":"n_moIvM@1",
			"index":18,
			"inputs":{
				"name":"Input Guardrails",
				"conditions":{
					"filters":[
						{
							"filter":{
								"operator":"EQUAL",
								"value":"Input Guardrails"
							},
							"property":"{{ n_zKUfl.outputs.result.parent }}"
						}
					],
					"operator":"AND"
				}
			},
			"skip":false,
			"subTitle":"Branch Condition",
			"title":"Input Guardrails",
			"type":"BRANCH_CONDITION"
		},
		{
			"context":{
				"appName":"code_by_unifyapps",
				"resourceVersion":929,
				"resourceName":"code_by_unifyapps_groovy",
				"type":"APPLICATION"
			},
			"debug":false,
			"dirty":false,
			"fallbackMode":"STOP",
			"groupId":"n_moIvM@n_vU2yL@_JAI60-1@l@1",
			"id":"n_5NnK0",
			"index":19,
			"inputs":{
				"output":{
					"additionalProperties":false,
					"properties":{
						"subTurn":{
							"additionalProperties":false,
							"properties":{},
							"title":"Sub Turn",
							"type":"object"
						}
					},
					"required":[
						"subTurn"
					],
					"type":"object"
				},
				"input":{
					"additionalProperties":false,
					"properties":{
						"auxByOwnerIndex":{
							"additionalProperties":false,
							"properties":{},
							"title":"Aux By Owner Index",
							"type":"object"
						},
						"currentSpan":{
							"additionalProperties":false,
							"properties":{},
							"title":"Current Span",
							"type":"object"
						},
						"index":{
							"title":"Index",
							"type":"integer"
						}
					},
					"required":[
						"currentSpan"
					],
					"type":"object"
				},
				"code":"def currentSpan = (binding.hasVariable('currentSpan') && currentSpan != null) ? currentSpan : [:]\ndef index = (binding.hasVariable('index') && index != null) ? index : -1\ndef auxByOwnerIndex = (binding.hasVariable('auxByOwnerIndex') && auxByOwnerIndex != null) ? auxByOwnerIndex : [:]\n\ndef subTurn = [:]\n\n// The cost/token columns are read optimistically. They were left out on the assumption that a\n// guardrail span never carries them; the query selects them for every row, and `if (value != null)`\n// already omits the ones that really are absent — so an LLM-backed guard (Content Filters,\n// Groundedness, Denial) now contributes its cost instead of silently contributing nothing.\n[\n    'currentType',\n    'aiAgentId',\n    'success',\n    'startTime',\n    'llmTimeTaken',\n    'provider',\n    'modelId',\n    'finishReason',\n    'llmErrorType',\n    'cost',\n    'inputToken',\n    'outputToken',\n    'billableInputTokens',\n    'inputTokenCost',\n    'outputTokenCost',\n    'cacheReadInputTokens',\n    'cacheWriteInputTokens',\n    'cacheReadCost',\n    'cacheWriteCost',\n    'guardrailViolationDetected',\n    'guardrailViolationReason',\n]\n.each { field ->\n    def value = currentSpan?.\"${field}\"\n    if (value != null) {\n        subTurn[field] = value\n    }\n}\n\ndef failedReason = null\nif (currentSpan?.guardrailViolationReason != null) {\n    failedReason = currentSpan.guardrailViolationReason as String\n} else if (currentSpan?.llmErrorType != null) {\n    failedReason = currentSpan.llmErrorType as String\n} else if (currentSpan?.success == false) {\n    failedReason = 'guardrail reported success=false'\n}\nif (failedReason != null) subTurn.failedReason = failedReason\n\n// Masking / filter steps that ran inside this guard. They used to be collected and thrown away.\ndef aux = auxByOwnerIndex[index.toString()] ?: []\nif (aux) {\n    subTurn.overheadSteps = aux.collect { s ->\n        def step = [:]\n        ['currentType','startTime','llmTimeTaken','success','cost',\n         'guardrailViolationDetected','guardrailViolationReason','attachedVia'].each { f ->\n            def v = s?.\"${f}\"\n            if (v != null) step[f] = v\n        }\n        step\n    }.sort { it?.startTime ?: 0 }\n    subTurn.overheadTimeMs = subTurn.overheadSteps.sum { (it?.llmTimeTaken ?: 0) } ?: 0\n}\n// Global span number. n_APnyI stamped every row with its position in the WHOLE ordered result\n// (offset + position-in-page), so this is continuous across pages; `index` is the loop's own\n// page-local counter and stays page-local because it is the key into ownerMeta/auxByOwnerIndex.\nsubTurn.spanIndex = (currentSpan?.spanIndex != null) ? currentSpan.spanIndex : index\n\nreturn subTurn",
				"compile_static":false,
				"captureStdOutput":false,
				"parameters":{
					"auxByOwnerIndex":"{{ n_VYOw1.outputs.result.auxByOwnerIndex }}",
					"currentSpan":"{{ n_vU2yL.outputs.item }}",
					"index":"{{ n_vU2yL.outputs.index }}"
				}
			},
			"skip":false,
			"subTitle":"Code",
			"title":"Execute Groovy code",
			"type":"ACTION"
		},
		{
			"context":{
				"appName":"variable_by_unifyapps",
				"resourceVersion":0,
				"resourceName":"variable_by_unifyapps_add_item_to_list",
				"type":"APPLICATION"
			},
			"debug":false,
			"dirty":false,
			"fallbackMode":"STOP",
			"groupId":"n_moIvM@n_vU2yL@_JAI60-1@l@1",
			"id":"n_Oad4Z",
			"index":20,
			"inputs":{
				"itemToAdd":"{{ n_5NnK0.outputs.result }}",
				"insertLocation":"END",
				"captureAddedItem":false,
				"listName":"{{ n_Cegc9.outputs.items[0] }}"
			},
			"skip":false,
			"subTitle":"Variable",
			"title":"Add item to list",
			"type":"ACTION"
		},
		{
			"context":{
				"appName":"branch_condition",
				"resourceVersion":0,
				"resourceName":"branch_condition",
				"type":"APPLICATION"
			},
			"debug":false,
			"dirty":false,
			"fallbackMode":"STOP",
			"groupId":"n_moIvM@n_vU2yL@_JAI60-1@l@2",
			"id":"n_moIvM@2",
			"index":21,
			"inputs":{
				"name":"Agent Execution",
				"conditions":{
					"filters":[
						{
							"filter":{
								"operator":"EQUAL",
								"value":"Agent Execution"
							},
							"property":"{{ n_zKUfl.outputs.result.parent }}"
						}
					],
					"operator":"AND"
				}
			},
			"skip":false,
			"subTitle":"Branch Condition",
			"title":"Agent Execution",
			"type":"BRANCH_CONDITION"
		},
		{
			"context":{
				"appName":"if_else",
				"resourceVersion":61,
				"resourceName":"if_else_condition",
				"type":"APPLICATION"
			},
			"debug":false,
			"dirty":false,
			"fallbackMode":"STOP",
			"groupId":"n_moIvM@n_vU2yL@_JAI60-1@l@2",
			"id":"n_KZZsd",
			"index":22,
			"inputs":{
				"filters":[
					{
						"filter":{
							"operator":"EQUAL",
							"value":"LLM"
						},
						"property":"{{ n_zKUfl.outputs.result.currentType }}"
					}
				],
				"operator":"AND"
			},
			"skip":false,
			"subTitle":"Condition",
			"title":"Condition",
			"type":"IF_ELSE"
		},
		{
			"context":{
				"appName":"code_by_unifyapps",
				"resourceVersion":0,
				"resourceName":"code_by_unifyapps_groovy",
				"type":"APPLICATION"
			},
			"debug":false,
			"dirty":false,
			"fallbackMode":"STOP",
			"groupId":"n_KZZsd@n_moIvM@n_vU2yL@_JAI60-1@l@2@y",
			"id":"n_k02Aq",
			"index":23,
			"inputs":{
				"output":{
					"additionalProperties":false,
					"properties":{
						"subTurn":{
							"additionalProperties":false,
							"properties":{},
							"title":"Sub Turn",
							"type":"object"
						}
					},
					"required":[
						"subTurn"
					],
					"type":"object"
				},
				"input":{
					"additionalProperties":false,
					"properties":{
						"currentSpan":{
							"additionalProperties":false,
							"properties":{},
							"title":"Current Span",
							"type":"object"
						},
						"index":{
							"title":"Index",
							"type":"integer"
						},
						"ownerMetaByIndex":{
							"additionalProperties":false,
							"properties":{},
							"title":"Owner Meta By Index",
							"type":"object"
						}
					},
					"required":[
						"currentSpan"
					],
					"type":"object"
				},
				"code":"def currentSpan = (binding.hasVariable('currentSpan') && currentSpan != null) ? currentSpan : [:]\ndef index = (binding.hasVariable('index') && index != null) ? index : -1\ndef ownerMetaByIndex = (binding.hasVariable('ownerMetaByIndex') && ownerMetaByIndex != null) ? ownerMetaByIndex : [:]\n\ndef meta = ownerMetaByIndex[index.toString()]\n\n// A retry ROLLUP is an ancestor frame re-reporting a descendant's result (see n_VYOw1). Its cost,\n// its token counts and its finishReason are a verbatim copy of a call that is already in this page,\n// and its duration spans the whole retry chain rather than any call of its own. So the charge\n// columns are not copied onto it at all — a reader who sees `cost: 0.84` on a span will add it, and\n// nothing in a footnote stops that. What rides instead is the flag saying what the row is.\ndef isRetryRollup = (meta?.retryRollup == true)\n\ndef subTurn = [:]\n\ndef copy = { List fields ->\n    fields.each { field ->\n        def value = currentSpan?.\"${field}\"\n        if (value != null) subTurn[field] = value\n    }\n}\n\n// What the row IS, and when it ran. True of every LLM row including a rollup.\ncopy(['currentType', 'aiAgentId', 'success', 'provider', 'modelId', 'llmErrorType',\n      'startTime', 'llmTimeTaken'])\n\n// What the row COST. Only ever read from a row that stands for a real call.\nif (!isRetryRollup) {\n    // `finish_reason` was read here for as long as the node has existed, but the query aliases the\n    // column `finishReason` (_pr_finish_reason AS finishReason). `if (value != null)` swallowed the\n    // miss, so the field was absent from every LLM span of every turn. Both spellings are accepted\n    // now so the node is insensitive to the alias.\n    copy(['finishReason', 'cost', 'inputToken', 'outputToken', 'billableInputTokens',\n          'inputTokenCost', 'outputTokenCost', 'cacheReadInputTokens', 'cacheWriteInputTokens',\n          'cacheReadCost', 'cacheWriteCost'])\n    if (subTurn.finishReason == null && currentSpan?.finish_reason != null) {\n        subTurn.finishReason = currentSpan.finish_reason\n    }\n} else {\n    subTurn.isRetryRollup = true\n    subTurn.rollupNote = 'duplicate row: repeats the cost, tokens and finishReason of the ' +\n        'attempt that succeeded, and spans the whole retry chain. Excluded from every total.'\n}\n\n// Why the call failed, when it did. There is no free-text error column on e_traces — llmErrorType\n// and finishReason are all there is — so the field is built from those and OMITTED ENTIRELY when\n// there is nothing to say. A key that is always present and usually null is noise.\n//\n// llmErrorType is stamped on the row that SUCCEEDED after a retry as well as on the attempts that\n// failed, so it cannot be read as \"this failed\" on its own. A recovered error is reported as\n// recoveredFrom, not as a failure — and a rollup reports neither, because it is not a call.\nif (isRetryRollup) {\n    // nothing to say: this row is a copy, not an outcome\n} else if (currentSpan?.success == false) {\n    def fr = subTurn.finishReason\n    subTurn.failedReason = (currentSpan?.llmErrorType != null) ? (currentSpan.llmErrorType as String)\n                         : (fr != null) ? ('finishReason: ' + fr)\n                         : 'llm call reported success=false'\n} else if (currentSpan?.llmErrorType != null) {\n    subTurn.recoveredFrom = currentSpan.llmErrorType as String\n}\n\n// execution attribution, carried internally\n// These ids are what the arithmetic downstream actually runs on:\n//   threadId      the react loop (rootExecutionId). The input diff baselines against it — NOT\n//                 aiAgentId, which merges an agent's parallel invocations into one conversation,\n//                 and NOT executionInstanceId, which is per-FRAME and would make every call look\n//                 like a thread start.\n//   callId        the logical LLM call this row belongs to (parentExecutionId, guarded). Several\n//                 rows share one when a call was retried.\n//   retryRollup   this row duplicates another row's charge — see above.\n//\n// Emitting four id strings on 523 spans is noise in a response, which is why they ride under a\n// single `_x` key that n_jrFAb strips from the files and n_WB2iD strips from the turns. The maths\n// gets the ids; the reader never sees them.\ndef x = [:]\nif (currentSpan?.spanIndex != null) x.spanIndex = currentSpan.spanIndex\nif (currentSpan?.executionInstanceId != null) x.executionInstanceId = currentSpan.executionInstanceId\nif (meta != null) {\n    if (meta.threadId != null) x.threadId = meta.threadId\n    if (meta.callId != null) x.callId = meta.callId\n    if (meta.threadPrevIndex != null) x.threadPrevIndex = meta.threadPrevIndex\n    if (meta.retryRollup == true) x.retryRollup = true\n    if (meta.baselineOffPage == true) x.baselineOffPage = true\n}\nif (x) subTurn._x = x\n\nreturn subTurn\n",
				"compile_static":false,
				"captureStdOutput":false,
				"parameters":{
					"currentSpan":"{{ n_vU2yL.outputs.item }}",
					"index":"{{ n_vU2yL.outputs.index }}",
					"ownerMetaByIndex":"{{ n_VYOw1.outputs.result.ownerMetaByIndex }}"
				}
			},
			"skip":false,
			"subTitle":"Code",
			"title":"Build LLM Specific Fields",
			"type":"ACTION"
		},
		{
			"context":{
				"appName":"variable_by_unifyapps",
				"resourceVersion":0,
				"resourceName":"variable_by_unifyapps_update_variables",
				"type":"APPLICATION"
			},
			"debug":false,
			"dirty":false,
			"fallbackMode":"STOP",
			"groupId":"n_KZZsd@n_moIvM@n_vU2yL@_JAI60-1@l@2@y",
			"id":"n_qgM1x",
			"index":24,
			"inputs":{
				"variables":[
					{
						"source":"{{ n_2oX47.outputs.subTurn }}",
						"value":"{{ n_k02Aq.outputs.result }}"
					}
				]
			},
			"skip":false,
			"subTitle":"Variable",
			"title":"Update variables",
			"type":"ACTION"
		},
		{
			"context":{
				"appName":"analytics_by_unifyapps",
				"resourceVersion":0,
				"resourceName":"analytics_by_unifyapps_execute_query",
				"type":"APPLICATION"
			},
			"debug":false,
			"dirty":false,
			"fallbackMode":"STOP",
			"groupId":"n_KZZsd@n_moIvM@n_vU2yL@_JAI60-1@l@2@y",
			"id":"n_84rFM",
			"index":25,
			"inputs":{
				"outputSchema":{
					"additionalProperties":false,
					"properties":{
						"input":{
							"title":"Input",
							"type":"string"
						},
						"outputObject":{
							"title":"Output Object",
							"type":"string"
						}
					},
					"required":[
						"input",
						"outputObject"
					],
					"type":"object"
				},
				"query":"SELECT\n  _pr_input AS input,\n  COALESCE(_pr_outputObject, _pr_output) AS outputObject\nFROM ENTITY_e_traces\nWHERE id = :trace_id\nLIMIT 1;",
				"record":{
					"trace_id":"{{ n_vU2yL.outputs.item.id }}"
				},
				"params":{
					"additionalProperties":false,
					"properties":{
						"trace_id":{
							"title":"Trace Id",
							"type":"string"
						}
					},
					"required":[],
					"type":"object"
				},
				"group":"ENTITY_REPORTING"
			},
			"skip":false,
			"subTitle":"Analytics by UnifyApps",
			"title":"Get Trace Payload (Full)",
			"type":"ACTION"
		},
		{
			"context":{
				"appName":"code_by_unifyapps",
				"resourceVersion":0,
				"resourceName":"code_by_unifyapps_groovy",
				"type":"APPLICATION"
			},
			"debug":false,
			"dirty":false,
			"fallbackMode":"STOP",
			"groupId":"n_KZZsd@n_moIvM@n_vU2yL@_JAI60-1@l@2@y",
			"id":"n_VgcsB",
			"index":26,
			"inputs":{
				"output":{
					"additionalProperties":false,
					"properties":{
						"hasLargePayload":{
							"title":"Has Large Payload",
							"type":"boolean"
						},
						"inlineNewMessages":{
							"items":{
								"additionalProperties":false,
								"properties":{},
								"type":"object"
							},
							"title":"Inline New Messages",
							"type":"array"
						},
						"inlineSystemPrompt":{
							"title":"Inline System Prompt",
							"type":"string"
						},
						"largePayload":{
							"additionalProperties":false,
							"properties":{},
							"title":"Large Payload",
							"type":"object"
						},
						"largePayloadString":{
							"title":"Large Payload String",
							"type":"string"
						},
						"nextLlmInputByAgent":{
							"additionalProperties":false,
							"properties":{},
							"title":"Next Llm Input By Agent",
							"type":"object"
						},
						"payloadChars":{
							"title":"Payload Chars",
							"type":"integer"
						},
						"subTurnResponse":{
							"additionalProperties":false,
							"properties":{
								"inputDiff":{
									"additionalProperties":false,
									"properties":{},
									"title":"Input Diff",
									"type":"object"
								},
								"outputChars":{
									"title":"Output Chars",
									"type":"integer"
								},
								"outputPreview":{
									"title":"Output Preview",
									"type":"string"
								}
							},
							"title":"Sub Turn Response",
							"type":"object"
						}
					},
					"required":[
						"subTurnResponse",
						"nextLlmInputByAgent",
						"largePayload",
						"hasLargePayload",
						"largePayloadString"
					],
					"type":"object"
				},
				"input":{
					"additionalProperties":false,
					"properties":{
						"input":{
							"title":"Input",
							"type":"string"
						},
						"outputObject":{
							"title":"Output Object",
							"type":"string"
						},
						"page":{
							"additionalProperties":false,
							"properties":{},
							"title":"Page",
							"type":"object"
						},
						"previousLlmInput":{
							"additionalProperties":false,
							"properties":{},
							"title":"Previous Llm Input",
							"type":"object"
						},
						"subTurn":{
							"additionalProperties":false,
							"properties":{},
							"title":"Sub Turn",
							"type":"object"
						}
					},
					"required":[
						"subTurn"
					],
					"type":"object"
				},
				"code":"def inStr   = binding.hasVariable('input') ? input : null\ndef outObj  = binding.hasVariable('outputObject') ? outputObject : null\n\ndef prevMap = (binding.hasVariable('previousLlmInput') && previousLlmInput instanceof Map)\n    ? previousLlmInput\n    : [:]\n\ndef subTurnIn = (binding.hasVariable('subTurn') && subTurn != null) ? subTurn : [:]\n\n// Baseline key: the react LOOP (rootExecutionId, stamped as threadId upstream), not the agent id\n// and not the per-step executionInstanceId. One agent invoked eight times in parallel is eight\n// prefixes, and keying on the step id would report every single call as a thread start.\ndef x = (subTurnIn?._x instanceof Map) ? subTurnIn._x : [:]\ndef agentKey = (x?.threadId) ? x.threadId.toString()\n    : (subTurnIn?.threadId) ? subTurnIn.threadId.toString()\n    : ((subTurnIn?.aiAgentId) ? subTurnIn.aiAgentId.toString() : '__unknown_agent__')\n\n// Reserved slot in the same carrier: the thread of the PREVIOUS span in wall-clock order, so a\n// hand-off is reportable without adding a node or a binding.\ndef LAST = '__lastAgent'\ndef previousAgentId = prevMap[LAST]\n\ndef prev = (prevMap[agentKey] instanceof Map) ? prevMap[agentKey] : [:]\n\n// The diff baseline is the thread's PREVIOUS LLM call, and that call is only on hand if it is in\n// this page. On a page that does not start at 0 the first call of every thread has no baseline —\n// not because it is the thread's first call, but because its predecessor is in an earlier page.\n//\n// Reporting `firstTurnForAgent: true` there would be a confident lie, and it would cascade: the\n// whole system prompt reads as \"changed\", the entire tool list as \"added\", the entire conversation\n// as \"new messages\". So the two cases are separated. Baseline present -> unchanged behaviour.\n// Baseline missing because the page is cut -> the diff says UNKNOWN (null), never \"changed\".\ndef page = (binding.hasVariable('page') && page instanceof Map) ? page : [:]\ndef pageStartsMidTurn = (x?.baselineOffPage == true) ||\n                        ((page?.offset != null) && ((page.offset as BigDecimal) > 0G))\ndef baselineOffPage = prev.isEmpty() && pageStartsMidTurn\ndef firstTurnForAgent = prev.isEmpty() && !baselineOffPage\n// A retry ROLLUP duplicates a call that is already in this page; a FAILED ATTEMPT never got a\n// reply at all. Neither carries the conversation forward, so neither may become the baseline the\n// next call diffs against — that would leave the real call diffing against a prompt that was never\n// sent and reporting its entire history as new.\ndef isRetryRollup   = (x?.retryRollup == true) || (subTurnIn?.isRetryRollup == true)\ndef isFailedAttempt = (subTurnIn?.success == false)\ndef carriesReply    = !isRetryRollup && !isFailedAttempt\n\ndef currInput = [:]\nif (inStr) {\n    try { currInput = new groovy.json.JsonSlurper().parseText(inStr as String) }\n    catch (Exception e) { currInput = [:] }\n}\n\n// Every probe returns `null` = unavailable, never `[]`, so an unknown provider shape surfaces as\n// a gap instead of as a clean, empty diff.\ndef flatten\nflatten = { v ->\n    if (v == null) return null\n    if (v instanceof CharSequence) return v as String\n    if (v instanceof List) {\n        def joined = v.collect { p ->\n            if (p instanceof CharSequence) return p as String\n            if (p instanceof Map) return (p?.text ?: p?.content ?: '') as String\n            return ''\n        }.join('')\n        return joined ? joined : null\n    }\n    if (v instanceof Map) return flatten(v?.text ?: v?.content ?: v?.parts)\n    return null\n}\n\ndef firstList = { List candidates ->\n    for (c in candidates) { if (c instanceof List && !c.isEmpty()) return c }\n    for (c in candidates) { if (c instanceof List) return c }\n    return null\n}\n\ndef readMessages = { payload ->\n    if (!(payload instanceof Map)) return null\n    return firstList([\n        payload?.message,\n        payload?.messages,\n        payload?.contents,\n        payload?.input instanceof Map ? payload.input.message : null,\n        payload?.input instanceof Map ? payload.input.messages : null,\n        payload?.request instanceof Map ? payload.request.messages : null,\n        payload?.body instanceof Map ? payload.body.messages : null\n    ])\n}\n\ndef readToolNames = { payload ->\n    if (!(payload instanceof Map)) return null\n    def list = firstList([\n        payload?.tools,\n        payload?.tool,\n        payload?.input instanceof Map ? payload.input.tools : null,\n        payload?.request instanceof Map ? payload.request.tools : null,\n        payload?.toolConfig instanceof Map ? payload.toolConfig.functionDeclarations : null,\n        payload?.tool_config instanceof Map ? payload.tool_config.function_declarations : null\n    ])\n    if (list == null) return null\n    return list.collect { t ->\n        if (t instanceof CharSequence) return t as String\n        if (!(t instanceof Map)) return null\n        return t?.name ?:\n               (t?.function instanceof Map ? t.function.name : null) ?:\n               (t?.functionDeclaration instanceof Map ? t.functionDeclaration.name : null) ?:\n               (t?.toolSpec instanceof Map ? t.toolSpec.name : null) ?:\n               t?.toolName\n    }.findAll { it != null }\n}\n\ndef readSystemPrompt = { payload ->\n    if (!(payload instanceof Map)) return null\n    def named = [\n        'system'            : payload?.system,\n        'systemPrompt'      : payload?.systemPrompt,\n        'systemInstruction' : payload?.systemInstruction instanceof Map ? payload.systemInstruction.parts : payload?.systemInstruction,\n        'system_instruction': payload?.system_instruction,\n        'instructions'      : payload?.instructions,\n        'instruction'       : payload?.instruction,\n        'input.system'      : payload?.input instanceof Map ? payload.input.system : null,\n        'request.system'    : payload?.request instanceof Map ? payload.request.system : null\n    ]\n    for (e in named.entrySet()) {\n        def s = flatten(e.value)\n        if (s) return [text: s, key: e.key]\n    }\n    for (e in payload.entrySet()) {\n        def k = e.key?.toString() ?: ''\n        if (!(k.toLowerCase() =~ /system|instruction|prompt/)) continue\n        def s = flatten(e.value)\n        if (s) return [text: s, key: k]\n    }\n    def msgs = readMessages(payload)\n    if (msgs && msgs[0] instanceof Map && (msgs[0]?.role == 'system' || msgs[0]?.role == 'developer')) {\n        def s = flatten(msgs[0]?.content)\n        if (s) return [text: s, key: 'message[0].role=system']\n    }\n    return null\n}\n\ndef sysHit       = readSystemPrompt(currInput)\ndef currSys      = sysHit?.text\ndef currToolList = readToolNames(currInput)\ndef currMessages = readMessages(currInput)\n\ndef prevSys       = prev?.systemPrompt\ndef prevToolNames = prev?.toolNames\ndef prevMsgCount  = prev?.messageCount\n\n// diff calculation/comparison\ndef sysAvailable = (currSys != null)\ndef sysChanged\nif (!sysAvailable) {\n    sysChanged = null\n} else if (baselineOffPage) {\n    sysChanged = null\n} else if (firstTurnForAgent) {\n    sysChanged = true\n} else {\n    sysChanged = (currSys != prevSys)\n}\n\ndef toolsAdded, toolsRemoved\nif (currToolList == null || baselineOffPage) {\n    // No baseline in this page: the full tool list is not a list of ADDITIONS.\n    toolsAdded = null; toolsRemoved = null\n} else if (firstTurnForAgent || prevToolNames == null) {\n    toolsAdded = currToolList; toolsRemoved = []\n} else {\n    toolsAdded = currToolList.findAll { !(it in (prevToolNames as Set)) }\n    toolsRemoved = prevToolNames.findAll { !(it in (currToolList as Set)) }\n}\n\ndef historyReset = false\ndef fullHistoryAsNew = false\ndef newMessages\nif (currMessages == null) {\n    newMessages = null\n} else if (baselineOffPage) {\n    // Same reason: without the previous call there is no delta to take, so what rides is the whole\n    // history — labelled as the whole history, not as `newMessageCount` messages that arrived.\n    newMessages = currMessages\n    fullHistoryAsNew = true\n} else if (firstTurnForAgent || prevMsgCount == null) {\n    newMessages = currMessages\n} else if (currMessages.size() < (prevMsgCount as int)) {\n    // The conversation got SHORTER — a compaction or a trim. The tail is not a delta of the old\n    // prefix, so it is reported as a reset rather than as growth.\n    newMessages = currMessages\n    historyReset = true\n} else {\n    newMessages = currMessages.drop(prevMsgCount as int)\n}\n\n// the diff: metrics inline, payload out of line\n// systemPrompt runs ~146 KB and newMessages carries whole conversations. Both used to ride inline\n// on the response (or force the whole sub-turn into a file). The METRICS stay inline — they are\n// what the arithmetic runs on — and the two large values go to one file per span, referenced from\n// the keys they used to occupy.\ndef inputDiff = [\n    // agentId              : (subTurnIn?.aiAgentId) ? subTurnIn.aiAgentId.toString() : null,\n    // threadId             : agentKey,\n    previousThreadId     : previousAgentId,\n    // agentChanged         : (previousAgentId != null && previousAgentId != agentKey),\n    firstTurnForAgent    : baselineOffPage ? null : firstTurnForAgent,\n    isRetryRollup        : isRetryRollup ?: null,\n\n    systemPromptAvailable: sysAvailable,\n    systemPromptSourceKey: sysHit?.key,\n    systemPromptChanged  : sysChanged,\n    systemPromptChars    : currSys == null ? null : currSys.length(),\n\n    toolsAdded           : toolsAdded,\n    toolsRemoved         : toolsRemoved,\n    toolCount            : currToolList == null ? null : currToolList.size(),\n\n    newMessageCount      : newMessages == null ? null : newMessages.size(),\n    totalMessageCount    : currMessages == null ? null : currMessages.size(),\n    historyReset         : historyReset\n]\n\n// Only present on a paged call that lost its baseline, so an unpaged run is byte-for-byte what it\n// was. `firstTurnForAgent: null` above is the same statement in the field the reader already\n// looks at; this says why.\nif (baselineOffPage) {\n    inputDiff.baselineOffPage = true\n    inputDiff.baselineReason = 'the previous call of this thread is before offset ' + page.offset\n    if (fullHistoryAsNew) inputDiff.newMessagesIsFullHistory = true\n}\n\n// The payload's key list is a DIAGNOSTIC for an unreadable shape. Emitting it on every span cost\n// 18 KB of a 414 KB response saying the same twenty keys 63 times, so it rides only when a probe\n// actually missed.\n\nif (!(currInput instanceof Map) || currInput.isEmpty()) {\n    inputDiff.available = false\n    inputDiff.reason = 'stored LLM input was empty or could not be parsed'\n} else if (currSys == null && currToolList == null && currMessages == null) {\n    inputDiff.available = false\n    inputDiff.reason = 'stored LLM input parsed but matched no known shape — see inputKeys'\n    inputDiff.inputKeys = currInput.keySet() as List\n} else {\n    inputDiff.available = true\n}\n\ndef largePayload = [:]\nif (sysChanged == true && currSys != null) largePayload.systemPrompt = currSys\nif (newMessages != null && !newMessages.isEmpty())  largePayload.newMessages = newMessages\n// The file call downstream is gated on this. A span whose diff payload fits inline does not need\n// a signed URL, and writing one cost a synchronous sub-automation call on EVERY LLM span.\ndef FILE_THRESHOLD_CHARS = 4000\ndef payloadChars = largePayload.isEmpty() ? 0 : groovy.json.JsonOutput.toJson(largePayload).length()\ndef hasLargePayload = payloadChars > FILE_THRESHOLD_CHARS\nif (!largePayload.isEmpty()) {\n    largePayload.spanIndex = (x?.spanIndex != null) ? x.spanIndex : subTurnIn?.spanIndex\n    largePayload.threadId = agentKey\n}\n// Under the threshold the values ride inline instead of behind a URL — the point of the gate is to\n// avoid the round trip, not to lose the data.\ndef inlineSystemPrompt = (!hasLargePayload && sysChanged == true) ? currSys : null\ndef inlineNewMessages  = (!hasLargePayload && newMessages != null && !newMessages.isEmpty()) ? newMessages : null\n\n// the call's own output, inline and bounded\ndef MAX_OUTPUT_CHARS = 3000\ndef outText = (outObj == null) ? null\n            : ((outObj instanceof CharSequence) ? (outObj as String) : groovy.json.JsonOutput.toJson(outObj))\ndef outputField = null\ndef outputChars = null\nif (outText != null) {\n    outputChars = outText.length()\n    outputField = (outputChars > MAX_OUTPUT_CHARS)\n        ? outText.substring(0, MAX_OUTPUT_CHARS) + '…[truncated]'\n        : outText\n}\n\n// Carry only what the next diff needs — never the message array itself.\ndef updatedMap = [:]\nupdatedMap.putAll(prevMap)\nif (carriesReply) {\n    updatedMap[agentKey] = [\n        systemPrompt: currSys,\n        toolNames   : currToolList,\n        messageCount: currMessages == null ? null : currMessages.size()\n    ]\n    updatedMap[LAST] = agentKey\n}\n\nreturn [\n    subTurnResponse: [\n        inputDiff     : inputDiff,\n        outputPreview : outputField,\n        outputChars   : outputChars\n    ],\n    largePayload       : largePayload,\n    largePayloadString : groovy.json.JsonOutput.toJson(largePayload),\n    payloadChars       : payloadChars,\n    hasLargePayload    : hasLargePayload,\n    inlineSystemPrompt : inlineSystemPrompt,\n    inlineNewMessages  : inlineNewMessages,\n    nextLlmInputByAgent: updatedMap\n]",
				"compile_static":false,
				"captureStdOutput":true,
				"parameters":{
					"input":"{{ n_84rFM.outputs.objects[0].columns.input }}",
					"outputObject":"{{ n_84rFM.outputs.objects[0].columns.outputObject }}",
					"page":"{{ n_APnyI.outputs.result.page }}",
					"previousLlmInput":"{{ n_rZz5h.outputs.previousLlmInput }}",
					"subTurn":"{{ n_k02Aq.outputs.result }}"
				}
			},
			"options":{
				"bulkheadConfig":{
					"enabled":false,
					"maxLeaseTimeUnit":"SECONDS",
					"maxWaitDurationUnit":"SECONDS"
				},
				"businessHoursConfig":{
					"enabled":false,
					"waitForNextWindow":false
				},
				"cacheConfig":{
					"enabled":false,
					"usagePolicy":"ALWAYS",
					"useInMemoryCache":false
				},
				"circuitBreakerConfig":{
					"enabled":false
				},
				"disableLogging":false,
				"enabledForReExecution":false,
				"hookConfig":{
					"enabled":false,
					"postHooks":[
						
					],
					"preHooks":[
						
					]
				},
				"logConfig":{
					"enabled":false,
					"logPolicies":[
						
					]
				},
				"rateLimitConfig":{
					"enabled":false,
					"waitForNextWindow":false
				},
				"retryConfig":{
					"backOffFactor":null,
					"captureRetries":false,
					"enabled":false
				},
				"stepError":"STOP",
				"telemetryConfig":{
					"enableTelemetry":false,
					"metricConfigs":[
						
					]
				}
			},
			"skip":false,
			"subTitle":"Code by UnifyApps",
			"title":"Input Delta Calculation",
			"type":"ACTION"
		},
		{
			"context":{
				"appName":"variable_by_unifyapps",
				"resourceVersion":990,
				"resourceName":"variable_by_unifyapps_update_variables",
				"type":"APPLICATION"
			},
			"debug":false,
			"dirty":false,
			"fallbackMode":"STOP",
			"groupId":"n_KZZsd@n_moIvM@n_vU2yL@_JAI60-1@l@2@y",
			"id":"n_yc5sd",
			"index":27,
			"inputs":{
				"variables":[
					{
						"source":"{{ n_rZz5h.outputs.previousLlmInput }}",
						"value":"{{ n_VgcsB.outputs.result.nextLlmInputByAgent }}"
					}
				]
			},
			"skip":false,
			"subTitle":"Variable by UnifyApps",
			"title":"Update variables",
			"type":"ACTION"
		},
		{
			"context":{
				"appName":"if_else",
				"resourceVersion":61,
				"resourceName":"if_else_condition",
				"type":"APPLICATION"
			},
			"debug":false,
			"dirty":false,
			"fallbackMode":"STOP",
			"groupId":"n_KZZsd@n_moIvM@n_vU2yL@_JAI60-1@l@2@y",
			"id":"n_IFllm",
			"index":28,
			"inputs":{
				"filters":[
					{
						"filter":{
							"operator":"EQUAL",
							"value":"True"
						},
						"property":"{{ n_VgcsB.outputs.result.hasLargePayload }}"
					}
				],
				"operator":"AND"
			},
			"skip":false,
			"subTitle":"Condition",
			"title":"Diff payload > 4000 chars?",
			"type":"IF_ELSE"
		},
		{
			"context":{
				"appName":"files_by_unifyapps",
				"resourceVersion":0,
				"resourceName":"files_by_unifyapps_create_temp_file",
				"type":"APPLICATION"
			},
			"debug":false,
			"dirty":false,
			"fallbackMode":"STOP",
			"groupId":"n_IFllm@n_KZZsd@n_moIvM@n_vU2yL@_JAI60-1@l@2@y@y",
			"id":"n_rfqMg",
			"index":29,
			"inputs":{
				"folderPath":"tmp/singleTurnTimeline",
				"isTmp":false,
				"fileName":"subTurnPayload_{{ n_v4sZv.outputs.message_id }}_{{ n_vU2yL.outputs.index }}.txt",
				"initialContentType":"plainText",
				"uploadToCloudStorage":true,
				"initialContent":"{{ n_VgcsB.outputs.result.largePayloadString }}",
				"mimeType":"text/plain",
				"type":"SINGLE",
				"fileType":"txt"
			},
			"skip":false,
			"subTitle":"Files by UnifyApps",
			"title":"Create a file",
			"type":"ACTION"
		},
		{
			"context":{
				"appName":"variable_by_unifyapps",
				"resourceVersion":0,
				"resourceName":"variable_by_unifyapps_update_variables",
				"type":"APPLICATION"
			},
			"debug":false,
			"dirty":false,
			"fallbackMode":"STOP",
			"groupId":"n_IFllm@n_KZZsd@n_moIvM@n_vU2yL@_JAI60-1@l@2@y@y",
			"id":"n_upFil",
			"index":30,
			"inputs":{
				"variables":[
					{
						"source":"{{ n_2oX47.outputs.subTurnFile }}",
						"value":"{{ n_rfqMg.outputs.fileDetails }}"
					}
				]
			},
			"skip":false,
			"subTitle":"Variable",
			"title":"Record file object",
			"type":"ACTION"
		},
		{
			"context":{
				"appName":"code_by_unifyapps",
				"resourceVersion":0,
				"resourceName":"code_by_unifyapps_groovy",
				"type":"APPLICATION"
			},
			"debug":false,
			"dirty":false,
			"fallbackMode":"STOP",
			"groupId":"n_KZZsd@n_moIvM@n_vU2yL@_JAI60-1@l@2@y",
			"id":"n_bGet4",
			"index":31,
			"inputs":{
				"output":{
					"additionalProperties":false,
					"properties":{
						"subTurn":{
							"additionalProperties":false,
							"properties":{},
							"title":"Sub Turn",
							"type":"object"
						}
					},
					"required":[
						"subTurn"
					],
					"type":"object"
				},
				"input":{
					"additionalProperties":false,
					"properties":{
						"hasLargePayload":{
							"title":"Has Large Payload",
							"type":"boolean"
						},
						"inlineNewMessages":{
							"items":{
								"additionalProperties":false,
								"properties":{},
								"type":"object"
							},
							"title":"Inline New Messages",
							"type":"array"
						},
						"inlineSystemPrompt":{
							"title":"Inline System Prompt",
							"type":"string"
						},
						"inputDiff":{
							"additionalProperties":false,
							"properties":{},
							"title":"Input Diff",
							"type":"object"
						},
						"output":{
							"title":"Output",
							"type":"string"
						},
						"outputChars":{
							"title":"Output Chars",
							"type":"integer"
						},
						"payloadChars":{
							"title":"Payload Chars",
							"type":"integer"
						},
						"payloadFile":{
							"additionalProperties":false,
							"properties":{},
							"title":"Payload File",
							"type":"object"
						},
						"subTurn":{
							"additionalProperties":false,
							"properties":{},
							"title":"Sub Turn",
							"type":"object"
						}
					},
					"required":[
						"subTurn"
					],
					"type":"object"
				},
				"code":"def subTurn = (binding.hasVariable('subTurn') && subTurn != null) ? subTurn : [:]\n\ndef inputDiff       = binding.hasVariable('inputDiff') ? inputDiff : null\ndef outputText      = binding.hasVariable('output') ? output : null\ndef outputChars     = binding.hasVariable('outputChars') ? outputChars : null\ndef hasLargePayload = binding.hasVariable('hasLargePayload') ? hasLargePayload : false\ndef payloadFile     = binding.hasVariable('payloadFile') ? payloadFile : null\ndef payloadChars    = binding.hasVariable('payloadChars') ? payloadChars : null\ndef inlineSystemPrompt = binding.hasVariable('inlineSystemPrompt') ? inlineSystemPrompt : null\ndef inlineNewMessages  = binding.hasVariable('inlineNewMessages') ? inlineNewMessages : null\n\n// `subTurnFileObject` is gone. The sub-turn no longer hides behind a signed URL: the diff METRICS\n// are inline, and only the two values that are actually large — the system prompt and the new\n// messages — are referenced out of line, from the keys that used to hold them.\nif (inputDiff != null) {\n    def diff = new LinkedHashMap(inputDiff instanceof Map ? inputDiff : [:])\n    if (payloadChars != null) diff.payloadChars = payloadChars\n    if (hasLargePayload == true && payloadFile != null) {\n        // Over the threshold the file is written ONCE per span and hung at span level as\n        // subTurnFileObject; the two diff keys name what to read out of it rather than each\n        // repeating the whole file object (110 KB of a 414 KB response said the same URL twice).\n        subTurn['subTurnFileObject'] = payloadFile\n        if (diff.systemPromptChanged == true) {\n            diff.systemPrompt = [key: 'systemPrompt', chars: diff.systemPromptChars, in: 'subTurnFileObject']\n        }\n        if ((diff.newMessageCount ?: 0) > 0) {\n            diff.newMessages = [key: 'newMessages', count: diff.newMessageCount, in: 'subTurnFileObject']\n        }\n    } else {\n        // Under it no file was written at all, so the values ride inline.\n        if (inlineSystemPrompt != null) diff.systemPrompt = inlineSystemPrompt\n        if (inlineNewMessages != null)  diff.newMessages = inlineNewMessages\n    }\n    subTurn['inputDiff'] = diff\n}\n\nif (outputText != null) {\n    subTurn['outputPreview'] = outputText\n    if (outputChars != null) subTurn['outputChars'] = outputChars\n}\n\nreturn subTurn",
				"compile_static":false,
				"captureStdOutput":false,
				"parameters":{
					"hasLargePayload":"{{ n_VgcsB.outputs.result.hasLargePayload }}",
					"inlineNewMessages":{
						"items":"{{ n_VgcsB.outputs.result.inlineNewMessages[0] }}",
						"source":"{{ n_VgcsB.outputs.result.inlineNewMessages }}",
						"ua:type":"mappedArray"
					},
					"inlineSystemPrompt":"{{ n_VgcsB.outputs.result.inlineSystemPrompt }}",
					"inputDiff":"{{ n_VgcsB.outputs.result.subTurnResponse.inputDiff }}",
					"output":"{{ n_VgcsB.outputs.result.subTurnResponse.outputPreview }}",
					"outputChars":"{{ n_VgcsB.outputs.result.subTurnResponse.outputChars }}",
					"payloadChars":"{{ n_VgcsB.outputs.result.payloadChars }}",
					"payloadFile":"{{ n_2oX47.outputs.subTurnFile }}",
					"subTurn":"{{ n_2oX47.outputs.subTurn }}"
				}
			},
			"skip":false,
			"subTitle":"Code",
			"title":"Attach Payload Data",
			"type":"ACTION"
		},
		{
			"context":{
				"appName":"code_by_unifyapps",
				"resourceVersion":0,
				"resourceName":"code_by_unifyapps_groovy",
				"type":"APPLICATION"
			},
			"debug":false,
			"dirty":false,
			"fallbackMode":"STOP",
			"groupId":"n_KZZsd@n_moIvM@n_vU2yL@_JAI60-1@l@2@y",
			"id":"n_mPkXD",
			"index":32,
			"inputs":{
				"output":{
					"additionalProperties":false,
					"properties":{
						"tools":{
							"items":{
								"additionalProperties":false,
								"properties":{},
								"type":"object"
							},
							"title":"Tools",
							"type":"array"
						}
					},
					"required":[
						"tools"
					],
					"type":"object"
				},
				"input":{
					"additionalProperties":false,
					"properties":{
						"index":{
							"title":"Index",
							"type":"integer"
						},
						"toolsByOwnerIndex":{
							"additionalProperties":false,
							"properties":{},
							"title":"Tools By Owner Index",
							"type":"object"
						}
					},
					"required":[
						"toolsByOwnerIndex",
						"index"
					],
					"type":"object"
				},
				"code":"def toolsByOwnerIndex = (binding.hasVariable('toolsByOwnerIndex') && toolsByOwnerIndex != null) ? toolsByOwnerIndex : [:]\ndef index = (binding.hasVariable('index') && index != null) ? index : -1\n\ndef tools = toolsByOwnerIndex[index.toString()] ?: []\n\nreturn [\n    tools: tools\n]",
				"compile_static":false,
				"captureStdOutput":false,
				"parameters":{
					"index":"{{ n_vU2yL.outputs.index }}",
					"toolsByOwnerIndex":"{{ n_VYOw1.outputs.result.toolsByOwnerIndex }}"
				}
			},
			"skip":false,
			"subTitle":"Code",
			"title":"Tools Called by LLM",
			"type":"ACTION"
		},
		{
			"context":{
				"appName":"variable_by_unifyapps",
				"resourceVersion":0,
				"resourceName":"variable_by_unifyapps_create_list",
				"type":"APPLICATION"
			},
			"debug":false,
			"dirty":false,
			"fallbackMode":"STOP",
			"groupId":"n_KZZsd@n_moIvM@n_vU2yL@_JAI60-1@l@2@y",
			"id":"n_BZnmR",
			"index":33,
			"inputSchema":{
				"type":"SCHEMA_AND_LAYOUT",
				"dynamic":false,
				"layout":{
					"createList":{
						"items":{
							"ui:description":"Set the initial values of the list (defaults to empty)",
							"ui:title":"Initial list items"
						},
						"ua:type":{
							"ui:widget":"hidden"
						},
						"ui:order":[
							"*",
							"items"
						],
						"ui:title":""
					},
					"ua:title":{
						"ui:widget":"hidden"
					},
					"ua:list_type":{
						"ui:widget":"hidden"
					}
				},
				"schema":{
					"allOf":[
						{
							"if":{
								"properties":{
									"ua:list_type":{
										"const":"OBJECT"
									}
								}
							},
							"then":{
								"properties":{
									"createList":{
										"properties":{
											"items":{
												"properties":{},
												"type":"object"
											}
										},
										"type":"object"
									}
								}
							}
						},
						{
							"if":{
								"properties":{
									"ua:list_type":{
										"const":"STRING"
									}
								}
							},
							"then":{
								"properties":{
									"createList":{
										"properties":{
											"items":{
												"type":"string"
											},
											"source":{
												"format":"ua:source",
												"title":"List of String",
												"type":"string"
											}
										},
										"type":"object"
									}
								}
							}
						},
						{
							"if":{
								"properties":{
									"ua:list_type":{
										"const":"INTEGER"
									}
								}
							},
							"then":{
								"properties":{
									"createList":{
										"properties":{
											"items":{
												"type":"integer"
											},
											"source":{
												"format":"ua:source",
												"title":"List of Integers",
												"type":"string"
											}
										},
										"type":"object"
									}
								}
							}
						},
						{
							"if":{
								"properties":{
									"ua:list_type":{
										"const":"NUMBER"
									}
								}
							},
							"then":{
								"properties":{
									"createList":{
										"properties":{
											"items":{
												"type":"number"
											},
											"source":{
												"format":"ua:source",
												"title":"List of Numbers",
												"type":"string"
											}
										},
										"type":"object"
									}
								}
							}
						},
						{
							"if":{
								"properties":{
									"ua:list_type":{
										"const":"BOOLEAN"
									}
								}
							},
							"then":{
								"properties":{
									"createList":{
										"properties":{
											"items":{
												"type":"boolean"
											},
											"source":{
												"format":"ua:source",
												"title":"List of Booleans",
												"type":"string"
											}
										},
										"type":"object"
									}
								}
							}
						}
					],
					"format":"ua:list",
					"type":"object",
					"properties":{
						"createList":{
							"properties":{
								"items":{
									"additionalProperties":false,
									"properties":{},
									"type":[
										"object",
										"string",
										"number",
										"boolean",
										"integer"
									]
								},
								"source":{
									"format":"ua:source",
									"title":"List Item Source",
									"type":"string"
								},
								"ua:type":{
									"default":"mappedArray",
									"type":"string"
								}
							},
							"type":"object"
						},
						"ua:list_type":{
							"default":"OBJECT",
							"oneOf":[
								{
									"const":"OBJECT",
									"title":"Object"
								},
								{
									"const":"STRING",
									"title":"String"
								},
								{
									"const":"NUMBER",
									"title":"Number"
								},
								{
									"const":"INTEGER",
									"title":"Integer"
								},
								{
									"const":"BOOLEAN",
									"title":"Boolean"
								}
							],
							"title":"List Type",
							"type":"string"
						},
						"ua:title":{
							"type":"string"
						}
					}
				}
			},
			"inputs":{
				"createList":{
					"ua:type":"mappedArray"
				},
				"ua:title":"Tool Objects",
				"ua:list_type":"OBJECT"
			},
			"outputSchema":{
				"type":"SCHEMA",
				"dynamic":false,
				"schema":{
					"type":"object",
					"properties":{
						"items":{
							"disableActions":true,
							"items":{
								"properties":{},
								"type":"object"
							},
							"title":"Tool Objects",
							"type":"array"
						}
					}
				}
			},
			"skip":false,
			"subTitle":"Variable",
			"title":"Create Tool Objects",
			"type":"ACTION"
		},
		{
			"context":{
				"appName":"loop",
				"resourceVersion":0,
				"resourceName":"loop_for_each",
				"type":"APPLICATION"
			},
			"debug":false,
			"dirty":false,
			"fallbackMode":"STOP",
			"groupId":"n_KZZsd@n_moIvM@n_vU2yL@_JAI60-1@l@2@y",
			"id":"n_DNslO",
			"index":34,
			"inputs":{
				"repeatMode":"SINGLE",
				"listSource":"{{ n_mPkXD.outputs.result.tools }}",
				"captureIterations":true
			},
			"skip":false,
			"subTitle":"Loop",
			"title":"For each loop",
			"type":"LOOP"
		},
		{
			"context":{
				"appName":"variable_by_unifyapps",
				"resourceVersion":0,
				"resourceName":"variable_by_unifyapps_create_variables",
				"type":"APPLICATION"
			},
			"debug":false,
			"dirty":false,
			"fallbackMode":"STOP",
			"groupId":"n_DNslO@n_KZZsd@n_moIvM@n_vU2yL@_JAI60-1@l@2@y@l",
			"id":"n_cvTol",
			"index":35,
			"inputSchema":{
				"type":"SCHEMA_AND_LAYOUT",
				"dynamic":false,
				"layout":{},
				"schema":{
					"additionalProperties":false,
					"type":"object",
					"properties":{
						"toolTurn":{
							"additionalProperties":false,
							"properties":{},
							"title":"Tool Turn",
							"type":"object"
						},
						"toolTurnFileObject":{
							"additionalProperties":false,
							"properties":{},
							"title":"Tool Turn File Object",
							"type":"object"
						}
					},
					"required":[
						"toolTurnFileObject",
						"toolTurn"
					]
				}
			},
			"inputs":{},
			"outputSchema":{
				"type":"SCHEMA",
				"dynamic":false,
				"schema":{
					"additionalProperties":false,
					"type":"object",
					"properties":{
						"toolTurn":{
							"additionalProperties":false,
							"properties":{},
							"title":"Tool Turn",
							"type":"object"
						},
						"toolTurnFileObject":{
							"additionalProperties":false,
							"properties":{},
							"title":"Tool Turn File Object",
							"type":"object"
						}
					},
					"required":[
						"toolTurnFileObject",
						"toolTurn"
					]
				}
			},
			"skip":false,
			"subTitle":"Variable",
			"title":"Create variables",
			"type":"ACTION"
		},
		{
			"context":{
				"appName":"code_by_unifyapps",
				"resourceVersion":0,
				"resourceName":"code_by_unifyapps_groovy",
				"type":"APPLICATION"
			},
			"debug":false,
			"dirty":false,
			"fallbackMode":"STOP",
			"groupId":"n_DNslO@n_KZZsd@n_moIvM@n_vU2yL@_JAI60-1@l@2@y@l",
			"id":"n_t3ANJ",
			"index":36,
			"inputs":{
				"output":{
					"additionalProperties":false,
					"properties":{
						"toolTraceId":{
							"title":"Tool Trace Id",
							"type":"string"
						},
						"toolTurn":{
							"additionalProperties":false,
							"properties":{},
							"title":"Tool Turn",
							"type":"object"
						}
					},
					"required":[
						"toolTurn",
						"toolTraceId"
					],
					"type":"object"
				},
				"input":{
					"additionalProperties":false,
					"properties":{
						"currentToolSpan":{
							"additionalProperties":false,
							"properties":{},
							"title":"Current Tool Span",
							"type":"object"
						},
						"delegationToolIds":{
							"items":{
								"type":"string"
							},
							"title":"Delegation Tool Ids",
							"type":"array"
						}
					},
					"required":[
						"currentToolSpan"
					],
					"type":"object"
				},
				"code":"def currentToolSpan = (binding.hasVariable('currentToolSpan') && currentToolSpan != null) ? currentToolSpan : [:]\ndef delegationToolIds = (binding.hasVariable('delegationToolIds') && delegationToolIds != null) ? delegationToolIds : []\n\ndef toolTraceId = currentToolSpan?.id\ndef toolTurn = [:]\n\n[\n    'currentType',\n    'success',\n    'provider',\n    'toolActionId',\n    'toolAppName',\n    'toolInvocationCost',\n    'toolName',\n    'startTime',\n    'llmTimeTaken',\n    'isDelegation',\n    // 'attachedVia'\n].each { field ->\n    def value = currentToolSpan?.\"${field}\"\n    if (value != null) {\n        toolTurn[field] = value\n    }\n}\n\n// e_traces carries no error text for a tool call, so this is the only honest thing to say — and\n// it is only said when the call actually failed.\nif (currentToolSpan?.success == false) toolTurn.failedReason = 'tool reported success=false'\n\n// A delegation wrapper's duration CONTAINS the delegated agent's whole run, so its milliseconds\n// must not be added to the same total as that agent's own LLM and tool time. On one real case the\n// four wrappers were 4,908,311 ms of the 4,972,765 ms reported as tool time — 98.7% double count,\n// and page 1 claimed more tool time (56 min) than the page lasted (30.5 min).\n//\n// n_VYOw1 now stamps the verdict on the row itself, so the line above already copied it. This is\n// the belt-and-braces path for the case where that stamp did not survive: same answer, computed\n// from the id list. n_WB2iD then re-derives it a third time from the assembled windows.\nif (toolTurn.isDelegation != true && toolTraceId != null &&\n    (delegationToolIds as Set).contains(toolTraceId as String)) {\n    toolTurn.isDelegation = true\n}\n\nreturn [\n    toolTurn: toolTurn,\n    toolTraceId: toolTraceId\n]",
				"compile_static":false,
				"captureStdOutput":false,
				"parameters":{
					"currentToolSpan":"{{ n_DNslO.outputs.item }}",
					"delegationToolIds":{
						"items":"{{ n_VYOw1.outputs.result.delegationToolIds[0] }}",
						"source":"{{ n_VYOw1.outputs.result.delegationToolIds }}",
						"ua:type":"mappedArray"
					}
				}
			},
			"skip":false,
			"subTitle":"Code",
			"title":"Execute Groovy code",
			"type":"ACTION"
		},
		{
			"context":{
				"appName":"analytics_by_unifyapps",
				"resourceVersion":0,
				"resourceName":"analytics_by_unifyapps_execute_query",
				"type":"APPLICATION"
			},
			"debug":false,
			"dirty":false,
			"fallbackMode":"STOP",
			"groupId":"n_DNslO@n_KZZsd@n_moIvM@n_vU2yL@_JAI60-1@l@2@y@l",
			"id":"n_Wr6cd",
			"index":37,
			"inputs":{
				"outputSchema":{
					"additionalProperties":false,
					"properties":{
						"input":{
							"title":"Input",
							"type":"string"
						},
						"outputObject":{
							"title":"Output Object",
							"type":"string"
						}
					},
					"required":[
						"input",
						"outputObject"
					],
					"type":"object"
				},
				"query":"SELECT\n  _pr_input AS input,\n  COALESCE(_pr_outputObject, _pr_output) AS outputObject\nFROM ENTITY_e_traces\nWHERE id = :tool_trace_id\nLIMIT 1;",
				"record":{
					"tool_trace_id":"{{ n_t3ANJ.outputs.result.toolTraceId }}"
				},
				"params":{
					"additionalProperties":false,
					"properties":{
						"tool_trace_id":{
							"title":"Tool Trace Id",
							"type":"string"
						}
					},
					"required":[],
					"type":"object"
				},
				"group":"ENTITY_REPORTING"
			},
			"skip":false,
			"subTitle":"Analytics by UnifyApps",
			"title":"Get Input/Output Full",
			"type":"ACTION"
		},
		{
			"context":{
				"appName":"code_by_unifyapps",
				"resourceVersion":0,
				"resourceName":"code_by_unifyapps_groovy",
				"type":"APPLICATION"
			},
			"debug":false,
			"dirty":false,
			"fallbackMode":"STOP",
			"groupId":"n_DNslO@n_KZZsd@n_moIvM@n_vU2yL@_JAI60-1@l@2@y@l",
			"id":"n_zFiVb",
			"index":38,
			"inputs":{
				"output":{
					"additionalProperties":false,
					"properties":{
						"inputChars":{
							"title":"Input Chars",
							"type":"integer"
						},
						"needsFile":{
							"title":"Needs File",
							"type":"boolean"
						},
						"outputChars":{
							"title":"Output Chars",
							"type":"integer"
						},
						"response":{
							"additionalProperties":false,
							"properties":{},
							"title":"Response",
							"type":"object"
						},
						"responseString":{
							"title":"Response String",
							"type":"string"
						}
					},
					"required":[
						"response",
						"inputChars",
						"outputChars",
						"needsFile",
						"responseString"
					],
					"type":"object"
				},
				"input":{
					"additionalProperties":false,
					"properties":{
						"input":{
							"title":"Input",
							"type":"string"
						},
						"outputObject":{
							"title":"Output Object",
							"type":"string"
						}
					},
					"required":[
						"input",
						"outputObject"
					],
					"type":"object"
				},
				"code":"def inObj  = binding.hasVariable('input') ? input : null\ndef outObj = binding.hasVariable('outputObject') ? outputObject : null\n\ndef asText = { v ->\n    if (v == null) return null\n    (v instanceof CharSequence) ? (v as String) : groovy.json.JsonOutput.toJson(v)\n}\n\ndef inText  = asText(inObj)\ndef outText = asText(outObj)\ndef inputChars  = inText == null ? 0 : inText.length()\ndef outputChars = outText == null ? 0 : outText.length()\n\n// The file call downstream is gated on this: a tool whose whole I/O fits inline does not need a\n// signed URL, and writing one cost a synchronous sub-automation call per tool span.\ndef FILE_THRESHOLD_CHARS = 2000\ndef needsFile = (inputChars + outputChars) > FILE_THRESHOLD_CHARS\n\ndef responseObj = [\n    inputObject : inObj,\n    outputObject: outObj\n]\ndef responseString = groovy.json.JsonOutput.toJson(responseObj)\n\nreturn [\n    response       : responseObj,\n    responseString : responseString,\n    inputChars     : inputChars,\n    outputChars    : outputChars,\n    needsFile      : needsFile\n]",
				"compile_static":false,
				"captureStdOutput":false,
				"parameters":{
					"input":"{{ n_Wr6cd.outputs.objects[0].columns.input }}",
					"outputObject":"{{ n_Wr6cd.outputs.objects[0].columns.outputObject }}"
				}
			},
			"skip":false,
			"subTitle":"Code",
			"title":"Record Tool I/O",
			"type":"ACTION"
		},
		{
			"context":{
				"appName":"if_else",
				"resourceVersion":61,
				"resourceName":"if_else_condition",
				"type":"APPLICATION"
			},
			"debug":false,
			"dirty":false,
			"fallbackMode":"STOP",
			"groupId":"n_DNslO@n_KZZsd@n_moIvM@n_vU2yL@_JAI60-1@l@2@y@l",
			"id":"n_IFtol",
			"index":39,
			"inputs":{
				"filters":[
					{
						"filter":{
							"operator":"EQUAL",
							"value":"True"
						},
						"property":"{{ n_zFiVb.outputs.result.needsFile }}"
					}
				],
				"operator":"AND"
			},
			"skip":false,
			"subTitle":"Condition",
			"title":"Tool I/O > 2000 chars?",
			"type":"IF_ELSE"
		},
		{
			"context":{
				"appName":"files_by_unifyapps",
				"resourceVersion":0,
				"resourceName":"files_by_unifyapps_create_temp_file",
				"type":"APPLICATION"
			},
			"debug":false,
			"dirty":false,
			"fallbackMode":"STOP",
			"groupId":"n_IFtol@n_DNslO@n_KZZsd@n_moIvM@n_vU2yL@_JAI60-1@l@2@y@l@y",
			"id":"n_A2tYc",
			"index":40,
			"inputs":{
				"folderPath":"tmp/singleTurnTimeline",
				"isTmp":false,
				"fileName":"toolTurn_{{ n_v4sZv.outputs.message_id }}_{{ n_vU2yL.outputs.index }}_{{ n_DNslO.outputs.index }}.txt",
				"initialContentType":"plainText",
				"uploadToCloudStorage":true,
				"initialContent":"{{ n_zFiVb.outputs.result.responseString }}",
				"mimeType":"text/plain",
				"type":"SINGLE",
				"fileType":"txt"
			},
			"skip":false,
			"subTitle":"Files by UnifyApps",
			"title":"Create a file",
			"type":"ACTION"
		},
		{
			"context":{
				"appName":"variable_by_unifyapps",
				"resourceVersion":0,
				"resourceName":"variable_by_unifyapps_update_variables",
				"type":"APPLICATION"
			},
			"debug":false,
			"dirty":false,
			"fallbackMode":"STOP",
			"groupId":"n_IFtol@n_DNslO@n_KZZsd@n_moIvM@n_vU2yL@_JAI60-1@l@2@y@l@y",
			"id":"n_upTol",
			"index":41,
			"inputs":{
				"variables":[
					{
						"source":"{{ n_cvTol.outputs.toolTurnFileObject }}",
						"value":"{{ n_A2tYc.outputs.fileDetails }}"
					}
				]
			},
			"skip":false,
			"subTitle":"Variable",
			"title":"Record file object",
			"type":"ACTION"
		},
		{
			"context":{
				"appName":"code_by_unifyapps",
				"resourceVersion":0,
				"resourceName":"code_by_unifyapps_groovy",
				"type":"APPLICATION"
			},
			"debug":false,
			"dirty":false,
			"fallbackMode":"STOP",
			"groupId":"n_DNslO@n_KZZsd@n_moIvM@n_vU2yL@_JAI60-1@l@2@y@l",
			"id":"n_cFcDR",
			"index":42,
			"inputs":{
				"output":{
					"additionalProperties":false,
					"properties":{
						"subTurn":{
							"additionalProperties":false,
							"properties":{},
							"title":"Sub Turn",
							"type":"object"
						}
					},
					"required":[
						"subTurn"
					],
					"type":"object"
				},
				"input":{
					"additionalProperties":false,
					"properties":{
						"currentToolTurn":{
							"additionalProperties":false,
							"properties":{},
							"title":"Current Tool Turn",
							"type":"object"
						},
						"inputChars":{
							"title":"Input Chars",
							"type":"integer"
						},
						"inputObject":{
							"additionalProperties":false,
							"properties":{},
							"title":"Input Object",
							"type":"object"
						},
						"outputChars":{
							"title":"Output Chars",
							"type":"integer"
						},
						"outputObject":{
							"additionalProperties":false,
							"properties":{},
							"title":"Output Object",
							"type":"object"
						},
						"previewChars":{
							"title":"Preview Chars",
							"type":"integer"
						},
						"toolTurnFileObject":{
							"additionalProperties":false,
							"properties":{},
							"title":"Tool Turn File Object",
							"type":"object"
						}
					},
					"required":[
						"inputObject",
						"outputObject",
						"currentToolTurn",
						"toolTurnFileObject"
					],
					"type":"object"
				},
				"code":"def currentToolTurn = (binding.hasVariable('currentToolTurn') && currentToolTurn != null) ? currentToolTurn : [:]\ndef previewChars = (binding.hasVariable('previewChars') && previewChars != null) ? (previewChars as int) : 2000\ndef inputChars   = binding.hasVariable('inputChars') ? inputChars : null\ndef outputChars  = binding.hasVariable('outputChars') ? outputChars : null\n\ndef asText = { obj ->\n    if (obj == null) return null\n    (obj instanceof String) ? obj : groovy.json.JsonOutput.toJson(obj)\n}\n\ndef preview = { obj ->\n    def text = asText(obj)\n    if (text == null) return null\n    return text.length() > previewChars ? text.substring(0, previewChars) + '…[truncated]' : text\n}\n\n[\n    inputPreview       : 'inputObject',\n    outputPreview      : 'outputObject',\n    toolTurnFileObject : 'toolTurnFileObject',\n].each { outField, bindingVar ->\n    if (binding.hasVariable(bindingVar)) {\n        def value = binding.getVariable(bindingVar)\n        if (value != null) {\n            currentToolTurn[outField] = outField.endsWith('Preview') ? preview(value) : value\n        }\n    }\n}\n\n// Full lengths always ride, whatever the preview budget was — otherwise a truncated preview is\n// indistinguishable from a short result.\nif (inputChars != null)  currentToolTurn['inputChars']  = inputChars\nif (outputChars != null) currentToolTurn['outputChars'] = outputChars\n// currentToolTurn['previewChars'] = previewChars\n\n// The delegation target, read from the FULL input rather than from the (possibly 200-char)\n// preview, so the tree can name the agent this tool handed off to at any preview budget.\nif (binding.hasVariable('inputObject') && inputObject != null) {\n    def full = asText(inputObject)\n    def m = full =~ /\"agentName\"\\s*:\\s*\"([^\"]{1,120})\"/\n    if (m.find()) currentToolTurn['targetAgentName'] = m.group(1)\n}\n\nreturn currentToolTurn",
				"compile_static":false,
				"captureStdOutput":false,
				"parameters":{
					"currentToolTurn":"{{ n_t3ANJ.outputs.result.toolTurn }}",
					"inputChars":"{{ n_zFiVb.outputs.result.inputChars }}",
					"inputObject":"{{ n_zFiVb.outputs.result.response.inputObject }}",
					"outputChars":"{{ n_zFiVb.outputs.result.outputChars }}",
					"outputObject":"{{ n_zFiVb.outputs.result.response.outputObject }}",
					"previewChars":"{{ n_VYOw1.outputs.result.previewCharsPerSpan }}",
					"toolTurnFileObject":"{{ n_cvTol.outputs.toolFile }}"
				}
			},
			"skip":false,
			"subTitle":"Code",
			"title":"Add data fields in Sub Turn",
			"type":"ACTION"
		},
		{
			"context":{
				"appName":"variable_by_unifyapps",
				"resourceVersion":0,
				"resourceName":"variable_by_unifyapps_add_item_to_list",
				"type":"APPLICATION"
			},
			"debug":false,
			"dirty":false,
			"fallbackMode":"STOP",
			"groupId":"n_DNslO@n_KZZsd@n_moIvM@n_vU2yL@_JAI60-1@l@2@y@l",
			"id":"n_dwr7W",
			"index":43,
			"inputs":{
				"itemToAdd":"{{ n_cFcDR.outputs.result }}",
				"insertLocation":"END",
				"captureAddedItem":false,
				"listName":"{{ n_BZnmR.outputs.items[0] }}"
			},
			"skip":false,
			"subTitle":"Variable",
			"title":"Add item to list",
			"type":"ACTION"
		},
		{
			"context":{
				"appName":"code_by_unifyapps",
				"resourceVersion":0,
				"resourceName":"code_by_unifyapps_groovy",
				"type":"APPLICATION"
			},
			"debug":false,
			"dirty":false,
			"fallbackMode":"STOP",
			"groupId":"n_KZZsd@n_moIvM@n_vU2yL@_JAI60-1@l@2@y",
			"id":"n_XJ345",
			"index":44,
			"inputs":{
				"output":{
					"additionalProperties":false,
					"properties":{
						"subTurn":{
							"additionalProperties":false,
							"properties":{},
							"title":"Sub Turn",
							"type":"object"
						}
					},
					"required":[
						"subTurn"
					],
					"type":"object"
				},
				"input":{
					"additionalProperties":false,
					"properties":{
						"auxByOwnerIndex":{
							"additionalProperties":false,
							"properties":{},
							"title":"Aux By Owner Index",
							"type":"object"
						},
						"index":{
							"title":"Index",
							"type":"integer"
						},
						"subTurn":{
							"additionalProperties":false,
							"properties":{},
							"title":"Sub Turn",
							"type":"object"
						},
						"toolObjects":{
							"items":{
								"additionalProperties":false,
								"properties":{},
								"type":"object"
							},
							"title":"Tool Objects",
							"type":"array"
						}
					},
					"required":[
						"toolObjects",
						"subTurn"
					],
					"type":"object"
				},
				"code":"def toolObjects = (binding.hasVariable('toolObjects') && toolObjects != null) ? toolObjects : []\ndef subTurn = (binding.hasVariable('subTurn') && subTurn != null) ? subTurn : [:]\ndef auxByOwnerIndex = (binding.hasVariable('auxByOwnerIndex') && auxByOwnerIndex != null) ? auxByOwnerIndex : [:]\ndef index = (binding.hasVariable('index') && index != null) ? index : -1\n\ndef sortedTools = toolObjects\n    .findAll { it?.startTime != null }\n    .collect { it + [endTime: it.startTime + (it.llmTimeTaken ?: 0)] }\n    .sort { a, b ->\n        (a.startTime <=> b.startTime) ?:\n        (b.endTime <=> a.endTime) ?:\n        ((a.toolName ?: '') <=> (b.toolName ?: ''))\n    }\n\n// A tool row with no startTime cannot be placed on the timeline, but dropping it silently also\n// dropped its cost. Keep it, unbatched, and say why.\ndef undated = toolObjects.findAll { it != null && it?.startTime == null }\n\n//\n// Group overlapping tools into batches\n// endTime is inferred from llmTimeTaken\n//\ndef batches = []\n\nsortedTools.each { tool ->\n    def lastBatch = batches ? batches[-1] : null\n\n    def overlapsLastBatch = lastBatch && lastBatch.any { t ->\n        tool.startTime < t.endTime && t.startTime < tool.endTime\n    }\n\n    if (overlapsLastBatch) {\n        lastBatch << tool\n    } else {\n        batches << [tool]\n    }\n}\n\ndef parallelToolCalls = batches.any { it.size() > 1 }\n\ndef aux = (auxByOwnerIndex[index.toString()] ?: []).collect { s ->\n    def step = [:]\n    ['currentType','startTime','llmTimeTaken','success','cost',\n     'guardrailViolationDetected','guardrailViolationReason','attachedVia'].each { f ->\n        def v = s?.\"${f}\"\n        if (v != null) step[f] = v\n    }\n    step\n}.sort { it?.startTime ?: 0 }\n\ndef subTurnObject = subTurn + [\n    tools             : sortedTools.collect { it.findAll { k, v -> k != 'endTime' } } +\n                        undated.collect { it + [unplaced: true] },\n    parallelToolCalls : parallelToolCalls,\n    toolBatches       : batches.collect { batch ->\n        batch.collect { it.toolName }\n    }\n]\n\n// Masking / filter / retrieval-side steps that ran inside this LLM call. Previously collected\n// into `interstitialsByOwnerIndex` and then dropped — 122 of the sample turn's 245 rows.\nif (aux) {\n    subTurnObject.overheadSteps = aux\n    subTurnObject.overheadTimeMs = aux.sum { (it?.llmTimeTaken ?: 0) } ?: 0\n}\n\nreturn subTurnObject",
				"compile_static":false,
				"captureStdOutput":false,
				"parameters":{
					"auxByOwnerIndex":"{{ n_VYOw1.outputs.result.auxByOwnerIndex }}",
					"index":"{{ n_vU2yL.outputs.index }}",
					"subTurn":"{{ n_bGet4.outputs.result }}",
					"toolObjects":{
						"items":"{{ n_BZnmR.outputs.items[0] }}",
						"source":"{{ n_BZnmR.outputs.items }}",
						"ua:type":"mappedArray"
					}
				}
			},
			"skip":false,
			"subTitle":"Code",
			"title":"Execute Groovy code",
			"type":"ACTION"
		},
		{
			"context":{
				"appName":"variable_by_unifyapps",
				"resourceVersion":0,
				"resourceName":"variable_by_unifyapps_add_item_to_list",
				"type":"APPLICATION"
			},
			"debug":false,
			"dirty":false,
			"fallbackMode":"STOP",
			"groupId":"n_KZZsd@n_moIvM@n_vU2yL@_JAI60-1@l@2@y",
			"id":"n_mcJ3K",
			"index":45,
			"inputs":{
				"itemToAdd":"{{ n_XJ345.outputs.result }}",
				"insertLocation":"END",
				"captureAddedItem":false,
				"listName":"{{ n_eE59d.outputs.items[0] }}"
			},
			"skip":false,
			"subTitle":"Variable",
			"title":"Add item to list",
			"type":"ACTION"
		},
		{
			"context":{
				"appName":"branch_condition",
				"resourceVersion":0,
				"resourceName":"branch_condition",
				"type":"APPLICATION"
			},
			"debug":false,
			"dirty":false,
			"fallbackMode":"STOP",
			"groupId":"n_moIvM@n_vU2yL@_JAI60-1@l@3",
			"id":"n_moIvM@3",
			"index":46,
			"inputs":{
				"name":"Response Guardrail",
				"conditions":{
					"filters":[
						{
							"filter":{
								"operator":"EQUAL",
								"value":"Response Guardrail"
							},
							"property":"{{ n_zKUfl.outputs.result.parent }}"
						}
					],
					"operator":"AND"
				}
			},
			"skip":false,
			"subTitle":"Branch Condition",
			"title":"Response Guardrail",
			"type":"BRANCH_CONDITION"
		},
		{
			"context":{
				"appName":"code_by_unifyapps",
				"resourceVersion":929,
				"resourceName":"code_by_unifyapps_groovy",
				"type":"APPLICATION"
			},
			"debug":false,
			"dirty":false,
			"fallbackMode":"STOP",
			"groupId":"n_moIvM@n_vU2yL@_JAI60-1@l@3",
			"id":"n_kYScV",
			"index":47,
			"inputs":{
				"output":{
					"additionalProperties":false,
					"properties":{
						"subTurn":{
							"additionalProperties":false,
							"properties":{},
							"title":"Sub Turn",
							"type":"object"
						}
					},
					"required":[
						"subTurn"
					],
					"type":"object"
				},
				"input":{
					"additionalProperties":false,
					"properties":{
						"auxByOwnerIndex":{
							"additionalProperties":false,
							"properties":{},
							"title":"Aux By Owner Index",
							"type":"object"
						},
						"currentSpan":{
							"additionalProperties":false,
							"properties":{},
							"title":"Current Span",
							"type":"object"
						},
						"index":{
							"title":"Index",
							"type":"integer"
						}
					},
					"required":[
						"currentSpan"
					],
					"type":"object"
				},
				"code":"def currentSpan = (binding.hasVariable('currentSpan') && currentSpan != null) ? currentSpan : [:]\ndef index = (binding.hasVariable('index') && index != null) ? index : -1\ndef auxByOwnerIndex = (binding.hasVariable('auxByOwnerIndex') && auxByOwnerIndex != null) ? auxByOwnerIndex : [:]\n\ndef subTurn = [:]\n\n// The cost/token columns are read optimistically. They were left out on the assumption that a\n// guardrail span never carries them; the query selects them for every row, and `if (value != null)`\n// already omits the ones that really are absent — so an LLM-backed guard (Content Filters,\n// Groundedness, Denial) now contributes its cost instead of silently contributing nothing.\n[\n    'currentType',\n    'aiAgentId',\n    'success',\n    'startTime',\n    'llmTimeTaken',\n    'provider',\n    'modelId',\n    'finishReason',\n    'llmErrorType',\n    'cost',\n    'inputToken',\n    'outputToken',\n    'billableInputTokens',\n    'inputTokenCost',\n    'outputTokenCost',\n    'cacheReadInputTokens',\n    'cacheWriteInputTokens',\n    'cacheReadCost',\n    'cacheWriteCost',\n    'guardrailViolationDetected',\n    'guardrailViolationReason',\n]\n.each { field ->\n    def value = currentSpan?.\"${field}\"\n    if (value != null) {\n        subTurn[field] = value\n    }\n}\n\ndef failedReason = null\nif (currentSpan?.guardrailViolationReason != null) {\n    failedReason = currentSpan.guardrailViolationReason as String\n} else if (currentSpan?.llmErrorType != null) {\n    failedReason = currentSpan.llmErrorType as String\n} else if (currentSpan?.success == false) {\n    failedReason = 'response guardrail reported success=false'\n}\nif (failedReason != null) subTurn.failedReason = failedReason\n\n// Masking / filter steps that ran inside this guard. They used to be collected and thrown away.\ndef aux = auxByOwnerIndex[index.toString()] ?: []\nif (aux) {\n    subTurn.overheadSteps = aux.collect { s ->\n        def step = [:]\n        ['currentType','startTime','llmTimeTaken','success','cost',\n         'guardrailViolationDetected','guardrailViolationReason','attachedVia'].each { f ->\n            def v = s?.\"${f}\"\n            if (v != null) step[f] = v\n        }\n        step\n    }.sort { it?.startTime ?: 0 }\n    subTurn.overheadTimeMs = subTurn.overheadSteps.sum { (it?.llmTimeTaken ?: 0) } ?: 0\n}\n// Global span number. n_APnyI stamped every row with its position in the WHOLE ordered result\n// (offset + position-in-page), so this is continuous across pages; `index` is the loop's own\n// page-local counter and stays page-local because it is the key into ownerMeta/auxByOwnerIndex.\nsubTurn.spanIndex = (currentSpan?.spanIndex != null) ? currentSpan.spanIndex : index\n\nreturn subTurn",
				"compile_static":false,
				"captureStdOutput":false,
				"parameters":{
					"auxByOwnerIndex":"{{ n_VYOw1.outputs.result.auxByOwnerIndex }}",
					"currentSpan":"{{ n_vU2yL.outputs.item }}",
					"index":"{{ n_vU2yL.outputs.index }}"
				}
			},
			"skip":false,
			"subTitle":"Code",
			"title":"Execute Groovy code",
			"type":"ACTION"
		},
		{
			"context":{
				"appName":"variable_by_unifyapps",
				"resourceVersion":0,
				"resourceName":"variable_by_unifyapps_add_item_to_list",
				"type":"APPLICATION"
			},
			"debug":false,
			"dirty":false,
			"fallbackMode":"STOP",
			"groupId":"n_moIvM@n_vU2yL@_JAI60-1@l@3",
			"id":"n_FHsWj",
			"index":48,
			"inputs":{
				"itemToAdd":"{{ n_kYScV.outputs.result }}",
				"insertLocation":"END",
				"captureAddedItem":false,
				"listName":"{{ n_Be9QU.outputs.items[0] }}"
			},
			"skip":false,
			"subTitle":"Variable",
			"title":"Add item to list",
			"type":"ACTION"
		},
		{
			"context":{
				"appName":"branch_condition",
				"resourceVersion":0,
				"resourceName":"branch_condition",
				"type":"APPLICATION"
			},
			"debug":false,
			"dirty":false,
			"fallbackMode":"STOP",
			"groupId":"n_moIvM@n_vU2yL@_JAI60-1@l@4",
			"id":"n_moIvM@4",
			"index":49,
			"inputs":{
				"name":"Knowledge Retrieval",
				"conditions":{
					"filters":[
						{
							"filter":{
								"operator":"EQUAL",
								"value":"ReferFromKnowledge"
							},
							"property":"{{ n_zKUfl.outputs.result.parent }}"
						},
						{
							"filter":{
								"operator":"EQUAL",
								"value":"Retrieval"
							},
							"property":"{{ n_zKUfl.outputs.result.currentType }}"
						}
					],
					"operator":"AND"
				}
			},
			"skip":false,
			"subTitle":"Branch Condition",
			"title":"Knowledge Retrieval",
			"type":"BRANCH_CONDITION"
		},
		{
			"context":{
				"appName":"code_by_unifyapps",
				"resourceVersion":3801,
				"resourceName":"code_by_unifyapps_groovy",
				"type":"APPLICATION"
			},
			"debug":false,
			"dirty":false,
			"fallbackMode":"STOP",
			"groupId":"n_moIvM@n_vU2yL@_JAI60-1@l@4",
			"id":"n_UlhJE",
			"index":50,
			"inputs":{
				"output":{
					"additionalProperties":false,
					"properties":{
						"subTurn":{
							"additionalProperties":false,
							"properties":{},
							"title":"Sub Turn",
							"type":"object"
						}
					},
					"required":[
						"subTurn"
					],
					"type":"object"
				},
				"input":{
					"additionalProperties":false,
					"properties":{
						"auxByOwnerIndex":{
							"additionalProperties":false,
							"properties":{},
							"title":"Aux By Owner Index",
							"type":"object"
						},
						"currentSpan":{
							"additionalProperties":false,
							"properties":{},
							"title":"Current Span",
							"type":"object"
						},
						"index":{
							"title":"Index",
							"type":"integer"
						},
						"retrievalStepsByOwnerIndex":{
							"additionalProperties":false,
							"properties":{},
							"title":"Retrieval Steps By Owner Index",
							"type":"object"
						}
					},
					"required":[
						"currentSpan",
						"retrievalStepsByOwnerIndex",
						"index"
					],
					"type":"object"
				},
				"code":"def currentSpan = (binding.hasVariable('currentSpan') && currentSpan != null) ? currentSpan : [:]\ndef retrievalStepsByOwnerIndex = (binding.hasVariable('retrievalStepsByOwnerIndex') && retrievalStepsByOwnerIndex != null) ? retrievalStepsByOwnerIndex : [:]\ndef auxByOwnerIndex = (binding.hasVariable('auxByOwnerIndex') && auxByOwnerIndex != null) ? auxByOwnerIndex : [:]\ndef index = (binding.hasVariable('index') && index != null) ? index : -1\n\ndef subTurn = [:]\n// The owner's OWN cost was never copied, so retrievalCost was the children's cost only.\n['currentType','aiAgentId','success','startTime','llmTimeTaken',\n 'cost','inputToken','outputToken','inputTokenCost','outputTokenCost',\n 'billableInputTokens'].each { f ->\n    def v = currentSpan?.\"${f}\"\n    if (v != null) subTurn[f] = v\n}\nif (currentSpan?.success == false) subTurn.failedReason = 'retrieval reported success=false'\n\ndef steps = (retrievalStepsByOwnerIndex[index.toString()] ?: []).collect { child ->\n    def step = [:]\n    ['currentType','success','startTime','llmTimeTaken',\n     'cost','inputToken','outputToken','inputTokenCost','outputTokenCost','attachedVia'].each { f ->\n        def v = child?.\"${f}\"\n        if (v != null) step[f] = v\n    }\n    step\n}.sort { it?.startTime ?: 0 }\n\ndef aux = (auxByOwnerIndex[index.toString()] ?: []).collect { s ->\n    def step = [:]\n    ['currentType','startTime','llmTimeTaken','success','cost','attachedVia'].each { f ->\n        def v = s?.\"${f}\"\n        if (v != null) step[f] = v\n    }\n    step\n}.sort { it?.startTime ?: 0 }\n\nsubTurn.steps = steps\nif (aux) {\n    subTurn.overheadSteps = aux\n    subTurn.overheadTimeMs = aux.sum { (it?.llmTimeTaken ?: 0) } ?: 0\n}\n// Owner cost + step cost + the aux steps that ran inside the retrieval (embeddings, rerankers).\nsubTurn.retrievalCost = ((currentSpan?.cost ?: 0) as BigDecimal) +\n                        ((steps.sum { it?.cost ?: 0 } ?: 0) as BigDecimal) +\n                        ((aux.sum { it?.cost ?: 0 } ?: 0) as BigDecimal)\n// Global span number. n_APnyI stamped every row with its position in the WHOLE ordered result\n// (offset + position-in-page), so this is continuous across pages; `index` is the loop's own\n// page-local counter and stays page-local because it is the key into ownerMeta/auxByOwnerIndex.\nsubTurn.spanIndex = (currentSpan?.spanIndex != null) ? currentSpan.spanIndex : index\n\nreturn subTurn",
				"compile_static":false,
				"captureStdOutput":false,
				"parameters":{
					"auxByOwnerIndex":"{{ n_VYOw1.outputs.result.auxByOwnerIndex }}",
					"currentSpan":"{{ n_vU2yL.outputs.item }}",
					"index":"{{ n_vU2yL.outputs.index }}",
					"retrievalStepsByOwnerIndex":"{{ n_VYOw1.outputs.result.retrievalStepsByOwnerIndex }}"
				}
			},
			"skip":false,
			"subTitle":"Code by UnifyApps",
			"title":"Execute Groovy code",
			"type":"ACTION"
		},
		{
			"context":{
				"appName":"variable_by_unifyapps",
				"resourceVersion":3845,
				"resourceName":"variable_by_unifyapps_add_item_to_list",
				"type":"APPLICATION"
			},
			"debug":false,
			"dirty":false,
			"fallbackMode":"STOP",
			"groupId":"n_moIvM@n_vU2yL@_JAI60-1@l@4",
			"id":"n_kxKO5",
			"index":51,
			"inputs":{
				"itemToAdd":"{{ n_UlhJE.outputs.result }}",
				"insertLocation":"END",
				"listName":"{{ n_FPOkF.outputs.items[0] }}"
			},
			"skip":false,
			"subTitle":"Variable by UnifyApps",
			"title":"Add item to list",
			"type":"ACTION"
		},
		{
			"context":{
				"appName":"variable_by_unifyapps",
				"resourceVersion":0,
				"resourceName":"variable_by_unifyapps_create_list",
				"type":"APPLICATION"
			},
			"debug":false,
			"dirty":false,
			"fallbackMode":"STOP",
			"groupId":"_JAI60-1",
			"id":"n_QOhaa",
			"index":52,
			"inputSchema":{
				"type":"SCHEMA_AND_LAYOUT",
				"dynamic":false,
				"layout":{
					"createList":{
						"items":{
							"ui:description":"Set the initial values of the list (defaults to empty)",
							"ui:title":"Initial list items"
						},
						"ua:type":{
							"ui:widget":"hidden"
						},
						"ui:order":[
							"*",
							"items"
						],
						"ui:title":""
					},
					"ua:title":{
						"ui:widget":"hidden"
					},
					"ua:list_type":{
						"ui:widget":"hidden"
					}
				},
				"schema":{
					"allOf":[
						{
							"if":{
								"properties":{
									"ua:list_type":{
										"const":"OBJECT"
									}
								}
							},
							"then":{
								"properties":{
									"createList":{
										"properties":{
											"items":{
												"properties":{},
												"type":"object"
											}
										},
										"type":"object"
									}
								}
							}
						},
						{
							"if":{
								"properties":{
									"ua:list_type":{
										"const":"STRING"
									}
								}
							},
							"then":{
								"properties":{
									"createList":{
										"properties":{
											"items":{
												"type":"string"
											},
											"source":{
												"format":"ua:source",
												"title":"List of String",
												"type":"string"
											}
										},
										"type":"object"
									}
								}
							}
						},
						{
							"if":{
								"properties":{
									"ua:list_type":{
										"const":"INTEGER"
									}
								}
							},
							"then":{
								"properties":{
									"createList":{
										"properties":{
											"items":{
												"type":"integer"
											},
											"source":{
												"format":"ua:source",
												"title":"List of Integers",
												"type":"string"
											}
										},
										"type":"object"
									}
								}
							}
						},
						{
							"if":{
								"properties":{
									"ua:list_type":{
										"const":"NUMBER"
									}
								}
							},
							"then":{
								"properties":{
									"createList":{
										"properties":{
											"items":{
												"type":"number"
											},
											"source":{
												"format":"ua:source",
												"title":"List of Numbers",
												"type":"string"
											}
										},
										"type":"object"
									}
								}
							}
						},
						{
							"if":{
								"properties":{
									"ua:list_type":{
										"const":"BOOLEAN"
									}
								}
							},
							"then":{
								"properties":{
									"createList":{
										"properties":{
											"items":{
												"type":"boolean"
											},
											"source":{
												"format":"ua:source",
												"title":"List of Booleans",
												"type":"string"
											}
										},
										"type":"object"
									}
								}
							}
						}
					],
					"format":"ua:list",
					"type":"object",
					"properties":{
						"createList":{
							"properties":{
								"items":{
									"additionalProperties":false,
									"properties":{
										"batchSize":{
											"title":"Batch Size",
											"type":"integer"
										},
										"chunkIndex":{
											"title":"Chunk Index",
											"type":"integer"
										},
										"file":{
											"additionalProperties":false,
											"properties":{},
											"title":"File",
											"type":"object"
										},
										"startIndex":{
											"title":"Start Index",
											"type":"integer"
										}
									},
									"required":[
										"chunkIndex",
										"startIndex",
										"file",
										"batchSize"
									],
									"type":[
										"object",
										"string",
										"number",
										"boolean",
										"integer"
									]
								},
								"source":{
									"format":"ua:source",
									"title":"List Item Source",
									"type":"string"
								},
								"ua:type":{
									"default":"mappedArray",
									"type":"string"
								}
							},
							"type":"object"
						},
						"ua:list_type":{
							"default":"OBJECT",
							"oneOf":[
								{
									"const":"OBJECT",
									"title":"Object"
								},
								{
									"const":"STRING",
									"title":"String"
								},
								{
									"const":"NUMBER",
									"title":"Number"
								},
								{
									"const":"INTEGER",
									"title":"Integer"
								},
								{
									"const":"BOOLEAN",
									"title":"Boolean"
								}
							],
							"title":"List Type",
							"type":"string"
						},
						"ua:title":{
							"type":"string"
						}
					}
				}
			},
			"inputs":{
				"createList":{
					"ua:type":"mappedArray"
				},
				"ua:title":"Agent Execution Batches",
				"ua:list_type":"OBJECT"
			},
			"outputSchema":{
				"type":"SCHEMA",
				"dynamic":false,
				"schema":{
					"type":"object",
					"properties":{
						"items":{
							"disableActions":true,
							"items":{
								"properties":{
									"batchSize":{
										"title":"Batch Size",
										"type":"integer"
									},
									"chunkIndex":{
										"title":"Chunk Index",
										"type":"integer"
									},
									"file":{
										"additionalProperties":false,
										"properties":{},
										"title":"File",
										"type":"object"
									},
									"startIndex":{
										"title":"Start Index",
										"type":"integer"
									}
								},
								"required":[
									"chunkIndex",
									"startIndex",
									"file",
									"batchSize"
								],
								"type":"object"
							},
							"title":"Agent Execution Batches",
							"type":"array"
						}
					}
				}
			},
			"skip":false,
			"subTitle":"Variable by UnifyApps",
			"title":"Create list",
			"type":"ACTION"
		},
		{
			"context":{
				"appName":"code_by_unifyapps",
				"resourceVersion":0,
				"resourceName":"code_by_unifyapps_groovy",
				"type":"APPLICATION"
			},
			"debug":false,
			"dirty":false,
			"fallbackMode":"STOP",
			"groupId":"_JAI60-1",
			"id":"n_chkGt",
			"index":53,
			"inputs":{
				"output":{
					"additionalProperties":false,
					"properties":{
						"agentCount":{
							"title":"Agent Count",
							"type":"integer"
						},
						"chunkPlan":{
							"items":{
								"additionalProperties":false,
								"properties":{},
								"type":"object"
							},
							"title":"Chunk Plan",
							"type":"array"
						},
						"chunkSize":{
							"title":"Chunk Size",
							"type":"integer"
						},
						"chunkedAgentIds":{
							"items":{
								"type":"string"
							},
							"title":"Chunked Agent Ids",
							"type":"array"
						},
						"needsChunking":{
							"title":"Needs Chunking",
							"type":"boolean"
						},
						"splitIntoFiles":{
							"title":"Split Into Files",
							"type":"boolean"
						},
						"turnCount":{
							"title":"Turn Count",
							"type":"integer"
						}
					},
					"required":[
						"chunkPlan",
						"needsChunking"
					],
					"type":"object"
				},
				"input":{
					"additionalProperties":false,
					"properties":{
						"agentExecution":{
							"items":{
								"additionalProperties":false,
								"properties":{},
								"type":"object"
							},
							"title":"Agent Execution",
							"type":"array"
						}
					},
					"required":[
						"agentExecution"
					],
					"type":"object"
				},
				"code":"// Plan the turn files, per agent.\n//\n// The break is per agentId: an agent's chunk 0 is ITS OWN first 30 calls, not \"wherever that agent\n// happened to start in the global array\". An agent at or under the chunk size is not filed at all;\n// 47 turns is two files (30 + 17).\n//\n// Under pagination this plans over THIS PAGE's calls for that agent, so a chunk's startIndex is a\n// position inside the page. The page it was cut from is stamped onto the file record by n_WB2iD,\n// which already holds `page` — deliberately NOT threaded through here. The earlier attempt passed\n// pageOffset down n_chkGt -> n_jrFAb -> n_GnI26 -> n_Tm8Ns and it arrived null, because n_Tm8Ns\n// hand-builds the list item from six named keys and pageOffset was not one of them. Four hops,\n// four chances to drop a field silently; the assembler needs zero.\ndef turns = (binding.hasVariable('agentExecution') && agentExecution != null) ? agentExecution : []\n\ndef CHUNK_SIZE = 30\n// false: an agent at or under CHUNK_SIZE keeps its turns inline even when a bigger agent forced the\n//        response into the file shape.\n// true : every agent is filed as soon as any agent crosses the threshold.\ndef FILE_ALL_AGENTS_WHEN_SPLIT = false\n\ndef byAgent = [:]\ndef order = []\nturns.eachWithIndex { t, i ->\n    def a = (t?.aiAgentId ?: '__unknown') as String\n    if (!byAgent.containsKey(a)) { byAgent[a] = []; order << a }\n    byAgent[a] << i\n}\n\ndef anyOverLimit = order.any { byAgent[it].size() > CHUNK_SIZE }\n\ndef chunkPlan = []\ndef chunkedAgentIds = []\norder.each { a ->\n    def rows = byAgent[a]\n    def fileThis = anyOverLimit && (FILE_ALL_AGENTS_WHEN_SPLIT || rows.size() > CHUNK_SIZE)\n    if (!fileThis) return\n    chunkedAgentIds << a\n    def c = 0\n    for (int start = 0; start < rows.size(); start += CHUNK_SIZE) {\n        def end = Math.min(start + CHUNK_SIZE, rows.size())\n        chunkPlan << [\n            agentId   : a,\n            chunkIndex: c,\n            startIndex: start,\n            turnCount : end - start,\n            // Indices into the flat list, so the turns themselves do not have to travel through\n            // this node's output.\n            rowIndices: rows.subList(start, end) as List\n        ]\n        c++\n    }\n}\n\nreturn [\n    chunkPlan      : chunkPlan,\n    chunkedAgentIds: chunkedAgentIds,\n    turnCount      : turns.size(),\n    agentCount     : order.size(),\n    chunkSize      : CHUNK_SIZE,\n    // The whole block is presented as files rather than inline turns once anything was filed.\n    splitIntoFiles : !chunkPlan.isEmpty(),\n    needsChunking  : !chunkPlan.isEmpty()\n]",
				"compile_static":false,
				"captureStdOutput":true,
				"parameters":{
					"agentExecution":{
						"items":"{{ n_eE59d.outputs.items[0] }}",
						"source":"{{ n_eE59d.outputs.items }}",
						"ua:type":"mappedArray"
					}
				}
			},
			"skip":false,
			"subTitle":"Code by UnifyApps",
			"title":"Plan chunk files (per agent)",
			"type":"ACTION"
		},
		{
			"context":{
				"appName":"if_else",
				"resourceVersion":61,
				"resourceName":"if_else_condition",
				"type":"APPLICATION"
			},
			"debug":false,
			"dirty":false,
			"fallbackMode":"STOP",
			"groupId":"_JAI60-1",
			"id":"n_IFchk",
			"index":54,
			"inputs":{
				"filters":[
					{
						"filter":{
							"operator":"EQUAL",
							"value":"True"
						},
						"property":"{{ n_chkGt.outputs.result.needsChunking }}"
					}
				],
				"operator":"AND"
			},
			"skip":false,
			"subTitle":"Condition",
			"title":"More than 10 turns?",
			"type":"IF_ELSE"
		},
		{
			"context":{
				"appName":"loop",
				"resourceVersion":0,
				"resourceName":"loop_for_each",
				"type":"APPLICATION"
			},
			"debug":false,
			"dirty":false,
			"fallbackMode":"STOP",
			"groupId":"n_IFchk@_JAI60-1@y",
			"id":"n_mm9mc",
			"index":55,
			"inputs":{
				"repeatMode":"SINGLE",
				"listSource":"{{ n_chkGt.outputs.result.chunkPlan }}",
				"captureIterations":true
			},
			"skip":false,
			"subTitle":"Loop",
			"title":"For each chunk",
			"type":"LOOP"
		},
		{
			"context":{
				"appName":"code_by_unifyapps",
				"resourceVersion":0,
				"resourceName":"code_by_unifyapps_groovy",
				"type":"APPLICATION"
			},
			"debug":false,
			"dirty":false,
			"fallbackMode":"STOP",
			"groupId":"n_mm9mc@n_IFchk@_JAI60-1@y@l",
			"id":"n_jrFAb",
			"index":56,
			"inputs":{
				"output":{
					"additionalProperties":false,
					"properties":{
						"agentId":{
							"title":"Agent Id",
							"type":"string"
						},
						"chunkIndex":{
							"title":"Chunk Index",
							"type":"integer"
						},
						"content":{
							"title":"Content",
							"type":"string"
						},
						"count":{
							"title":"Count",
							"type":"integer"
						},
						"startIndex":{
							"title":"Start Index",
							"type":"integer"
						}
					},
					"required":[
						"content",
						"count"
					],
					"type":"object"
				},
				"input":{
					"additionalProperties":false,
					"properties":{
						"agentExecution":{
							"items":{
								"additionalProperties":false,
								"properties":{},
								"type":"object"
							},
							"title":"Agent Execution",
							"type":"array"
						},
						"chunk":{
							"additionalProperties":false,
							"properties":{},
							"title":"Chunk",
							"type":"object"
						}
					},
					"required":[
						"chunk",
						"agentExecution"
					],
					"type":"object"
				},
				"code":"def all  = (binding.hasVariable('agentExecution') && agentExecution != null) ? agentExecution : []\ndef plan = (binding.hasVariable('chunk') && chunk != null) ? chunk : [:]\n\ndef rows = (plan?.rowIndices instanceof List) ? plan.rowIndices : []\ndef slice = rows.collect { i -> (i >= 0 && i < all.size()) ? all[i] : null }.findAll { it != null }\n\n// `_x` is the internal execution-id carrier (see n_k02Aq). It is what the arithmetic runs on, not\n// something a reader of the file should see, and it is stripped in exactly two places: here for the\n// filed turns, and in n_WB2iD for the inline ones.\ndef strip = { t -> (t instanceof Map) ? t.findAll { k, v -> k != '_x' } : t }\n\ndef lines = slice.collect { groovy.json.JsonOutput.toJson(strip(it)) }\n\nreturn [\n    agentId    : plan?.agentId,\n    chunkIndex : plan?.chunkIndex,\n    startIndex : plan?.startIndex,\n    count      : lines.size(),\n    content    : lines.join('\\n')\n]",
				"compile_static":false,
				"captureStdOutput":false,
				"parameters":{
					"agentExecution":{
						"items":"{{ n_eE59d.outputs.items[0] }}",
						"source":"{{ n_eE59d.outputs.items }}",
						"ua:type":"mappedArray"
					},
					"chunk":"{{ n_mm9mc.outputs.item }}"
				}
			},
			"skip":false,
			"subTitle":"Code by UnifyApps",
			"title":"Execute Groovy code",
			"type":"ACTION"
		},
		{
			"context":{
				"appName":"files_by_unifyapps",
				"resourceVersion":2256,
				"resourceName":"files_by_unifyapps_create_temp_file",
				"type":"APPLICATION"
			},
			"debug":false,
			"dirty":false,
			"fallbackMode":"STOP",
			"groupId":"n_mm9mc@n_IFchk@_JAI60-1@y@l",
			"id":"n_icOtM",
			"index":57,
			"inputs":{
				"folderPath":"tmp/singleTurnTimeline",
				"isTmp":false,
				"fileName":"chunk_{{ n_jrFAb.outputs.result.agentId }}_{{ n_jrFAb.outputs.result.chunkIndex }}.txt",
				"initialContentType":"plainText",
				"uploadToCloudStorage":true,
				"initialContent":"{{ n_jrFAb.outputs.result.content }}",
				"mimeType":"text/plain",
				"type":"SINGLE",
				"fileType":"txt"
			},
			"skip":false,
			"subTitle":"Files by UnifyApps",
			"title":"Create a file",
			"type":"ACTION"
		},
		{
			"context":{
				"appName":"code_by_unifyapps",
				"resourceVersion":0,
				"resourceName":"code_by_unifyapps_groovy",
				"type":"APPLICATION"
			},
			"debug":false,
			"dirty":false,
			"fallbackMode":"STOP",
			"groupId":"n_mm9mc@n_IFchk@_JAI60-1@y@l",
			"id":"n_GnI26",
			"index":58,
			"inputs":{
				"output":{
					"additionalProperties":false,
					"properties":{
						"agentId":{
							"title":"Agent Id",
							"type":"string"
						},
						"chunkIndex":{
							"title":"Chunk Index",
							"type":"integer"
						},
						"file":{
							"additionalProperties":false,
							"properties":{},
							"title":"File",
							"type":"object"
						},
						"startIndex":{
							"title":"Start Index",
							"type":"integer"
						},
						"turnCount":{
							"title":"Turn Count",
							"type":"integer"
						}
					},
					"required":[
						"file"
					],
					"type":"object"
				},
				"input":{
					"additionalProperties":false,
					"properties":{
						"agentId":{
							"title":"Agent Id",
							"type":"string"
						},
						"chunkIndex":{
							"title":"Chunk Index",
							"type":"integer"
						},
						"file":{
							"additionalProperties":false,
							"properties":{},
							"title":"File",
							"type":"object"
						},
						"startIndex":{
							"title":"Start Index",
							"type":"integer"
						},
						"turnCount":{
							"title":"Turn Count",
							"type":"integer"
						}
					},
					"required":[
						"file"
					],
					"type":"object"
				},
				"code":"def file = (binding.hasVariable('file') && file != null) ? file : null\n\nreturn [\n    agentId    : binding.hasVariable('agentId') ? agentId : null,\n    chunkIndex : binding.hasVariable('chunkIndex') ? chunkIndex : null,\n    startIndex : binding.hasVariable('startIndex') ? startIndex : null,\n    turnCount  : binding.hasVariable('turnCount') ? turnCount : null,\n    file       : file\n]",
				"compile_static":false,
				"captureStdOutput":false,
				"parameters":{
					"agentId":"{{ n_jrFAb.outputs.result.agentId }}",
					"chunkIndex":"{{ n_jrFAb.outputs.result.chunkIndex }}",
					"file":"{{ n_icOtM.outputs.fileDetails }}",
					"startIndex":"{{ n_jrFAb.outputs.result.startIndex }}",
					"turnCount":"{{ n_jrFAb.outputs.result.count }}"
				}
			},
			"skip":false,
			"subTitle":"Code by UnifyApps",
			"title":"Execute Groovy code",
			"type":"ACTION"
		},
		{
			"context":{
				"appName":"variable_by_unifyapps",
				"resourceVersion":0,
				"resourceName":"variable_by_unifyapps_add_item_to_list",
				"type":"APPLICATION"
			},
			"debug":false,
			"dirty":false,
			"fallbackMode":"STOP",
			"groupId":"n_mm9mc@n_IFchk@_JAI60-1@y@l",
			"id":"n_Tm8Ns",
			"index":59,
			"inputs":{
				"itemToAdd":{
					"agentId":"{{ n_GnI26.outputs.result.agentId }}",
					"batchSize":30,
					"chunkIndex":"{{ n_GnI26.outputs.result.chunkIndex }}",
					"file":"{{ n_GnI26.outputs.result.file }}",
					"startIndex":"{{ n_GnI26.outputs.result.startIndex }}",
					"turnCount":"{{ n_GnI26.outputs.result.turnCount }}"
				},
				"insertLocation":"END",
				"captureAddedItem":false,
				"listName":"{{ n_QOhaa.outputs.items[0] }}"
			},
			"skip":false,
			"subTitle":"Variable by UnifyApps",
			"title":"Add item to list",
			"type":"ACTION"
		},
		{
			"context":{
				"appName":"code_by_unifyapps",
				"resourceVersion":0,
				"resourceName":"code_by_unifyapps_groovy",
				"type":"APPLICATION"
			},
			"debug":false,
			"dirty":false,
			"fallbackMode":"STOP",
			"groupId":"_JAI60-1",
			"id":"n_WB2iD",
			"index":60,
			"inputs":{
				"output":{
					"additionalProperties":false,
					"properties":{
						"turn":{
							"additionalProperties":false,
							"properties":{},
							"title":"Turn",
							"type":"object"
						}
					},
					"required":[
						"turn"
					],
					"type":"object"
				},
				"input":{
					"additionalProperties":false,
					"properties":{
						"agentExecution":{
							"items":{
								"additionalProperties":false,
								"properties":{},
								"type":"object"
							},
							"title":"Agent Execution",
							"type":"array"
						},
						"agentExecutionFiles":{
							"items":{
								"additionalProperties":false,
								"properties":{},
								"type":"object"
							},
							"title":"Agent Execution Files",
							"type":"array"
						},
						"grouping":{
							"additionalProperties":false,
							"properties":{},
							"title":"Grouping",
							"type":"object"
						},
						"inputGuardrails":{
							"items":{
								"additionalProperties":false,
								"properties":{},
								"type":"object"
							},
							"title":"Input Guardrails",
							"type":"array"
						},
						"knowledgeRetrieval":{
							"items":{
								"additionalProperties":false,
								"properties":{},
								"type":"object"
							},
							"title":"Knowledge Retrieval",
							"type":"array"
						},
						"messageId":{
							"title":"Message Id",
							"type":"string"
						},
						"page":{
							"additionalProperties":false,
							"properties":{},
							"title":"Page",
							"type":"object"
						},
						"responseGuardrail":{
							"items":{
								"additionalProperties":false,
								"properties":{},
								"type":"object"
							},
							"title":"Response Guardrail",
							"type":"array"
						},
						"unattributedSteps":{
							"items":{
								"additionalProperties":false,
								"properties":{},
								"type":"object"
							},
							"title":"Unattributed Steps",
							"type":"array"
						}
					},
					"required":[
						"inputGuardrails",
						"agentExecution",
						"responseGuardrail",
						"knowledgeRetrieval",
						"messageId",
						"agentExecutionFiles"
					],
					"type":"object"
				},
				"code":"def getInput = { String name, def defaultValue = null ->\n    binding?.hasVariable(name) ? binding.getVariable(name) : defaultValue\n}\n\ndef num = { n ->\n    if (n == null) return 0L\n    try {\n        return (n as BigDecimal).longValue()\n    } catch (ignored) { return 0L }\n}\n\ndef money = { n ->\n    if (n == null) return 0G\n    try { return n as BigDecimal }\n    catch (ignored) { return 0 }\n}\n\ndef listInput = { String name ->\n    def value = getInput(name, [])\n    value instanceof Collection ? value : []\n}\n\ndef messageId = getInput('messageId', null)\n\ndef inGuards  = listInput('inputGuardrails')\ndef agentExec = listInput('agentExecution')\ndef respGuard = listInput('responseGuardrail')\ndef retrieval = listInput('knowledgeRetrieval')\ndef unattributed = listInput('unattributedSteps')\ndef agentExecFiles = listInput('agentExecutionFiles')\ndef grouping  = getInput('grouping', null)\ndef page      = getInput('page', null)\n\n// Is this the whole message, or a window on it? Everything summed below — cost, llmCalls,\n// toolsCount, latencyMs, status — is computed from the rows that were actually fetched. On a full\n// run that is the turn. On a page it is the PAGE, and saying so is the difference between a\n// number a reader can trust and one they will quietly mis-read as the turn's total.\ndef pageOffset = (page instanceof Map && page?.offset != null) ? num(page.offset) : 0L\ndef windowed   = (page instanceof Map) && (page?.partial == true)\n\ndef top = []\ntop.addAll(inGuards)\ntop.addAll(agentExec)\ntop.addAll(respGuard)\ntop.addAll(retrieval)\n\n// the internal carrier\n// `_x` holds the execution ids the arithmetic needs (see n_k02Aq). Read here, stripped before the\n// turn is returned, so the response carries the right numbers and none of the plumbing.\ndef X = { item -> (item instanceof Map && item?._x instanceof Map) ? item._x : [:] }\ndef threadIdOf = { item -> (X(item).threadId ?: item?.threadId ?: item?.aiAgentId) as String }\n\ndef childList = { item, String key ->\n    def v = (item instanceof Map) ? item[key] : null\n    v instanceof Collection ? v : []\n}\ndef safeTools = { item -> childList(item, 'tools') }\n\n// EVERY row of the turn, flattened once: the four top-level lists, the tool rows nested under LLM\n// calls, the retrieval steps nested under a retrieval, the masking/filter steps nested under any of\n// them, and the rows nothing could claim. The timing math used to run on the top-level lists ALONE,\n// which meant tool execution was not in the coverage at all — it fell into `gapMs` and was reported\n// as idle time.\ndef everySpan = []\neverySpan.addAll(top)\ntop.each { item ->\n    everySpan.addAll(safeTools(item))\n    everySpan.addAll(childList(item, 'steps'))\n    everySpan.addAll(childList(item, 'overheadSteps'))\n}\neverySpan.addAll(unattributed)\n\ndef dated = everySpan.findAll { it != null && it?.startTime != null }\ndef endOf = { s -> num(s?.startTime) + num(s?.llmTimeTaken) }\n\ndef startTime = dated ? dated.collect { num(it.startTime) }.min() : null\ndef endTime   = dated ? dated.collect { endOf(it) }.max() : null\n\ndef latencyMs =\n    (startTime != null && endTime != null)\n        ? (num(endTime) - num(startTime))\n        : 0L\n\n// retry classification, re-derived here rather than trusted\n// n_VYOw1 already decided this and stamped it. This recomputes the same verdict from the assembled\n// spans, which needs nothing but the frame id, startTime, llmTimeTaken, success and llmErrorType —\n// every one of which rides on the span — so a lost stamp anywhere upstream cannot bring the double\n// count back. The two paths are OR-ed.\n//\n// The signature of the duplicate writer (`_uMwW1`, see n_VYOw1): a success row that shares its\n// FRAME with a failed row and lasted at least as long. The frame's own attempt failed, so any\n// success it reports came from a descendant — it is a copy of a call that is already in this page.\ndef frameOf = { t -> ((X(t).executionInstanceId ?: t?.executionInstanceId) as String) + '@' + num(t?.startTime) }\ndef longestFailureInFrame = [:]\nagentExec.each { t ->\n    if (t?.success != false) return\n    def k = frameOf(t)\n    def d = num(t?.llmTimeTaken)\n    if (longestFailureInFrame[k] == null || d > longestFailureInFrame[k]) longestFailureInFrame[k] = d\n}\ndef derivedRollup = { t ->\n    if (t?.success == false || t?.llmErrorType == null) return false\n    def worst = longestFailureInFrame[frameOf(t)]\n    return worst != null && num(t?.llmTimeTaken) >= worst\n}\ndef isRetryRollup   = { t -> (t?.isRetryRollup == true) || (X(t).retryRollup == true) || derivedRollup(t) }\ndef isFailedAttempt = { t -> t?.success == false }\n\ndef rollupSpans    = agentExec.findAll { isRetryRollup(it) }\ndef failedAttempts = agentExec.findAll { !isRetryRollup(it) && isFailedAttempt(it) }\ndef billedLlm      = agentExec.findAll { !isRetryRollup(it) && !isFailedAttempt(it) }\nrollupSpans.each { it.isRetryRollup = true }\n\n// Every row that stands for a real round trip: the attempt that worked plus the attempts that did\n// not. Inside one chain these are sequential and non-overlapping — the failed frames tile the chain\n// end to end — so this is a plain sum with nothing to subtract. Roll-ups contribute nothing,\n// because their interval is a superset of rows already counted here.\ndef realLlmSpans = billedLlm + failedAttempts\ndef llmTimeMs = (realLlmSpans.sum { num(it?.llmTimeTaken) } ?: 0L) as long\n\n// A delegation wrapper (the InvokeAgent tool that stands in for a whole delegated run) already\n// contains the member agent's LLM and tool time. Adding it to toolTimeMs counted a team turn twice.\n// A tool row whose owning LLM call is not in this page has no owner to nest under, so it arrives\n// as an unattributed row. It is still a TOOL: counting it as overhead would report tool execution\n// as idle platform time and make `toolsCount` shrink as the page size shrinks. Split by what the\n// row says it is — the same rule that keeps masking time reading as masking.\ndef orphanTools = unattributed.findAll { (it?.currentType as String) == 'Tool' }\ndef orphanOther = unattributed.findAll { (it?.currentType as String) != 'Tool' }\n\ndef allToolRows = agentExec.collectMany { safeTools(it) } + orphanTools\n\n// delegation, re-derived here rather than trusted\n// A delegation wrapper is a tool whose interval CONTAINS the first call of another react loop: an\n// InvokeAgent standing in for a whole delegated run, or an ordinary callable that itself runs\n// agents (on the sampled case, `Create_Interface` — 1,501,189 ms covering ten threads — which no\n// name-based rule would ever catch). Its duration already contains that loop's LLM and tool time,\n// so adding it to toolTimeMs counts a team turn twice.\n//\n// n_VYOw1 already decided this and stamped it on the row. This recomputes the same verdict from\n// the assembled turns, which needs nothing but startTime, llmTimeTaken and the thread key — so a\n// lost stamp anywhere upstream cannot bring the double count back. The two paths are OR-ed and\n// `delegationSource` says which of them fired.\ndef threadFirstStart = [:]\ndef threadAgent = [:]\nagentExec.each { t ->\n    if (t?.startTime == null) return\n    def k = threadIdOf(t)\n    if (k == null) return\n    def st = num(t.startTime)\n    if (threadFirstStart[k] == null || st < threadFirstStart[k]) {\n        threadFirstStart[k] = st\n        threadAgent[k] = t?.aiAgentId as String\n    }\n}\ndef toolOwner = [:]\nagentExec.each { t ->\n    safeTools(t).each { tool ->\n        toolOwner[System.identityHashCode(tool)] = [thread: threadIdOf(t), agent: t?.aiAgentId as String]\n    }\n}\n// A tool the page cut away from its LLM call has no owner to read this from — but the ROW still\n// carries its own agent and its own react loop, which is all the rule needs. Without this an\n// orphaned delegation wrapper reads as ordinary tool time and its whole delegated run is counted\n// twice, which is exactly the double count the rule exists to prevent.\ndef ownerOf = { tool ->\n    toolOwner[System.identityHashCode(tool)] ?:\n        [thread: (tool?.rootExecutionId ?: tool?.threadId) as String, agent: tool?.aiAgentId as String]\n}\n\n// Same rule as n_VYOw1: the covered loop must belong to a DIFFERENT AGENT. Anything else is this\n// agent's own concurrency and its time is already counted under this agent.\ndef derivedDelegation = { tool ->\n    if (tool?.startTime == null) return false\n    def s0 = num(tool.startTime)\n    def e0 = s0 + num(tool?.llmTimeTaken)\n    if (e0 <= s0) return false\n    def own = ownerOf(tool)\n    return threadFirstStart.any { k, ts ->\n        k != own.thread && ts >= s0 && ts <= e0 &&\n        threadAgent[k] != null && own.agent != null && threadAgent[k] != own.agent\n    }\n}\n\ndef isDelegation = { tool -> (tool?.isDelegation == true) || derivedDelegation(tool) }\nallToolRows.each { tool -> if (isDelegation(tool)) tool.isDelegation = true }\n\ndef stampedDelegations = allToolRows.count { it?.isDelegation == true }\ndef realTools = allToolRows.findAll { it?.isDelegation != true }\ndef delegationTools = allToolRows.findAll { it?.isDelegation == true }\n\ndef toolTimeMs = (realTools.sum { num(it?.llmTimeTaken) } ?: 0L) as long\ndef delegationTimeMs = (delegationTools.sum { num(it?.llmTimeTaken) } ?: 0L) as long\n\ndef guardrailTimeMs = (\n    (inGuards + respGuard).sum {\n        num(it?.llmTimeTaken)\n    } ?: 0L\n) as long\n\ndef retrievalTimeMs = (\n    retrieval.sum {\n        num(it?.llmTimeTaken)\n    } ?: 0L\n) as long\n\ndef overheadSpans = top.collectMany { childList(it, 'overheadSteps') } + orphanOther\ndef overheadTimeMs = (overheadSpans.sum { num(it?.llmTimeTaken) } ?: 0L) as long\n\n// Wall-clock per step type. This is what makes \"where did the latency go\" answerable: masking time\n// reads as \"PII Mask\", a content filter reads as \"Content Filters\" — never as one anonymous bucket.\ndef timeByType = [:]\neverySpan.each { s ->\n    // A roll-up's milliseconds are a superset of rows that are already in this histogram, so it\n    // goes in no bucket at all — putting it anywhere would break the partition. It is not hidden:\n    // the row is still in the response, flagged, and retryRollupSpans counts it.\n    if (isRetryRollup(s)) return\n    // A delegation wrapper is bucketed separately, otherwise the histogram would disagree with\n    // toolTimeMs by exactly the delegated time it double-counts. Every millisecond that remains\n    // lands in exactly one bucket, and 'LLM' + 'LLM (failed attempt)' == llmTimeMs.\n    def type = (s?.currentType ?: 'Unknown') as String\n    def bucket = (s?.isDelegation == true) ? 'Delegation'\n               : (type == 'LLM' && isFailedAttempt(s)) ? 'LLM (failed attempt)'\n               : type\n    timeByType[bucket] = (timeByType[bucket] ?: 0L) + num(s?.llmTimeTaken)\n}\n\n// Build execution intervals and merge overlaps so nested calls\n// are not double-counted\n\ndef intervals = dated\n    .collect {\n        def start = num(it.startTime)\n        def end   = start + num(it.llmTimeTaken)\n        [\n            start: start,\n            end  : end\n        ]\n    }\n    .findAll { it.end > it.start }\n    .sort { a, b -> a.start <=> b.start }\n\ndef merged = []\nintervals.each { interval ->\n    if (!merged || interval.start > merged[-1].end) {\n        merged << [\n            start: interval.start,\n            end  : interval.end\n        ]\n    } \n    else {\n        def previousEnd = num(merged[-1].end)\n        def currentEnd  = num(interval.end)\n        merged[-1].end = currentEnd > previousEnd\n            ? currentEnd\n            : previousEnd\n    }\n}\n\ndef coveredMs = (merged.sum { interval -> num(interval.end) - num(interval.start) } ?: 0L) as long\ndef gapMs = latencyMs > coveredMs ? (latencyMs - coveredMs) : 0L\n\n// Summed over the rows that stand for a real call. Failed attempts carry cost 0 and 0 tokens, so\n// they change nothing here; roll-ups carry the FULL charge of the call they duplicate, so they\n// change everything — 67.60 of 295.04 on the sampled case. How much was suppressed is reported\n// rather than silently dropped, so the number is auditable against a raw SUM(cost) on e_traces.\ndef llmCost = (billedLlm.sum { money(it?.cost) } ?: 0G)\ndef retryRollupCostSuppressed = (rollupSpans.sum { money(it?.cost) } ?: 0G)\ndef toolCost = (allToolRows.sum { money(it?.toolInvocationCost) } ?: 0G)\ndef retrievalCost = (retrieval.sum { money(it?.retrievalCost) } ?: 0G)\n\n// Guardrail spans carry cost columns whenever the guard is model-backed; they were never summed.\ndef guardrailCost = (\n    (inGuards + respGuard).sum { money(it?.cost) } ?: 0G\n)\n\ndef overheadCost = (\n    overheadSpans.sum { money(it?.cost) } ?: 0G\n)\n\ndef totalCost =\n    money(llmCost) +\n    money(toolCost) +\n    money(retrievalCost) +\n    money(guardrailCost) +\n    money(overheadCost)\n\n// Counts stay over EVERY tool row, delegation wrappers included — it is only their TIME that\n// must not be added to toolTimeMs.\ndef toolNames = allToolRows\n    .collect { it?.toolName }\n    .findAll { it != null && it.toString().trim() }\n\ndef toolsCount = toolNames.size()\n\ndef distinctToolNames = toolNames\n    .collect { it.toString() }\n    .unique()\n\n// actual failures which occured\n// Failure looks at EVERY row, tools included — a failed tool call used to leave the turn reading\n// SUCCESS. But an attempt that a LATER attempt of the same logical call recovered from is not a\n// turn failure: the call succeeded, it just cost extra round trips. On the sampled case 131 of the\n// 147 failed attempts were recovered, and counting them made the message read FAILED.\n//\n// The logical call is the `parentExecutionId` chain (see n_VYOw1). A call where every attempt\n// failed is a real failure and stays one.\ndef callIdOf = { t -> (X(t).callId ?: frameOf(t)) as String }\ndef answeredCalls = billedLlm.collect { callIdOf(it) } as Set\ndef unrecovered = failedAttempts.findAll { !answeredCalls.contains(callIdOf(it)) }\n\ndef retryTimeMs      = (failedAttempts.sum { num(it?.llmTimeTaken) } ?: 0L) as long\ndef retriedLlmCalls  = failedAttempts.findAll { answeredCalls.contains(callIdOf(it)) }\n                                     .collect { callIdOf(it) }.unique().size()\ndef failedLlmCalls   = unrecovered.collect { callIdOf(it) }.unique().size()\n\ndef failures = everySpan.findAll {\n    if (isRetryRollup(it)) return false\n    if (it?.success != false) return false\n    // Only an LLM row can be recovered by a retry; anything else that says success=false failed.\n    if (((it?.currentType ?: '') as String) != 'LLM') return true\n    return !answeredCalls.contains(callIdOf(it))\n}\ndef status = failures ? 'FAILED' : 'SUCCESS'\n\ndef reasonOf = { s ->\n    if (s?.failedReason != null) return s.failedReason as String\n    if (s?.guardrailViolationReason != null) return s.guardrailViolationReason as String\n    if (s?.llmErrorType != null) return s.llmErrorType as String\n    def t = (s?.currentType ?: 'step') as String\n    def nm = s?.toolName ? (' ' + s.toolName) : ''\n    return t + nm + ' reported success=false'\n}\ndef failedReasons = failures.collect { reasonOf(it) }.findAll { it != null }.unique()\n\n// club the LLM calls by agent\n// agentExecution was a flat, wall-clock-ordered list, so a team turn read\n// \"trace A -> agent 1, trace B -> agent 2, trace C -> agent 2, trace D -> agent 1\".\n// Grouping by aiAgentId puts every call an agent made in one place. Each turn keeps its threadId,\n// so the agent invoked several times CONCURRENTLY (one sampled turn had the same agent id running\n// 8 parallel loops) is still separable inside its group — threadCount says how many there were.\ndef agentOrder = []\ndef byAgent = [:]\nagentExec.each { t ->\n    def a = (t?.aiAgentId ?: '__unknown') as String\n    if (!byAgent.containsKey(a)) { byAgent[a] = []; agentOrder << a }\n    byAgent[a] << t\n}\n\n// Every tool that handed work to another agent, with the window it was open for.\ndef delegations = []\nagentExec.each { t ->\n    safeTools(t).each { tool ->\n        if (tool?.isDelegation != true) return\n        def st = num(tool?.startTime)\n        delegations << [start    : st,\n                        end      : st + num(tool?.llmTimeTaken),\n                        agentId  : t?.aiAgentId,\n                        toolName : tool?.toolName,\n                        target   : tool?.targetAgentName]\n    }\n}\n\ndef firstStartOf = { List turns ->\n    def st = turns.findAll { it?.startTime != null }.collect { num(it.startTime) }\n    st ? st.min() : 0L\n}\n\ndef claimedWrappers = [] as Set\n// Turns that were written to a file do not also ride inline — that is the whole point of the file.\n// What stays is a COMPACT row per turn: every number the ledger arithmetic runs on (cost, tokens,\n// timings, finishReason, the tool names) minus the four heavy fields, so no reader has to fetch a\n// file to answer a question about cost or latency. The heavy fields live in the chunk.\ndef filesFor = { String agentId ->\n    agentExecFiles.findAll { (it?.agentId as String) == agentId }\n                  .sort { num(it?.chunkIndex) }\n                  .collect { f ->\n                      def rec = [chunkIndex: f?.chunkIndex, startIndex: f?.startIndex,\n                                 turnCount : f?.turnCount, file: f?.file]\n                      // `startIndex` is a position inside THIS page's list for that agent, so two\n                      // pages of one message both produce a \"chunk 0, startIndex 0\" file. The page\n                      // is what tells them apart, and it is stamped here — the node that holds\n                      // `page` — rather than threaded through four nodes to get here.\n                      if (windowed) rec.pageOffset = pageOffset\n                      rec\n                  }\n}\n\ndef agentWiseTurns = agentOrder\n    .collect { a -> [agentId: a, turns: byAgent[a].sort { num(it?.startTime) }] }\n    .sort { firstStartOf(it.turns) }\n    .collect { rec ->\n        def turns = rec.turns\n        def firstAt = firstStartOf(turns)\n        // The caller is the delegation tool whose window contains this agent's first call and is\n        // the TIGHTEST such window — the immediate caller, not its grandparent. A wrapper high in\n        // the chain contains every descendant, so \"smallest containing interval\" picks the real\n        // edge out.\n        //\n        // A manager firing several InvokeAgent calls in the SAME millisecond makes every wrapper\n        // contain every child, so an unclaimed wrapper wins over an already-claimed one — that is\n        // what stops two delegated agents both being named after the first wrapper's target. When\n        // every containing wrapper is already claimed the tightest one is reused rather than\n        // falling outward to a looser wrapper, because one tool really can spawn several loops.\n        def containing = delegations.findAll {\n            it.agentId != rec.agentId && it.start <= firstAt && it.end >= firstAt\n        }\n        def free = containing.findAll { !claimedWrappers.contains(it) }\n        def pool = free ?: containing\n        def caller = pool ? pool.min { it.end - it.start } : null\n        if (caller != null) claimedWrappers << caller\n        def tools = turns.collectMany { safeTools(it) }\n        [\n            agentId         : rec.agentId,\n            agentName       : caller?.target,\n            calledByAgentId : caller?.agentId,\n            calledByToolName: caller?.toolName,\n            // Same rules as the turn-level totals, so the per-agent numbers add up to them.\n            llmCallCount    : turns.count { !isRetryRollup(it) && !isFailedAttempt(it) },\n            failedAttempts  : turns.count { !isRetryRollup(it) && isFailedAttempt(it) } ?: null,\n            retryRollupSpans: turns.count { isRetryRollup(it) } ?: null,\n            threadCount     : turns.collect { threadIdOf(it) }.findAll { it != null }.unique().size(),\n            toolCount       : tools.size(),\n            delegationCount : tools.count { it?.isDelegation == true } ?: null,\n            cost            : (turns.findAll { !isRetryRollup(it) }.sum { money(it?.cost) } ?: 0G),\n            inputToken      : (turns.findAll { !isRetryRollup(it) }.sum { num(it?.inputToken) } ?: 0L) as long,\n            outputToken     : (turns.findAll { !isRetryRollup(it) }.sum { num(it?.outputToken) } ?: 0L) as long,\n            llmTimeMs       : (turns.findAll { !isRetryRollup(it) }.sum { num(it?.llmTimeTaken) } ?: 0L) as long,\n            toolTimeMs      : (tools.findAll { it?.isDelegation != true }.sum { num(it?.llmTimeTaken) } ?: 0L) as long,\n            startTime       : firstAt,\n        ]\n    }\n    .collect { rec ->\n        // Per-agent break at CHUNK_SIZE (see n_chkGt). A filed agent's `turns` IS the file list —\n        // 30 turns per file, ceil(n/30) files — and the agent record keeps every aggregate, so the\n        // per-agent arithmetic still needs nothing fetched. An agent under the limit is untouched.\n        def files = filesFor(rec.agentId)\n        rec.turns = files ?: byAgent[rec.agentId].sort { num(it?.startTime) }\n        rec.turnsInline = !files\n        rec\n    }\n\n// building the hierarchical tree\n// A compact text rendering of who called whom and what each agent ran. Built from the assembled\n// turns — the delegation edges are already resolved and the InvokeAgent target name already rides\n// on the tool span, so this needs no extra query.\ndef aliasOf = [:]\nagentWiseTurns.eachWithIndex { rec, i -> aliasOf[rec.agentId] = '#a' + (i + 1) }\n\ndef childrenOf = [:]\ndef roots = []\nagentWiseTurns.each { rec ->\n    if (rec.calledByAgentId != null && aliasOf.containsKey(rec.calledByAgentId)) {\n        childrenOf.get(rec.calledByAgentId, []) << rec\n    } else {\n        roots << rec\n    }\n}\n\ndef shortId = { String id -> (id != null && id.length() > 10) ? (id.substring(0, 10) + '…') : id }\ndef lines = []\ndef render\nrender = { rec, int depth, Set guard ->\n    if (guard.contains(rec.agentId)) return\n    guard << rec.agentId\n    def bar = depth == 0 ? '' : ('|' * depth + ' ')\n    def name = rec.agentName ?: shortId(rec.agentId)\n    def from = rec.calledByAgentId != null ? (' ← ' + (aliasOf[rec.calledByAgentId] ?: shortId(rec.calledByAgentId))) : ''\n    lines << (bar + '[' + name + ' ' + aliasOf[rec.agentId] + from + ']')\n\n    def childBar = '|' * (depth + 1) + ' '\n    def emitted = [] as Set\n    def toolSeq = (byAgent[rec.agentId] ?: []).sort { num(it?.startTime) }.collectMany { safeTools(it) }\n    def i = 0\n    while (i < toolSeq.size()) {\n        def tool = toolSeq[i]\n        def nm = (tool?.toolName ?: 'tool') as String\n        // Collapse a run of the same tool: eight TodoWriteTool lines say nothing eight times.\n        def j = i\n        while (j + 1 < toolSeq.size() && ((toolSeq[j + 1]?.toolName ?: 'tool') as String) == nm) j++\n        def count = j - i + 1\n        if (tool?.isDelegation == true) {\n            def target = tool?.targetAgentName\n            def child = (childrenOf[rec.agentId] ?: []).find {\n                (target != null && it.agentName == target) || !emitted.contains(it.agentId)\n            }\n            def label = child != null ? ((child.agentName ?: shortId(child.agentId)) + ' (' + aliasOf[child.agentId] + ')')\n                      : (target ?: '?')\n            lines << (childBar + nm + ' -> ' + label + ' ' + aliasOf[rec.agentId])\n            if (child != null && !emitted.contains(child.agentId)) {\n                emitted << child.agentId\n                render(child, depth + 1, guard)\n            }\n        } else {\n            lines << (childBar + nm + (count > 1 ? (' x' + count + ' (sequential)') : '') + ' ' + aliasOf[rec.agentId])\n        }\n        i = j + 1\n    }\n    // Children reached by a tool this loop never recorded as a delegation.\n    (childrenOf[rec.agentId] ?: []).each {\n        if (!emitted.contains(it.agentId)) { emitted << it.agentId; render(it, depth + 1, guard) }\n    }\n}\ndef guard = [] as Set\nroots.each { render(it, 0, guard) }\nagentWiseTurns.each { if (!guard.contains(it.agentId)) render(it, 0, guard) }\ndef tree = lines.join('\\n')\n\ndef turn = [\n    messageId         : messageId,\n    status            : status,\n    startTime         : startTime,\n    endTime           : endTime,\n    latencyMs         : latencyMs,\n    llmTimeMs         : llmTimeMs,\n    toolTimeMs        : toolTimeMs,\n    guardrailTimeMs   : guardrailTimeMs,\n    retrievalTimeMs   : retrievalTimeMs,\n    overheadTimeMs    : overheadTimeMs,\n    gapMs             : gapMs,\n    coveredMs         : coveredMs,\n    timeByType        : timeByType,\n    cost              : totalCost,\n    toolsCount        : toolsCount,\n    distinctToolNames : distinctToolNames,\n    // Calls that produced a response — one row each, and the only rows that carry a charge.\n    llmCalls          : billedLlm.size(),\n    // The raw row count, so nothing is hidden. The three kinds partition it exactly:\n    //   llmSpans == llmCalls + llmFailedAttempts + retryRollupSpans\n    llmSpans          : agentExec.size(),\n    llmFailedAttempts : failedAttempts.size(),\n    agentCount        : agentWiseTurns.size(),\n    tree              : tree,\n    inputGuardrails   : inGuards,\n    agentWiseTurns    : agentWiseTurns,\n    responseGuardrail : respGuard,\n    knowledgeRetrieval: retrieval\n]\n\n// Keys that only exist when they have something to say.\nif (failedReasons) turn.failedReasons = failedReasons\nif (failedAttempts) {\n    turn.retriedLlmCalls = retriedLlmCalls\n    turn.retryTimeMs = retryTimeMs\n    // Said out loud, because it is the opposite of what a reader assumes: the failed attempts of a\n    // chain are already part of llmTimeMs — they are the round trips that had to be redone — so\n    // this is a SHARE of it, never something to add to it.\n    turn.retryTimeMsIsWithin = 'llmTimeMs'\n    def recovered = agentExec.collect { it?.recoveredFrom }.findAll { it != null }.unique()\n    if (recovered) turn.recoveredFrom = recovered\n}\nif (failedLlmCalls > 0) turn.failedLlmCalls = failedLlmCalls\n// Only ever non-zero on traces written before the duplicate Capture Trace node was removed\n// (2026-08-16). Reported with the money it took out, so the total can be reconciled against a raw\n// SUM(cost) over e_traces without anyone having to guess where the difference came from.\nif (rollupSpans) {\n    turn.retryRollupSpans = rollupSpans.size()\n    turn.retryRollupCostSuppressed = retryRollupCostSuppressed\n    turn.retryRollupNote = 'duplicate rows written by the recursive-retry path: each repeats the ' +\n        'cost and tokens of the attempt that succeeded. Excluded from every total above.'\n}\nif (delegationTimeMs > 0) turn.delegationTimeMs = delegationTimeMs\n\n// if (unattributed) turn.unattributedSteps = unattributed\n\n// if (grouping != null) turn.grouping = grouping\n\n// One block or the other, never both: inline turns while the turn is small enough to read, the\n// same structure with each agent's turns replaced by its files once anything crossed the limit.\n// if (agentExecFiles) {\n//     turn.agentExecutionTurnFiles = agentWiseTurns\n// } else {\n//     turn.agentWiseTurns = agentWiseTurns\n// }\n\n// page specifics\n// Rides on every response, so a caller never has to remember what it asked for, and `hasMore` /\n// `nextOffset` are the whole loop: keep calling with nextOffset until hasMore is false.\nif (page instanceof Map) {\n    turn.page = page\n    if (windowed) {\n        // The totals above are this window's. Named plainly rather than renamed, so the shape a\n        // reader already knows does not change between a paged and an unpaged call — only this\n        // flag appears, and it appears exactly when the numbers are partial.\n        turn.partial = true\n        turn.scope = 'page'\n        turn.partialNote = 'covers spans ' + (page?.firstSpanIndex ?: 0) + '..' +\n            (page?.lastSpanIndex ?: 0) + ' of this message'\n        // NOT every total combines the same way, and getting this wrong is silent. The counters and\n        // the per-span time sums add. The WALL-CLOCK fields do not: page windows overlap, because a\n        // call that starts before the cut can still be running after it — on a real 546-span case\n        // the two pages' latencyMs summed to 225580 ms MORE than the turn actually lasted.\n        turn.pageMath = [\n            // Every one of these is decided per ROW, so a row lands on exactly one page and the\n            // sums are exact however the message was cut.\n            additive   : ['cost', 'costBreakdown', 'llmCalls', 'llmSpans', 'llmFailedAttempts',\n                          'retryRollupSpans', 'retryRollupCostSuppressed', 'retryTimeMs',\n                          'toolsCount', 'llmTimeMs', 'toolTimeMs', 'delegationTimeMs',\n                          'guardrailTimeMs', 'retrievalTimeMs', 'overheadTimeMs', 'timeByType'],\n            // Properties of a retry CHAIN, not of a row. A chain the cut ran through is seen by\n            // both pages, so summing these across pages can over-count by at most the number of\n            // chains at the seam — de-duplicate on the chain if an exact figure is needed.\n            perChain   : ['retriedLlmCalls', 'failedLlmCalls'],\n            notAdditive: ['startTime', 'endTime', 'latencyMs', 'coveredMs', 'gapMs'],\n            note       : 'take min(startTime) and max(endTime) across the pages and recompute ' +\n                         'latency from those; summing latencyMs double-counts the overlap'\n        ]\n        // A page that still has rows after it cannot conclude the message succeeded — it can only\n        // report that nothing in THIS window failed.\n        if (page?.hasMore == true && status == 'SUCCESS') turn.statusProvisional = true\n        // Tool rows the cut separated from their LLM call. They are counted and timed as tools,\n        // they just have no turn to nest under on this page.\n        if (orphanTools) turn.toolsOrphanedByPageEdge = orphanTools.size()\n    } else {\n        turn.scope = 'turn'\n    }\n}\n\n// strip the internal carrier\n// `_x` did its job (thread keying, retry pairing, the diff baseline). It is plumbing, not output,\n// and this is the one place the inline turns leave the automation — the filed ones are stripped in\n// n_jrFAb. Done last so nothing above can be surprised by a missing key.\ndef scrub\nscrub = { v ->\n    if (v instanceof Map) {\n        def out = [:]\n        v.each { k, val -> if (k != '_x') out[k] = scrub(val) }\n        return out\n    }\n    if (v instanceof Collection) return v.collect { scrub(it) }\n    return v\n}\nturn.inputGuardrails    = scrub(turn.inputGuardrails)\nturn.responseGuardrail  = scrub(turn.responseGuardrail)\nturn.knowledgeRetrieval = scrub(turn.knowledgeRetrieval)\nturn.agentWiseTurns     = scrub(turn.agentWiseTurns)\n\ndef costBreakdown = [llm: money(llmCost), tool: money(toolCost), retrieval: money(retrievalCost)]\nif (money(guardrailCost) != 0G) costBreakdown.guardrail = money(guardrailCost)\nif (money(overheadCost) != 0G) costBreakdown.overhead = money(overheadCost)\nturn.costBreakdown = costBreakdown\n\nreturn [\n    turn: turn\n]",
				"compile_static":false,
				"captureStdOutput":false,
				"parameters":{
					"agentExecution":{
						"items":"{{ n_eE59d.outputs.items[0] }}",
						"source":"{{ n_eE59d.outputs.items }}",
						"ua:type":"mappedArray"
					},
					"agentExecutionFiles":{
						"items":"{{ n_QOhaa.outputs.items[0] }}",
						"source":"{{ n_QOhaa.outputs.items }}",
						"ua:type":"mappedArray"
					},
					"grouping":"{{ n_VYOw1.outputs.result.grouping }}",
					"inputGuardrails":{
						"items":"{{ n_Cegc9.outputs.items[0] }}",
						"source":"{{ n_Cegc9.outputs.items }}",
						"ua:type":"mappedArray"
					},
					"knowledgeRetrieval":{
						"items":"{{ n_FPOkF.outputs.items[0] }}",
						"source":"{{ n_FPOkF.outputs.items }}",
						"ua:type":"mappedArray"
					},
					"messageId":"{{ n_v4sZv.outputs.message_id }}",
					"page":"{{ n_APnyI.outputs.result.page }}",
					"responseGuardrail":{
						"items":"{{ n_Be9QU.outputs.items[0] }}",
						"source":"{{ n_Be9QU.outputs.items }}",
						"ua:type":"mappedArray"
					},
					"unattributedSteps":{
						"items":"{{ n_VYOw1.outputs.result.unattributedSteps[0] }}",
						"source":"{{ n_VYOw1.outputs.result.unattributedSteps }}",
						"ua:type":"mappedArray"
					}
				}
			},
			"skip":false,
			"subTitle":"Code by UnifyApps",
			"title":"Execute Groovy code",
			"type":"ACTION"
		},
		{
			"context":{
				"appName":"callables",
				"resourceVersion":2757,
				"resourceName":"callables_return_to_automation",
				"type":"APPLICATION"
			},
			"debug":false,
			"dirty":false,
			"fallbackMode":"STOP",
			"groupId":"_JAI60-1",
			"id":"n_Xq7FL",
			"index":61,
			"inputs":{
				"result":{
					"result":"{{ n_WB2iD.outputs.result.turn }}"
				}
			},
			"skip":false,
			"subTitle":"Callable",
			"title":"Respond to automation",
			"type":"STOP"
		}
	],
	"ownerUserId":-1,
	"paused":false,
	"schemaReferences":[
		
	],
	"settings":{
		"enableNodeLevelLogging":true,
		"enableRunLogging":true,
		"enableVariableLogging":true,
		"route":{
			"default":false,
			"tierName":"global"
		}
	},
	"standard":true,
	"version":0,
	"workflowDefinitionId":"6a946cff970f74696f4b57ef",
	"workflowVersion":15
}