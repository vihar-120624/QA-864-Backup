{
	"changesetType":"IMPORT",
	"createdTime":1789395119852,
	"deleted":false,
	"deployedWorkflowVersion":1,
	"deploymentSource":{
		"changesetRequestId":"6aa7fffcb91cc6613f66322e",
		"changesetType":"IMPORT"
	},
	"edges":[
		{
			"fromNodeId":"_T2QbF",
			"id":"next@_T2QbF@n_DwcVq",
			"priority":0,
			"skip":false,
			"toNodeId":"n_DwcVq",
			"type":"next"
		},
		{
			"fromNodeId":"n_DwcVq",
			"id":"next@n_DwcVq@n_4HCLf",
			"name":"success",
			"priority":0,
			"skip":false,
			"toNodeId":"n_4HCLf",
			"type":"next"
		},
		{
			"fromNodeId":"n_DwcVq",
			"id":"error@n_DwcVq@n_d65Ac",
			"name":"error",
			"priority":0,
			"skip":false,
			"toNodeId":"n_d65Ac",
			"type":"error"
		},
		{
			"fromNodeId":"n_4HCLf",
			"id":"next@n_4HCLf@n_w9jHH",
			"priority":0,
			"skip":false,
			"toNodeId":"n_w9jHH",
			"type":"next"
		},
		{
			"fromNodeId":"n_d65Ac",
			"id":"next@n_d65Ac@n_w9jHH",
			"priority":0,
			"skip":false,
			"toNodeId":"n_w9jHH",
			"type":"next"
		},
		{
			"fromNodeId":"n_w9jHH",
			"id":"next@n_w9jHH@_v6dAe",
			"priority":0,
			"skip":false,
			"toNodeId":"_v6dAe",
			"type":"next"
		}
	],
	"id":"6aa800afc6a14707f1503260",
	"lastModifiedBy":-1,
	"lcName":"readdocument | getsections",
	"modifiedTime":1789395119854,
	"name":"readDocument | getSections",
	"nodes":[
		{
			"context":{
				"appName":"callables",
				"resourceName":"callables_from_automation"
			},
			"debug":false,
			"dirty":false,
			"fallbackMode":"STOP",
			"groupId":"n_IACqJ@_ONz84-1@y",
			"id":"_T2QbF",
			"index":1,
			"inputs":{
				"result":{
					"additionalProperties":false,
					"properties":{
						"toolOutput":{
							"title":"Tool Output",
							"type":"string"
						}
					},
					"required":[],
					"type":"object"
				},
				"setup":{
					"additionalProperties":false,
					"properties":{
						"files":{
							"items":{
								"additionalProperties":false,
								"properties":{
									"name":{
										"title":"Name",
										"type":"string"
									},
									"obj":{
										"additionalProperties":false,
										"properties":{},
										"title":"Obj",
										"type":"object"
									}
								},
								"required":[],
								"type":"object"
							},
							"title":"Files",
							"type":"array"
						}
					},
					"required":[],
					"type":"object"
				}
			},
			"skip":false,
			"subTitle":"Callable",
			"title":"Trigger via automation",
			"trigger":{
				"type":"CALLABLE"
			},
			"type":"START"
		},
		{
			"context":{
				"appName":"utility_by_unifyapps",
				"resourceVersion":10260,
				"resourceName":"utility_by_unifyapps_download_file_as_string",
				"type":"APPLICATION"
			},
			"debug":false,
			"dirty":false,
			"fallbackMode":"MANUAL",
			"groupId":"n_IACqJ@_ONz84-1@y",
			"id":"n_DwcVq",
			"index":2,
			"inputs":{
				"file":"{{ _T2QbF.outputs.files[0].obj }}",
				"encoding":"utf8"
			},
			"options":{
				"bulkheadConfig":{
					"enabled":false,
					"maxLeaseTimeUnit":"SECONDS",
					"maxWaitDurationUnit":"SECONDS"
				},
				"businessHoursConfig":{
					"enabled":false,
					"waitForNextWindow":false
				},
				"cacheConfig":{
					"enabled":false,
					"usagePolicy":"ALWAYS",
					"useInMemoryCache":false
				},
				"circuitBreakerConfig":{
					"enabled":false
				},
				"disableLogging":false,
				"enabledForReExecution":false,
				"hookConfig":{
					"enabled":false,
					"postHooks":[
						
					],
					"preHooks":[
						
					]
				},
				"logConfig":{
					"enabled":false,
					"logPolicies":[
						
					]
				},
				"rateLimitConfig":{
					"enabled":false,
					"waitForNextWindow":false
				},
				"retryConfig":{
					"backOffFactor":null,
					"captureRetries":false,
					"enabled":false
				},
				"stepError":"MANUAL",
				"telemetryConfig":{
					"enableTelemetry":false,
					"metricConfigs":[
						
					]
				},
				"timeoutConfig":{
					"enabled":false
				}
			},
			"skip":false,
			"subTitle":"Utility by UnifyApps",
			"title":"Download file as string",
			"type":"ACTION"
		},
		{
			"context":{
				"appName":"code_by_unifyapps",
				"resourceVersion":3791,
				"resourceName":"code_by_unifyapps_groovy",
				"type":"APPLICATION"
			},
			"debug":false,
			"dirty":false,
			"fallbackMode":"STOP",
			"groupId":"n_DwcVq@n_IACqJ@_ONz84-1@y@success",
			"id":"n_4HCLf",
			"index":3,
			"inputs":{
				"output":{
					"additionalProperties":false,
					"properties":{
						"num_sections":{
							"title":"Num Sections",
							"type":"integer"
						},
						"sections":{
							"items":{
								"additionalProperties":false,
								"properties":{},
								"type":"object"
							},
							"title":"Sections",
							"type":"array"
						},
						"total_chars":{
							"title":"Total Chars",
							"type":"integer"
						}
					},
					"required":[],
					"type":"object"
				},
				"input":{
					"additionalProperties":false,
					"properties":{
						"content":{
							"title":"Content",
							"type":"string"
						}
					},
					"required":[],
					"type":"object"
				},
				"code":"def originalMd = binding.hasVariable(\"content\") && binding.getVariable(\"content\") != null\n        ? binding.getVariable(\"content\").toString()\n        : \"\"\n\nfinal int BUDGET_CHARS = 9200   // caller truncates the flattened result at 10000\nfinal int GAP          = 4000   // slice size when the document has no headings\n\n// Keep original content for heading detection\ndef lines = originalMd.split(\"\\n\", -1)\n\n// This MUST use exactly the same cleaning as the reader Groovy\ndef md = originalMd\n        .replaceAll(/[\\r\\n\\t]+/, \"\")\n        .replaceAll(/ {2,}/, \"\")\n        .trim()\n\ndef sections = []\ndef headingPattern = ~/^(#{1,6})\\s+(.*)$/\n\n// ---------------------------------------------------------\n// Find headings in the original content\n// ---------------------------------------------------------\ndef headings = []\ndef originalPos = 0\n\nlines.each { line ->\n    def trimmed = line.trim()\n    def matcher = headingPattern.matcher(trimmed)\n\n    if (matcher.matches() && matcher.group(2).trim()) {\n        headings.add([\n                \"original_pos\": originalPos,\n                \"section\"     : matcher.group(2).trim(),\n                \"level\"       : matcher.group(1).length()\n        ])\n    }\n\n    originalPos += line.length()\n\n    if (originalPos < originalMd.length()) {\n        originalPos += 1\n    }\n}\n\n// ---------------------------------------------------------\n// Convert original positions to cleaned-content positions\n// using the EXACT same cleaning rules as the reader.\n// ---------------------------------------------------------\ndef getCleanedPosition = { int targetPos ->\n\n    if (targetPos <= 0) {\n        return 0\n    }\n\n    String prefix = originalMd.substring(\n            0,\n            Math.min(targetPos, originalMd.length())\n    )\n\n    prefix = prefix\n            .replaceAll(/[\\r\\n\\t]+/, \"\")\n            .replaceAll(/ {2,}/, \"\")\n            .trim()\n\n    return prefix.length()\n}\n\n// ---------------------------------------------------------\n// Rank each heading by how deeply it nests.\n//\n// This document writes \"### 1.1\", \"### 1.1.1\" and \"### 1.3.2.1\" at the same\n// markdown level, so markdown level alone cannot tell a subsection from a\n// fourth-level clause. Where a heading starts with a dotted section number,\n// that number is the real depth; otherwise fall back to the markdown level.\n// Used only for ranking, never emitted.\n// ---------------------------------------------------------\ndef numberPattern = ~/^(\\d+(?:\\.\\d+)*)\\.?(?:\\s|$).*/\n\ndef numericDepth = { String title ->\n    def m = numberPattern.matcher(title)\n    return m.matches() ? m.group(1).split(/\\./).length : 0\n}\n\nint minNumeric = 0\nint baseLevel = 0\nheadings.each { h ->\n    int d = numericDepth(h.section)\n    if (d > 0) {\n        if (minNumeric == 0 || d < minNumeric) { minNumeric = d; baseLevel = h.level }\n        else if (d == minNumeric && h.level < baseLevel) { baseLevel = h.level }\n    }\n}\nheadings.each { h ->\n    int d = numericDepth(h.section)\n    h.depth = (d > 0 && minNumeric > 0) ? baseLevel + (d - minNumeric) : h.level\n}\n\n// ---------------------------------------------------------\n// Choose how deep the table of contents can afford to go.\n//\n// Emitting every heading overflows the caller's fixed-size truncation, and\n// that cut is positional, so the tail of the document is dropped outright.\n// Instead drop whole depth TIERS from the bottom up until the serialized\n// list fits. Dropped headings are not lost from the text: each surviving\n// section's range is widened to the next SURVIVING heading, so it still\n// spans its dropped children.\n// ---------------------------------------------------------\ndef buildFrom = { List kept, int groupSize ->\n    def out = []\n    for (int g = 0; g < kept.size(); g += groupSize) {\n        def group = kept.subList(g, Math.min(g + groupSize, kept.size()))\n        int startPos = getCleanedPosition(group[0].original_pos)\n        int endPos = (g + groupSize < kept.size())\n                ? getCleanedPosition(kept[g + groupSize].original_pos)\n                : md.length()\n        if (endPos < startPos) endPos = startPos\n        def label = group.size() > 1 ? (group[0].section + \" ... \" + group[-1].section) : group[0].section\n        out << [\"section\": label, \"level\": group[0].level,\n                \"start_pos\": startPos, \"end_pos\": endPos, \"char_len\": endPos - startPos]\n    }\n    return out\n}\n\ndef serializedSize = { List secs ->\n    return ([\"num_sections\": secs.size(), \"total_chars\": md.length(),\n             \"sections\": secs.withIndex().collect { s, i ->\n                 [\"id\": i, \"section\": s[\"section\"], \"level\": s[\"level\"],\n                  \"start_pos\": s[\"start_pos\"], \"end_pos\": s[\"end_pos\"], \"char_len\": s[\"char_len\"]]\n             }]).toString().length()\n}\n\nif (!headings.isEmpty()) {\n\n    int shallowest = headings.collect { it.depth }.min()\n    def chosen = null\n\n    for (int d = shallowest; d <= 6; d++) {\n        def candidate = buildFrom(headings.findAll { it.depth <= d }, 1)\n        if (candidate.isEmpty()) continue\n        if (serializedSize(candidate) > BUDGET_CHARS) break\n        chosen = candidate\n    }\n\n    if (chosen == null) {\n        // Even the shallowest tier alone overflows. Merge neighbours so the\n        // ranges still tile the whole document rather than losing the tail.\n        def kept = headings.findAll { it.depth <= shallowest }\n        int groupSize = 1\n        while (true) {\n            chosen = buildFrom(kept, groupSize)\n            if (serializedSize(chosen) <= BUDGET_CHARS || groupSize >= kept.size()) break\n            groupSize++\n        }\n    }\n\n    sections = chosen\n\n} else {\n\n    // -----------------------------------------------------\n    // No headings -> split CLEANED document into 4000 chars\n    // -----------------------------------------------------\n    def total = md.length()\n    int i = 0\n\n    for (int start = 0; start < total; start += GAP) {\n\n        int end = Math.min(start + GAP, total)\n\n        def text = md.substring(start, end)\n\n        def words = text.split(/\\s+/)\n                .findAll { it }\n                .take(40)\n\n        def snippet = words.join(\" \")\n\n        sections.add([\n                \"section\"  : snippet ?: \"Part ${i + 1}\",\n                \"level\"    : 1,\n                \"start_pos\": start,\n                \"end_pos\"  : end,\n                \"char_len\" : end - start\n        ])\n\n        i++\n    }\n}\n\nreturn [\n        \"num_sections\": sections.size(),\n        \"total_chars\" : md.length(),\n        \"sections\"    : sections.withIndex().collect { s, i ->\n            [\n                    \"id\"        : i,\n                    \"section\"   : s[\"section\"],\n                    \"level\"     : s[\"level\"],\n                    \"start_pos\" : s[\"start_pos\"],\n                    \"end_pos\"   : s[\"end_pos\"],\n                    \"char_len\"  : s[\"char_len\"]\n            ]\n        }\n]",
				"compile_static":false,
				"captureStdOutput":false,
				"parameters":{
					"content":"{{ n_DwcVq.outputs.fileContent }}"
				}
			},
			"skip":false,
			"subTitle":"Code by UnifyApps",
			"title":"Execute Groovy code",
			"type":"ACTION"
		},
		{
			"context":{
				"appName":"code_by_unifyapps",
				"resourceVersion":3624,
				"resourceName":"code_by_unifyapps_python",
				"type":"APPLICATION"
			},
			"debug":false,
			"dirty":false,
			"fallbackMode":"STOP",
			"groupId":"n_DwcVq@n_IACqJ@_ONz84-1@y@error",
			"id":"n_d65Ac",
			"index":4,
			"inputs":{
				"output":{
					"additionalProperties":false,
					"properties":{
						"num_sections":{
							"title":"Num Sections",
							"type":"integer"
						},
						"sections":{
							"items":{
								"additionalProperties":false,
								"properties":{},
								"type":"object"
							},
							"title":"Sections",
							"type":"array"
						},
						"totalChars":{
							"title":"Total Chars",
							"type":"integer"
						}
					},
					"required":[],
					"type":"object"
				},
				"configurationMode":"DEFAULT",
				"code":"import re\n\nwith open(input_file, \"r\", encoding=\"utf-8\") as f:\n    md = f.read()\n\nHEADING_PAT = r'^(#{1,6})\\s+(.*)$'\nlines = md.splitlines(keepends=True)\n\nsections = []\ncur = None\npos = 0\n\ndef flush(sec, end_pos):\n    if sec is not None:\n        sec[\"end_pos\"] = end_pos\n        sec[\"char_len\"] = end_pos - sec[\"start_pos\"]\n        sections.append(sec)\n\nfor ln in lines:\n    m = re.match(HEADING_PAT, ln.strip())\n    if m and m.group(2).strip():\n        flush(cur, pos)                       # previous section ends here\n        cur = {\"section\": m.group(2).strip(), \"level\": len(m.group(1)),\n               \"start_pos\": pos}\n    else:\n        if cur is None:\n            cur = {\"section\": \"(preamble)\", \"level\": 0, \"start_pos\": 0}\n    pos += len(ln)\nflush(cur, pos)                               # last section ends at EOF\n\n# fallback: no real headings -> slice the doc into fixed gaps\nreal = [s for s in sections if s[\"section\"] != \"(preamble)\"]\nif not real:\n    GAP = 4000                                # chars per slice\n    total = len(md)\n    sections = []\n    for i, start in enumerate(range(0, total, GAP)):\n        end = min(start + GAP, total)\n        snippet = \" \".join(md[start:end].split()[:40])   # first 40 words as label\n        sections.append({\n            \"section\": snippet or f\"Part {i+1}\",\n            \"level\": 1,\n            \"start_pos\": start,\n            \"end_pos\": end,\n            \"char_len\": end - start,\n        })\n\nresult = {\n    \"num_sections\": len(sections),\n    \"total_chars\": len(md),\n    \"sections\": [\n        {\"id\": i, \"section\": s[\"section\"], \"level\": s[\"level\"],\n         \"start_pos\": s[\"start_pos\"], \"end_pos\": s[\"end_pos\"],\n         \"char_len\": s[\"char_len\"]}\n        for i, s in enumerate(sections)\n    ],\n}",
				"filesList":{
					"items":{
						"fileDetails":"{{ _T2QbF.outputs.files[0].obj }}",
						"fileName":"{{ _T2QbF.outputs.files[0].name }}"
					},
					"source":"{{ _T2QbF.outputs.files }}",
					"ua:type":"mappedArray"
				},
				"isAsync":false,
				"python_version":"3.12",
				"outputFilesGlob":"*",
				"entityId":"e_6a7372d25281b964f3b18aa7",
				"captureStdOutput":false
			},
			"skip":false,
			"subTitle":"Code by UnifyApps",
			"title":"Execute Python script",
			"type":"ACTION"
		},
		{
			"context":{
				"appName":"code_by_unifyapps",
				"resourceVersion":2813,
				"resourceName":"code_by_unifyapps_groovy",
				"type":"APPLICATION"
			},
			"debug":false,
			"dirty":false,
			"fallbackMode":"STOP",
			"groupId":"n_IACqJ@_ONz84-1@y",
			"id":"n_w9jHH",
			"index":5,
			"inputs":{
				"output":{
					"additionalProperties":false,
					"properties":{
						"out":{
							"title":"Out",
							"type":"string"
						}
					},
					"required":[],
					"type":"object"
				},
				"input":{
					"additionalProperties":false,
					"properties":{
						"input":{
							"title":"Input",
							"type":"string"
						}
					},
					"required":[],
					"type":"object"
				},
				"code":"def input = binding.hasVariable(\"input\") ? binding.getVariable(\"input\") : \"\"\n\ndef out = input?.toString() ?: \"\"\n\ndef truncated = false\n\nif (out.length() > 10000) {\n    out = out.substring(0, 10000)\n    truncated = true\n}\n\nout = out\n        .replaceAll(/[\\r\\n\\t]+/, \"\")\n        .replaceAll(/ {2,}/, \"\")\n        .trim()\n\nif (truncated) {\n    out += \"... [truncated]\"\n}\n\nreturn [out: out]",
				"compile_static":false,
				"captureStdOutput":false,
				"parameters":{
					"input":"{{ n_4HCLf.outputs.result }} {{ n_d65Ac.outputs.result }}"
				}
			},
			"skip":false,
			"subTitle":"Code by UnifyApps",
			"title":"Execute Groovy code",
			"type":"ACTION"
		},
		{
			"context":{
				"appName":"callables",
				"resourceName":"callables_return_to_automation",
				"type":"APPLICATION"
			},
			"debug":false,
			"dirty":false,
			"fallbackMode":"STOP",
			"groupId":"n_IACqJ@_ONz84-1@y",
			"id":"_v6dAe",
			"index":6,
			"inputs":{
				"result":{
					"toolOutput":"{{ n_w9jHH.outputs.result.out }}"
				}
			},
			"skip":false,
			"subTitle":"Callable",
			"title":"Respond to automation",
			"type":"STOP"
		}
	],
	"ownerUserId":-1,
	"paused":false,
	"schemaReferences":[
		
	],
	"settings":{
		"enableNodeLevelLogging":true,
		"enableRunLogging":true,
		"enableVariableLogging":true,
		"route":{
			"default":false,
			"tierName":"global"
		}
	},
	"standard":true,
	"version":0,
	"workflowDefinitionId":"6a77581f200309750c3c7177",
	"workflowVersion":44
}