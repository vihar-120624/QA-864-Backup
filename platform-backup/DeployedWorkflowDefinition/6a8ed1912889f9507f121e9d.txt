{
	"changesetType":"IMPORT",
	"createdTime":1787744657665,
	"deleted":false,
	"deployedWorkflowVersion":1,
	"deploymentSource":{
		"changesetRequestId":"6a8eca1d749c5474a80fee46",
		"changesetType":"IMPORT"
	},
	"edges":[
		{
			"fromNodeId":"n_zHXKh",
			"priority":0,
			"skip":false,
			"toNodeId":"n_0X9K1",
			"type":"next"
		},
		{
			"fromNodeId":"n_0X9K1",
			"priority":0,
			"skip":false,
			"toNodeId":"n_Z7Rqi",
			"type":"next"
		},
		{
			"fromNodeId":"n_Z7Rqi",
			"priority":0,
			"skip":false,
			"toNodeId":"n_c4PzF",
			"type":"loop"
		},
		{
			"fromNodeId":"n_c4PzF",
			"name":"yes",
			"priority":0,
			"skip":false,
			"toNodeId":"n_Opq3V",
			"type":"if"
		},
		{
			"fromNodeId":"n_Opq3V",
			"priority":0,
			"skip":false,
			"toNodeId":"n_krV7j",
			"type":"next"
		},
		{
			"fromNodeId":"n_krV7j",
			"priority":0,
			"skip":false,
			"toNodeId":"_X8BkJ",
			"type":"next"
		},
		{
			"fromNodeId":"n_c4PzF",
			"name":"no",
			"priority":0,
			"skip":false,
			"toNodeId":"_6K674",
			"type":"next"
		},
		{
			"fromNodeId":"_X8BkJ",
			"name":"loopback",
			"priority":0,
			"skip":false,
			"toNodeId":"n_Z7Rqi",
			"type":"next"
		},
		{
			"fromNodeId":"_6K674",
			"name":"loopback",
			"priority":0,
			"skip":false,
			"toNodeId":"n_Z7Rqi",
			"type":"next"
		},
		{
			"fromNodeId":"n_Z7Rqi",
			"priority":0,
			"skip":false,
			"toNodeId":"n_Sj3k7",
			"type":"next"
		},
		{
			"fromNodeId":"n_Sj3k7",
			"priority":0,
			"skip":false,
			"toNodeId":"n_gWeNE",
			"type":"next"
		},
		{
			"fromNodeId":"n_gWeNE",
			"priority":0,
			"skip":false,
			"toNodeId":"n_GX2uo",
			"type":"next"
		},
		{
			"fromNodeId":"n_GX2uo",
			"priority":0,
			"skip":false,
			"toNodeId":"_u2WlU",
			"type":"next"
		},
		{
			"fromNodeId":"_u2WlU",
			"name":"yes",
			"priority":0,
			"skip":false,
			"toNodeId":"n_le83X",
			"type":"if"
		},
		{
			"fromNodeId":"n_le83X",
			"priority":0,
			"skip":false,
			"toNodeId":"n_Vv1JX",
			"type":"next"
		},
		{
			"fromNodeId":"n_Vv1JX",
			"priority":0,
			"skip":false,
			"toNodeId":"_mQJST",
			"type":"next"
		},
		{
			"fromNodeId":"_mQJST",
			"priority":0,
			"skip":false,
			"toNodeId":"_TRD1z",
			"type":"next"
		},
		{
			"fromNodeId":"_u2WlU",
			"name":"no",
			"priority":0,
			"skip":false,
			"toNodeId":"n_3aisR",
			"type":"next"
		},
		{
			"fromNodeId":"n_3aisR",
			"priority":0,
			"skip":false,
			"toNodeId":"_C2vh5",
			"type":"next"
		},
		{
			"fromNodeId":"_C2vh5",
			"priority":0,
			"skip":false,
			"toNodeId":"_3gRoV",
			"type":"next"
		},
		{
			"fromNodeId":"_3gRoV",
			"priority":0,
			"skip":false,
			"toNodeId":"_FJh52",
			"type":"next"
		}
	],
	"id":"6a8ed1912889f9507f121e9d",
	"lastModifiedBy":-1,
	"lcName":"lowesexcel_generatepdfdoc",
	"modifiedTime":1787744657667,
	"name":"LowesExcel_GeneratePdfDoc",
	"nodes":[
		{
			"context":{
				"appName":"callables",
				"resourceVersion":398,
				"resourceName":"callables_from_automation"
			},
			"debug":false,
			"dirty":false,
			"fallbackMode":"STOP",
			"groupId":"_ftk3g-1",
			"id":"n_zHXKh",
			"index":1,
			"inputs":{
				"result":{
					"additionalProperties":false,
					"properties":{
						"file":{
							"additionalProperties":false,
							"properties":{},
							"required":[],
							"title":"file",
							"type":"object"
						},
						"url":{
							"title":"url",
							"type":"string"
						}
					},
					"type":"object"
				},
				"setup":{
					"additionalProperties":false,
					"properties":{
						"contentSlides":{
							"items":{
								"additionalProperties":false,
								"properties":{
									"base64string":{
										"title":"Base 64 String",
										"type":"string"
									},
									"content":{
										"title":"content",
										"type":"string"
									},
									"description":{
										"title":"description",
										"type":"string"
									},
									"subtitle":{
										"title":"subtitle",
										"type":"string"
									},
									"title":{
										"title":"title",
										"type":"string"
									},
									"url":{
										"title":"url",
										"type":"string"
									}
								},
								"required":[],
								"type":"object"
							},
							"title":"contentSlides",
							"type":"array"
						},
						"type":{
							"title":"Type",
							"type":"string"
						}
					},
					"required":[],
					"type":"object"
				}
			},
			"skip":false,
			"subTitle":"Callable",
			"title":"Trigger via automation",
			"trigger":{
				"type":"CALLABLE"
			},
			"type":"START"
		},
		{
			"context":{
				"appName":"variable_by_unifyapps",
				"resourceVersion":562,
				"resourceName":"variable_by_unifyapps_create_list",
				"type":"APPLICATION"
			},
			"debug":false,
			"dirty":false,
			"fallbackMode":"STOP",
			"groupId":"_ftk3g-1",
			"id":"n_0X9K1",
			"index":2,
			"inputSchema":{
				"type":"SCHEMA_AND_LAYOUT",
				"dynamic":false,
				"layout":{
					"createList":{
						"items":{
							"ui:description":"Set the initial values of the list (defaults to empty)",
							"ui:title":"Initial list items"
						},
						"ua:type":{
							"ui:widget":"hidden"
						},
						"ui:order":[
							"*",
							"items"
						],
						"ui:title":""
					},
					"ua:title":{
						"ui:widget":"hidden"
					},
					"ua:list_type":{
						"ui:widget":"hidden"
					}
				},
				"schema":{
					"allOf":[
						{
							"if":{
								"properties":{
									"ua:list_type":{
										"const":"OBJECT"
									}
								}
							},
							"then":{
								"properties":{
									"createList":{
										"properties":{
											"items":{
												"properties":{},
												"type":"object"
											}
										},
										"type":"object"
									}
								}
							}
						},
						{
							"if":{
								"properties":{
									"ua:list_type":{
										"const":"STRING"
									}
								}
							},
							"then":{
								"properties":{
									"createList":{
										"properties":{
											"items":{
												"type":"string"
											},
											"source":{
												"format":"ua:source",
												"title":"List of String",
												"type":"string"
											}
										},
										"type":"object"
									}
								}
							}
						},
						{
							"if":{
								"properties":{
									"ua:list_type":{
										"const":"INTEGER"
									}
								}
							},
							"then":{
								"properties":{
									"createList":{
										"properties":{
											"items":{
												"type":"integer"
											},
											"source":{
												"format":"ua:source",
												"title":"List of Integers",
												"type":"string"
											}
										},
										"type":"object"
									}
								}
							}
						},
						{
							"if":{
								"properties":{
									"ua:list_type":{
										"const":"NUMBER"
									}
								}
							},
							"then":{
								"properties":{
									"createList":{
										"properties":{
											"items":{
												"type":"number"
											},
											"source":{
												"format":"ua:source",
												"title":"List of Numbers",
												"type":"string"
											}
										},
										"type":"object"
									}
								}
							}
						},
						{
							"if":{
								"properties":{
									"ua:list_type":{
										"const":"BOOLEAN"
									}
								}
							},
							"then":{
								"properties":{
									"createList":{
										"properties":{
											"items":{
												"type":"boolean"
											},
											"source":{
												"format":"ua:source",
												"title":"List of Booleans",
												"type":"string"
											}
										},
										"type":"object"
									}
								}
							}
						}
					],
					"format":"ua:list",
					"type":"object",
					"properties":{
						"createList":{
							"properties":{
								"items":{
									"additionalProperties":false,
									"properties":{
										"content":{
											"title":"content",
											"type":"string"
										},
										"description":{
											"title":"description",
											"type":"string"
										},
										"subtitle":{
											"title":"subtitle",
											"type":"string"
										},
										"title":{
											"title":"title",
											"type":"string"
										},
										"url":{
											"title":"url",
											"type":"string"
										}
									},
									"type":"object"
								},
								"source":{
									"format":"ua:source",
									"title":"List Item Source",
									"type":"string"
								},
								"ua:type":{
									"default":"mappedArray",
									"type":"string"
								}
							},
							"type":"object"
						},
						"ua:list_type":{
							"default":"OBJECT",
							"oneOf":[
								{
									"const":"OBJECT",
									"title":"Object"
								},
								{
									"const":"STRING",
									"title":"String"
								},
								{
									"const":"NUMBER",
									"title":"Number"
								},
								{
									"const":"INTEGER",
									"title":"Integer"
								},
								{
									"const":"BOOLEAN",
									"title":"Boolean"
								}
							],
							"title":"List Type",
							"type":"string"
						},
						"ua:title":{
							"type":"string"
						}
					}
				}
			},
			"inputs":{
				"createList":{
					"ua:type":"mappedArray"
				},
				"ua:title":"contentSlides",
				"ua:list_type":"OBJECT"
			},
			"outputSchema":{
				"type":"SCHEMA",
				"dynamic":false,
				"schema":{
					"type":"object",
					"properties":{
						"items":{
							"disableActions":true,
							"items":{
								"properties":{
									"content":{
										"title":"content",
										"type":"string"
									},
									"description":{
										"title":"description",
										"type":"string"
									},
									"subtitle":{
										"title":"subtitle",
										"type":"string"
									},
									"title":{
										"title":"title",
										"type":"string"
									},
									"url":{
										"title":"url",
										"type":"string"
									}
								},
								"type":"object"
							},
							"title":"contentSlides",
							"type":"array"
						}
					}
				}
			},
			"skip":false,
			"subTitle":"contentSlides",
			"title":"Create list",
			"type":"ACTION"
		},
		{
			"context":{
				"appName":"loop",
				"resourceVersion":463,
				"resourceName":"loop_for_each",
				"type":"APPLICATION"
			},
			"debug":false,
			"dirty":false,
			"fallbackMode":"STOP",
			"groupId":"_ftk3g-1",
			"id":"n_Z7Rqi",
			"index":3,
			"inputs":{
				"repeatMode":"SINGLE",
				"listSource":"{{ n_zHXKh.outputs.contentSlides }}",
				"captureIterations":false
			},
			"skip":false,
			"subTitle":"inputSlides",
			"title":"For each loop",
			"type":"LOOP"
		},
		{
			"context":{
				"appName":"if_else",
				"resourceVersion":116,
				"resourceName":"if_else_condition",
				"type":"APPLICATION"
			},
			"debug":false,
			"dirty":false,
			"fallbackMode":"STOP",
			"groupId":"n_Z7Rqi@_ftk3g-1@l",
			"id":"n_c4PzF",
			"index":4,
			"inputs":{
				"filters":[
					{
						"filter":{
							"operator":"EXISTS"
						},
						"property":"{{ n_Z7Rqi.outputs.item.base64string }}"
					}
				],
				"operator":"AND"
			},
			"skip":false,
			"subTitle":"base64String?",
			"title":"Condition",
			"type":"IF_ELSE"
		},
		{
			"context":{
				"appName":"utility_by_unifyapps",
				"resourceVersion":542,
				"resourceName":"utility_by_unifyapps_upload_base_64_content",
				"type":"APPLICATION"
			},
			"debug":false,
			"dirty":false,
			"fallbackMode":"STOP",
			"groupId":"n_c4PzF@n_Z7Rqi@_ftk3g-1@l@y",
			"id":"n_Opq3V",
			"index":5,
			"inputs":{
				"fileName":"image_{{ n_Z7Rqi.outputs.index }}.png",
				"base64ContentFormat":"STANDARD",
				"content":"{{ n_Z7Rqi.outputs.item.base64string }}"
			},
			"skip":false,
			"subTitle":"image",
			"title":"Upload Base64 Content",
			"type":"ACTION"
		},
		{
			"context":{
				"appName":"utility_by_unifyapps",
				"resourceVersion":674,
				"resourceName":"utility_by_unifyapps_generate_public_url",
				"type":"APPLICATION"
			},
			"debug":false,
			"dirty":false,
			"fallbackMode":"STOP",
			"groupId":"n_c4PzF@n_Z7Rqi@_ftk3g-1@l@y",
			"id":"n_krV7j",
			"index":6,
			"inputs":{
				"file":"{{ n_Opq3V.outputs.fileDetails }}",
				"expiryTime":1
			},
			"skip":false,
			"subTitle":"PublicFileURL",
			"title":"Generate public file URL",
			"type":"ACTION"
		},
		{
			"context":{
				"appName":"variable_by_unifyapps",
				"resourceVersion":578,
				"resourceName":"variable_by_unifyapps_add_item_to_list",
				"type":"APPLICATION"
			},
			"debug":false,
			"dirty":false,
			"fallbackMode":"STOP",
			"groupId":"n_c4PzF@n_Z7Rqi@_ftk3g-1@l@y",
			"id":"_X8BkJ",
			"index":7,
			"inputs":{
				"itemToAdd":{
					"content":"{{ n_Z7Rqi.outputs.item.content }}",
					"description":"{{ n_Z7Rqi.outputs.item.description }}",
					"subtitle":"{{ n_Z7Rqi.outputs.item.subtitle }}",
					"title":"{{ n_Z7Rqi.outputs.item.title }}",
					"url":"{{ n_krV7j.outputs.url }}"
				},
				"insertLocation":"END",
				"listName":"{{ n_0X9K1.outputs.items[0] }}"
			},
			"skip":false,
			"subTitle":"contentSlides",
			"title":"Add item to list",
			"type":"ACTION"
		},
		{
			"context":{
				"appName":"variable_by_unifyapps",
				"resourceVersion":578,
				"resourceName":"variable_by_unifyapps_add_item_to_list",
				"type":"APPLICATION"
			},
			"debug":false,
			"dirty":false,
			"fallbackMode":"STOP",
			"groupId":"n_c4PzF@n_Z7Rqi@_ftk3g-1@l@n",
			"id":"_6K674",
			"index":8,
			"inputs":{
				"itemToAdd":{
					"content":"{{ n_Z7Rqi.outputs.item.content }}",
					"description":"{{ n_Z7Rqi.outputs.item.description }}",
					"subtitle":"{{ n_Z7Rqi.outputs.item.subtitle }}",
					"title":"{{ n_Z7Rqi.outputs.item.title }}",
					"url":"{{ n_Z7Rqi.outputs.item.url }}"
				},
				"insertLocation":"END",
				"listName":"{{ n_0X9K1.outputs.items[0] }}"
			},
			"skip":false,
			"subTitle":"contentSlides",
			"title":"Add item to list",
			"type":"ACTION"
		},
		{
			"additional":{
				"xsdSchemaConfig":{
					"root":{}
				},
				"visibleOptionalFields":[
					"root.parameters.contentSlides.items.title",
					"root.parameters.contentSlides.items.subtitle",
					"root.parameters.contentSlides.items.content",
					"root.parameters.contentSlides.items.url",
					"root.parameters.contentSlides.items.description"
				]
			},
			"context":{
				"appName":"code_by_unifyapps",
				"resourceVersion":596,
				"resourceName":"code_by_unifyapps_groovy",
				"type":"APPLICATION"
			},
			"debug":false,
			"dirty":false,
			"fallbackMode":"STOP",
			"groupId":"_ftk3g-1",
			"id":"n_Sj3k7",
			"index":9,
			"inputs":{
				"output":{
					"additionalProperties":false,
					"properties":{
						"slides":{
							"items":{
								"type":"object"
							},
							"title":"slides",
							"type":"array"
						}
					},
					"type":"object"
				},
				"input":{
					"additionalProperties":false,
					"properties":{
						"contentSlides":{
							"items":{
								"additionalProperties":false,
								"properties":{
									"content":{
										"title":"content",
										"type":"string"
									},
									"description":{
										"title":"description",
										"type":"string"
									},
									"subtitle":{
										"title":"subtitle",
										"type":"string"
									},
									"title":{
										"title":"title",
										"type":"string"
									},
									"url":{
										"title":"url",
										"type":"string"
									}
								},
								"type":"object"
							},
							"title":"contentSlides",
							"type":"array"
						}
					},
					"type":"object"
				},
				"code":"/*\nExpected input:\n{\n  \"contentSlides\":[\n    {\n      \"title\":\"\",        // optional\n      \"subtitle\":\"\",     // one of subtitle or content appears\n      \"content\":\"\",\n      \"url\":\"\",\n      \"description\":\"\"\n    }\n  ]\n}\n\nOutput (for downstream file generator):\n[\n  slides: [\n    // for subtitle slides\n    { title: \"...\", content_type: \"title_subtitle\", subtitle: \"...\" },\n\n    // for content slides\n    { title: \"...\", content_type: \"title_content\", content: \"para1\\n\\npara2\" },\n\n    // for title + image + description\n    { title: \"...\", content_type: \"title_image_description\", image_url: \"https://...\", description: \"...\" },\n\n    // for image + description (no title)\n    { content_type: \"image_description\", image_url: \"https://...\", description: \"...\" }\n\n    // for title + image + content\n    { title: \"...\", content_type: \"title_image_content\", image_url: \"https://...\", content: \"...\" },\n\n    // for image + content (no title)\n    { content_type: \"image_content\", image_url: \"https://...\", content: \"...\" }\n  ]\n]\n*/\n\ndef raw = binding.hasVariable(\"contentSlides\") ? binding.getVariable(\"contentSlides\") : []\ndef items = (raw instanceof Collection) ? raw : []\n\ndef slides = []\n\nitems.each { cs ->\n  def title = (cs?.title ?: \"\").toString().trim()\n  def subtitle = (cs?.subtitle ?: \"\").toString().trim()\n  def content = (cs?.content ?: \"\").toString().trim()\n  def url = (cs?.url ?: \"\").toString().trim()\n  def description = (cs?.description ?: \"\").toString().trim()\n\n  if (url) {\n    // Image-based slide\n    if (title) {\n      //Titled Slide\n      if(description){\n        // image description is present\n        def m = [\n          title       : title,\n          content_type: \"title_image_description\",\n          image_url   : url,\n          description : description\n        ]\n        slides << m\n      }\n      else {\n        def m = [\n          title       : title,\n          content_type: \"title_image_content\",\n          image_url   : url\n        ]\n        if(content){\n          m.content = content\n        }\n        slides << m\n      }\n    } else {\n      if(description){\n        def m = [\n          content_type: \"image_description\",\n          image_url   : url\n        ]\n        m.description = description\n        slides << m\n      }\n      else{\n        def m = [\n          content_type: \"image_content\",\n          image_url   : url\n        ]\n        if(content){\n          m.content = content\n        }\n        slides << m\n      }\n    }\n  } else if (content) {\n    // Title + Content (paragraphs can be separated by newlines)\n    slides << [\n      title       : title,\n      content_type: \"title_content\",\n      content     : content\n    ]\n  } else if (subtitle) {\n    // Title + Subtitle\n    slides << [\n      title       : title,\n      content_type: \"title_subtitle\",\n      subtitle    : subtitle\n    ]\n  } \n}\n\nreturn [slides: slides]",
				"compile_static":false,
				"captureStdOutput":false,
				"parameters":{
					"contentSlides":{
						"items":{
							"content":"{{ n_0X9K1.outputs.items[0].content }}",
							"description":"{{ n_0X9K1.outputs.items[0].description }}",
							"subtitle":"{{ n_0X9K1.outputs.items[0].subtitle }}",
							"title":"{{ n_0X9K1.outputs.items[0].title }}",
							"url":"{{ n_0X9K1.outputs.items[0].url }}"
						},
						"source":"{{ n_0X9K1.outputs.items }}",
						"ua:type":"mappedArray"
					}
				}
			},
			"skip":false,
			"subTitle":"slides",
			"title":"Execute Groovy code",
			"type":"ACTION"
		},
		{
			"context":{
				"appName":"storage_by_unifyapps",
				"resourceVersion":467,
				"resourceName":"storage_by_unifyapps_get_record_by_id",
				"type":"APPLICATION"
			},
			"debug":false,
			"dirty":false,
			"fallbackMode":"STOP",
			"groupId":"_ftk3g-1",
			"id":"n_gWeNE",
			"index":10,
			"inputs":{
				"object_type":"file_template",
				"includeRoleMappings":false,
				"includeCurrentUserPermissions":false,
				"translationsOption":"DEFAULT",
				"id":"e_691ae5d6b89ca466ebff0096",
				"readThroughSessionVariables":false
			},
			"skip":false,
			"subTitle":"PDF Template",
			"title":"Fetch record by ID",
			"type":"ACTION"
		},
		{
			"context":{
				"appName":"utility_by_unifyapps",
				"resourceVersion":654,
				"resourceName":"utility_by_unifyapps_generate_public_url",
				"type":"APPLICATION"
			},
			"debug":false,
			"dirty":false,
			"fallbackMode":"STOP",
			"groupId":"_ftk3g-1",
			"id":"n_GX2uo",
			"index":11,
			"inputs":{
				"file":"{{ n_gWeNE.outputs.properties.file }}",
				"expiryTime":1
			},
			"skip":false,
			"subTitle":"pptxTemplate",
			"title":"Generate public file URL",
			"type":"ACTION"
		},
		{
			"context":{
				"appName":"if_else",
				"resourceVersion":447,
				"resourceName":"if_else_condition",
				"type":"APPLICATION"
			},
			"debug":false,
			"dirty":false,
			"fallbackMode":"STOP",
			"groupId":"_ftk3g-1",
			"id":"_u2WlU",
			"index":12,
			"inputs":{
				"filters":[
					{
						"filter":{
							"operator":"EQUAL",
							"value":"pdf"
						},
						"property":"{{ n_zHXKh.outputs.type }}"
					}
				],
				"operator":"AND"
			},
			"skip":false,
			"subTitle":"Condition",
			"title":"Condition",
			"type":"IF_ELSE"
		},
		{
			"additional":{
				"visibleOptionalFields":[
					"root.parameters.slides.items"
				]
			},
			"context":{
				"appName":"code_by_unifyapps",
				"resourceVersion":0,
				"resourceName":"code_by_unifyapps_python",
				"type":"APPLICATION"
			},
			"debug":false,
			"dirty":false,
			"fallbackMode":"STOP",
			"groupId":"_u2WlU@_ftk3g-1@y",
			"id":"n_le83X",
			"index":13,
			"inputs":{
				"output":{
					"additionalProperties":false,
					"properties":{
						"base64":{
							"title":"base64",
							"type":"string"
						},
						"filename":{
							"title":"filename",
							"type":"string"
						},
						"mime_type":{
							"title":"mime_type",
							"type":"string"
						},
						"size":{
							"title":"size",
							"type":"number"
						}
					},
					"type":"object"
				},
				"configurationMode":"TEMPLATE",
				"input":{
					"additionalProperties":false,
					"properties":{
						"slides":{
							"items":{
								"type":"object"
							},
							"title":"slides",
							"type":"array"
						}
					},
					"type":"object"
				},
				"code":"from fpdf import FPDF\nimport requests\nfrom io import BytesIO\nfrom PIL import Image\nimport tempfile\nimport os\nimport math\nimport base64\nfrom datetime import datetime\n\nclass ImagePDF(FPDF):\n    def __init__(self):\n        super().__init__()\n        self.set_auto_page_break(auto=False)\n\n    def title_page(self, title, subtitle):\n        \"\"\"Create a title page with title and subtitle\"\"\"\n        self.add_page()\n        margin = 20  # mm\n        self.set_font('Arial', 'B', 24)\n        self.set_xy(margin, 100)\n        self.cell(self.w - 2*margin, 10, title or \"\", 0, 1, 'C')\n        self.set_font('Arial', 'I', 16)\n        self.ln(10)\n        self.set_x(margin)\n        self.cell(self.w - 2*margin, 10, subtitle or \"\", 0, 1, 'C')\n\n    def _estimate_text_height(self, text, max_width, line_height):\n        \"\"\"Roughly estimate the height (in mm) needed to render text within max_width using current font.\"\"\"\n        if not text:\n            return 0\n        total_lines = 0\n        paragraphs = [p for p in text.split('\\n') if p is not None]\n        if not paragraphs:\n            paragraphs = ['']\n        for para in paragraphs:\n            p = (para or \"\")\n            if p == \"\":\n                total_lines += 1\n                continue\n            w = self.get_string_width(p)\n            lines = max(1, math.ceil(w / max_width)) if max_width > 0 else 1\n            total_lines += lines\n        return total_lines * line_height\n\n    def _split_paragraphs(self, content):\n        \"\"\"Split content into paragraphs. Accepts string possibly containing blank lines.\"\"\"\n        if not content:\n            return []\n        parts = [p.strip() for p in content.split('\\n\\n') if p and p.strip()]\n        if parts:\n            return parts\n        return [p.strip() for p in content.split('\\n') if p and p.strip()]\n\n    def _estimate_paragraphs_height(self, paragraphs, max_width, line_height):\n        total = 0\n        for i, p in enumerate(paragraphs):\n            w = self.get_string_width(p)\n            lines = max(1, math.ceil(w / max_width)) if max_width > 0 else 1\n            total += lines * line_height\n            if i < len(paragraphs) - 1:\n                total += line_height * 0.5\n        return total\n\n    def content_pages(self, title, content, margin=20):\n        \"\"\"Title + multi-paragraph content. Splits across pages if needed.\"\"\"\n        title = title or \"\"\n        paragraphs = self._split_paragraphs(content or \"\")\n        if not paragraphs:\n            paragraphs = [\"\"]\n\n        title_font = ('Arial', 'B', 20)\n        body_font_size_candidates = [12, 11, 10, 9, 8]\n        line_height = 6.0\n        para_spacing = line_height * 0.5\n        title_box_height = 12.0\n        title_spacing = 5.0\n\n        chosen_body_font = body_font_size_candidates[-1]\n        for fs in body_font_size_candidates:\n            self.set_font('Arial', '', fs)\n            available_height = self.h - 2*margin - title_box_height - title_spacing\n            est = self._estimate_paragraphs_height(paragraphs, self.w - 2*margin, line_height)\n            if est <= available_height:\n                chosen_body_font = fs\n                break\n\n        remaining = list(paragraphs)\n        while remaining:\n            self.add_page()\n            self.set_font(*title_font)\n            self.set_xy(margin, margin)\n            self.cell(self.w - 2*margin, 10, title, 0, 1, 'C')\n\n            y_start = margin + title_box_height + title_spacing\n            self.set_xy(margin, y_start)\n            available_height = self.h - margin - self.get_y()\n\n            self.set_font('Arial', '', chosen_body_font)\n            chunk = []\n            used = 0.0\n            for i, p in enumerate(remaining):\n                w = self.get_string_width(p)\n                lines = max(1, math.ceil(w / (self.w - 2*margin)))\n                height_needed = lines * line_height + (para_spacing if chunk else 0.0)\n                if used + height_needed <= available_height or not chunk:\n                    chunk.append(p)\n                    used += height_needed\n                else:\n                    break\n\n            for idx, p in enumerate(chunk):\n                if idx > 0:\n                    self.ln(para_spacing)\n                self.set_x(margin)\n                self.multi_cell(self.w - 2*margin, line_height, p, 0, 'L')\n\n            remaining = remaining[len(chunk):]\n\n    def image_page(self, public_url, description=\"\", timeout=(5, 25)):\n        \"\"\"\n        Add a page with a centered image and a description beneath it.\n        Args:\n            public_url: Public URL to download the image from\n            description: Description text below the image\n        \"\"\"\n        self.add_page()\n        margin = 20\n        text_margin_top = 5\n        content_width = self.w - 2 * margin\n        content_height = self.h - 2 * margin\n\n        min_image_height = 20\n        candidate_font_sizes = [12, 11, 10, 9, 8]\n        chosen_font_size = candidate_font_sizes[-1]\n        text_height = 0\n        line_height = 6\n\n        for fs in candidate_font_sizes:\n            self.set_font('Arial', '', fs)\n            text_height_try = self._estimate_text_height(description, content_width, line_height)\n            if text_height_try + text_margin_top + min_image_height <= content_height:\n                chosen_font_size = fs\n                text_height = text_height_try\n                break\n        else:\n            self.set_font('Arial', '', chosen_font_size)\n            text_height = max(0, content_height - min_image_height - text_margin_top)\n\n        available_image_height = max(min_image_height, content_height - text_margin_top - text_height)\n\n        temp_file = None\n        try:\n            print(f\"Downloading image from: {public_url}\")\n            response = requests.get(public_url, timeout=timeout, verify=False)\n            response.raise_for_status()\n            img = Image.open(BytesIO(response.content))\n\n            img_format = (img.format.lower() if img.format else 'png')\n            if img_format not in ['jpg', 'jpeg', 'png']:\n                img_format = 'png'\n            if img_format in ['jpg', 'jpeg'] and img.mode in ['RGBA', 'LA', 'P']:\n                img = img.convert('RGB')\n\n            temp_file = tempfile.NamedTemporaryFile(delete=False, suffix=f'.{img_format}')\n            save_format = 'JPEG' if img_format in ['jpg', 'jpeg'] else 'PNG'\n            img.save(temp_file.name, format=save_format)\n            temp_file.close()\n\n            img_width_px, img_height_px = img.size\n\n            width_ratio = content_width / img_width_px\n            height_ratio = available_image_height / img_height_px\n            scale = min(width_ratio, height_ratio)\n\n            new_width = img_width_px * scale\n            new_height = img_height_px * scale\n\n            x = margin + (content_width - new_width) / 2\n            y = margin + (available_image_height - new_height) / 2\n\n            self.image(temp_file.name, x=x, y=y, w=new_width, h=new_height)\n\n            self.set_y(margin + available_image_height + text_margin_top)\n            self.set_x(margin)\n            self.set_font('Arial', '', chosen_font_size)\n            self.multi_cell(content_width, line_height, description, 0, 'C')\n\n        except Exception as e:\n            print(f\"Error loading image from {public_url}: {str(e)}\")\n            self.set_font('Arial', '', 12)\n            self.set_y(self.h / 2 - 10)\n            self.cell(0, 10, f'Error loading image: {str(e)}', 0, 1, 'C')\n\n            self.set_font('Arial', '', 11)\n            self.set_y(self.h / 2 + 10)\n            self.set_x(margin)\n            self.multi_cell(content_width, 6, description, 0, 'C')\n        finally:\n            if temp_file and os.path.exists(temp_file.name):\n                try:\n                    os.unlink(temp_file.name)\n                except:\n                    pass\n\n    def title_image_page(self, title, public_url, description=\"\", timeout=(5, 25)):\n        \"\"\"Title + image + description (no overlap)\"\"\"\n        self.add_page()\n        margin = 20\n        text_margin_top = 5\n        title_box_height = 12.0\n        title_spacing = 5.0\n\n        content_width = self.w - 2 * margin\n        content_height = self.h - 2 * margin - title_box_height - title_spacing\n\n        self.set_font('Arial', 'B', 20)\n        self.set_xy(margin, margin)\n        self.cell(content_width, 10, title or \"\", 0, 1, 'C')\n\n        min_image_height = 20\n        candidate_font_sizes = [12, 11, 10, 9, 8]\n        chosen_font_size = candidate_font_sizes[-1]\n        line_height = 6\n\n        text_height = 0\n        for fs in candidate_font_sizes:\n            self.set_font('Arial', '', fs)\n            text_height_try = self._estimate_text_height(description, content_width, line_height)\n            if text_height_try + text_margin_top + min_image_height <= content_height:\n                chosen_font_size = fs\n                text_height = text_height_try\n                break\n        else:\n            self.set_font('Arial', '', chosen_font_size)\n            text_height = max(0, content_height - min_image_height - text_margin_top)\n\n        available_image_height = max(min_image_height, content_height - text_margin_top - text_height)\n\n        temp_file = None\n        try:\n            print(f\"Downloading image from: {public_url}\")\n            response = requests.get(public_url, timeout=timeout, verify=False)\n            response.raise_for_status()\n            img = Image.open(BytesIO(response.content))\n\n            img_format = (img.format.lower() if img.format else 'png')\n            if img_format not in ['jpg', 'jpeg', 'png']:\n                img_format = 'png'\n            if img_format in ['jpg', 'jpeg'] and img.mode in ['RGBA', 'LA', 'P']:\n                img = img.convert('RGB')\n\n            temp_file = tempfile.NamedTemporaryFile(delete=False, suffix=f'.{img_format}')\n            save_format = 'JPEG' if img_format in ['jpg', 'jpeg'] else 'PNG'\n            img.save(temp_file.name, format=save_format)\n            temp_file.close()\n\n            img_width_px, img_height_px = img.size\n\n            width_ratio = content_width / img_width_px\n            height_ratio = available_image_height / img_height_px\n            scale = min(width_ratio, height_ratio)\n\n            new_width = img_width_px * scale\n            new_height = img_height_px * scale\n\n            x = margin + (content_width - new_width) / 2\n            y = margin + title_box_height + title_spacing + (available_image_height - new_height) / 2\n\n            self.image(temp_file.name, x=x, y=y, w=new_width, h=new_height)\n\n            self.set_y(margin + title_box_height + title_spacing + available_image_height + text_margin_top)\n            self.set_x(margin)\n            self.set_font('Arial', '', chosen_font_size)\n            self.multi_cell(content_width, line_height, description, 0, 'C')\n\n        except Exception as e:\n            print(f\"Error loading image from {public_url}: {str(e)}\")\n            self.set_font('Arial', '', 12)\n            self.set_y(self.h / 2 - 10)\n            self.cell(0, 10, f'Error loading image: {str(e)}', 0, 1, 'C')\n\n            self.set_font('Arial', '', 11)\n            self.set_y(self.h / 2 + 10)\n            self.set_x(margin)\n            self.multi_cell(content_width, 6, description, 0, 'C')\n        finally:\n            if temp_file and os.path.exists(temp_file.name):\n                try:\n                    os.unlink(temp_file.name)\n                except:\n                    pass\n\n    def image_content_page(self, public_url, content=\"\", timeout=(5, 25)):\n        \"\"\"\n        Page with a centered image and left-aligned paragraph content beneath it.\n\n        Rules:\n        - Image height on the first page is at least 1/2 of the available content height\n          (between top/bottom margins).\n        - If possible, keep *all* text on the same page with the image.\n        - If not, enforce the 1/2 rule and overflow remaining text to following pages.\n        \"\"\"\n        self.add_page()\n        margin = 20\n        text_margin_top = 5\n        content_width = self.w - 2 * margin\n        content_height = self.h - 2 * margin\n\n        min_image_height = 20\n        min_image_fraction = 0.50\n        line_height = 6.0\n        para_spacing = line_height * 0.5\n\n        paragraphs = self._split_paragraphs(content or \"\")\n        if not paragraphs:\n            paragraphs = []\n\n        # Estimate total text height at chosen font size\n        body_font_size = 12\n        self.set_font('Arial', '', body_font_size)\n        text_height_est = self._estimate_paragraphs_height(\n            paragraphs, content_width, line_height\n        )\n\n        # Maximum image height that still keeps ALL text on this page\n        max_image_height_if_all_text = content_height - text_margin_top - text_height_est\n        max_image_height_if_all_text = max(min_image_height, max_image_height_if_all_text)\n        max_image_height_if_all_text = min(max_image_height_if_all_text, content_height)\n\n        # Minimum allowed image height (1/2 rule)\n        target_min_image_height = min_image_fraction * content_height\n\n        if max_image_height_if_all_text >= target_min_image_height:\n            # We can satisfy the 1/2 rule AND keep all text on the first page\n            image_height = max_image_height_if_all_text\n            text_height_available = content_height - image_height - text_margin_top\n            all_text_on_first_page = True\n        else:\n            # To satisfy the 1/2 rule we must use a larger image and let text overflow\n            image_height = max(target_min_image_height, min_image_height)\n            image_height = min(image_height, content_height)\n            text_height_available = content_height - image_height - text_margin_top\n            all_text_on_first_page = False\n\n        temp_file = None\n        try:\n            print(f\"Downloading image from: {public_url}\")\n            response = requests.get(public_url, timeout=timeout, verify=False)\n            response.raise_for_status()\n            img = Image.open(BytesIO(response.content))\n\n            img_format = (img.format.lower() if img.format else 'png')\n            if img_format not in ['jpg', 'jpeg', 'png']:\n                img_format = 'png'\n            if img_format in ['jpg', 'jpeg'] and img.mode in ['RGBA', 'LA', 'P']:\n                img = img.convert('RGB')\n\n            temp_file = tempfile.NamedTemporaryFile(delete=False, suffix=f'.{img_format}')\n            save_format = 'JPEG' if img_format in ['jpg', 'jpeg'] else 'PNG'\n            img.save(temp_file.name, format=save_format)\n            temp_file.close()\n\n            img_width_px, img_height_px = img.size\n\n            # Scale image to fit within (content_width x image_height)\n            width_ratio = content_width / img_width_px\n            height_ratio = image_height / img_height_px\n            scale = min(width_ratio, height_ratio)\n\n            new_width = img_width_px * scale\n            new_height = img_height_px * scale\n\n            x = margin + (content_width - new_width) / 2\n            y = margin + (image_height - new_height) / 2\n\n            # Draw image\n            self.image(temp_file.name, x=x, y=y, w=new_width, h=new_height)\n\n            # Draw text under the image\n            self.set_font('Arial', '', body_font_size)\n            self.set_y(margin + image_height + text_margin_top)\n            self.set_x(margin)\n\n            remaining = list(paragraphs)\n            chunk = []\n            used = 0.0\n\n            for p in remaining:\n                p = p or \"\"\n                w = self.get_string_width(p)\n                lines = max(1, math.ceil(w / content_width)) if content_width > 0 else 1\n                height_needed = lines * line_height + (para_spacing if chunk else 0.0)\n\n                if used + height_needed <= text_height_available or not chunk:\n                    chunk.append(p)\n                    used += height_needed\n                else:\n                    break\n\n            # Render first-page chunk\n            for idx, p in enumerate(chunk):\n                if idx > 0:\n                    self.ln(para_spacing)\n                self.set_x(margin)\n                self.multi_cell(content_width, line_height, p, 0, 'L')\n\n            # Remaining paragraphs -> extra pages (no title)\n            remaining = remaining[len(chunk):]\n            if remaining:\n                remaining_text = \"\\n\\n\".join(remaining)\n                self.content_pages(\"\", remaining_text)\n\n        except Exception as e:\n            print(f\"Error loading image from {public_url}: {str(e)}\")\n            fallback_text = f\"[Error loading image: {str(e)}]\\n\\n{content or ''}\"\n            self.content_pages(\"\", fallback_text)\n        finally:\n            if temp_file and os.path.exists(temp_file.name):\n                try:\n                    os.unlink(temp_file.name)\n                except:\n                    pass\n\n    def title_image_content_page(self, title, public_url, content=\"\", timeout=(5, 25)):\n        \"\"\"\n        Page with a title, centered image, and left-aligned paragraph content beneath it.\n\n        Rules:\n        - Image height (in the region below the title) is at least 1/2 of that region.\n        - If possible, keep *all* text on the same page with the image.\n        - If not, enforce the 1/2 rule and overflow remaining text to following pages (with title).\n        \"\"\"\n        self.add_page()\n        margin = 20\n        text_margin_top = 5\n        title_box_height = 12.0\n        title_spacing = 5.0\n\n        content_width = self.w - 2 * margin\n        # Vertical space available for image + text under the title\n        content_height = self.h - 2 * margin - title_box_height - title_spacing\n\n        # Title\n        self.set_font('Arial', 'B', 20)\n        self.set_xy(margin, margin)\n        self.cell(content_width, 10, title or \"\", 0, 1, 'C')\n\n        min_image_height = 20\n        min_image_fraction = 0.50\n        line_height = 6.0\n        para_spacing = line_height * 0.5\n\n        paragraphs = self._split_paragraphs(content or \"\")\n        if not paragraphs:\n            paragraphs = []\n\n        # Estimate total text height\n        body_font_size = 12\n        self.set_font('Arial', '', body_font_size)\n        text_height_est = self._estimate_paragraphs_height(\n            paragraphs, content_width, line_height\n        )\n\n        # Max image height that still keeps all text under it on this page\n        max_image_height_if_all_text = content_height - text_margin_top - text_height_est\n        max_image_height_if_all_text = max(min_image_height, max_image_height_if_all_text)\n        max_image_height_if_all_text = min(max_image_height_if_all_text, content_height)\n\n        target_min_image_height = min_image_fraction * content_height\n\n        if max_image_height_if_all_text >= target_min_image_height:\n            image_height = max_image_height_if_all_text\n            text_height_available = content_height - image_height - text_margin_top\n            all_text_on_first_page = True\n        else:\n            image_height = max(target_min_image_height, min_image_height)\n            image_height = min(image_height, content_height)\n            text_height_available = content_height - image_height - text_margin_top\n            all_text_on_first_page = False\n\n        temp_file = None\n        try:\n            print(f\"Downloading image from: {public_url}\")\n            response = requests.get(public_url, timeout=timeout, verify=False)\n            response.raise_for_status()\n            img = Image.open(BytesIO(response.content))\n\n            img_format = (img.format.lower() if img.format else 'png')\n            if img_format not in ['jpg', 'jpeg', 'png']:\n                img_format = 'png'\n            if img_format in ['jpg', 'jpeg'] and img.mode in ['RGBA', 'LA', 'P']:\n                img = img.convert('RGB')\n\n            temp_file = tempfile.NamedTemporaryFile(delete=False, suffix=f'.{img_format}')\n            save_format = 'JPEG' if img_format in ['jpg', 'jpeg'] else 'PNG'\n            img.save(temp_file.name, format=save_format)\n            temp_file.close()\n\n            img_width_px, img_height_px = img.size\n\n            width_ratio = content_width / img_width_px\n            height_ratio = image_height / img_height_px\n            scale = min(width_ratio, height_ratio)\n\n            new_width = img_width_px * scale\n            new_height = img_height_px * scale\n\n            x = margin + (content_width - new_width) / 2\n            y = margin + title_box_height + title_spacing + (image_height - new_height) / 2\n\n            # Draw image\n            self.image(temp_file.name, x=x, y=y, w=new_width, h=new_height)\n\n            # Draw text under the image\n            self.set_font('Arial', '', body_font_size)\n            self.set_y(margin + title_box_height + title_spacing + image_height + text_margin_top)\n            self.set_x(margin)\n\n            remaining = list(paragraphs)\n            chunk = []\n            used = 0.0\n\n            for p in remaining:\n                p = p or \"\"\n                w = self.get_string_width(p)\n                lines = max(1, math.ceil(w / content_width)) if content_width > 0 else 1\n                height_needed = lines * line_height + (para_spacing if chunk else 0.0)\n\n                if used + height_needed <= text_height_available or not chunk:\n                    chunk.append(p)\n                    used += height_needed\n                else:\n                    break\n\n            for idx, p in enumerate(chunk):\n                if idx > 0:\n                    self.ln(para_spacing)\n                self.set_x(margin)\n                self.multi_cell(content_width, line_height, p, 0, 'L')\n\n            # Remaining paragraphs -> extra pages with same title\n            remaining = remaining[len(chunk):]\n            if remaining:\n                remaining_text = \"\\n\\n\".join(remaining)\n                self.content_pages(title, remaining_text)\n\n        except Exception as e:\n            print(f\"Error loading image from {public_url}: {str(e)}\")\n            fallback_text = f\"[Error loading image: {str(e)}]\\n\\n{content or ''}\"\n            self.content_pages(title, fallback_text)\n        finally:\n            if temp_file and os.path.exists(temp_file.name):\n                try:\n                    os.unlink(temp_file.name)\n                except:\n                    pass\n\n    def ending_page(self, ending_text):\n        \"\"\"Add an ending page with text\"\"\"\n        self.add_page()\n        self.set_font('Arial', '', 14)\n        self.set_y(self.h / 2 - 10)\n        self.multi_cell(0, 10, ending_text, 0, 'C')\n\n\ndef render_slides_into_pdf(pdf: ImagePDF, slides: list):\n    \"\"\"Renders a list of slide dicts into the PDF\"\"\"\n    for idx, s in enumerate(slides or []):\n        ctype = (s or {}).get(\"content_type\", \"\")\n        title = (s or {}).get(\"title\", \"\") or \"\"\n        subtitle = (s or {}).get(\"subtitle\", \"\") or \"\"\n        content = (s or {}).get(\"content\", \"\") or (s or {}).get(\"paragraphs\", \"\") or \"\"\n        image_url = (s or {}).get(\"image_url\") or (s or {}).get(\"image\") or (s or {}).get(\"url\") or \"\"\n        description = (s or {}).get(\"description\", \"\") or \"\"\n\n        if ctype == \"title_subtitle\":\n            pdf.title_page(title, subtitle)\n\n        elif ctype == \"title_content\":\n            pdf.content_pages(title, content)\n\n        elif ctype == \"title_image_description\":\n            if image_url:\n                pdf.title_image_page(title, image_url, description)\n            else:\n                pdf.content_pages(title, f\"[No image URL provided]\\n\\n{description}\")\n\n        elif ctype == \"image_description\":\n            if image_url:\n                pdf.image_page(image_url, description)\n            else:\n                pdf.content_pages(\"\", f\"[No image URL provided]\\n\\n{description}\")\n\n        elif ctype == \"title_image_content\":\n            if image_url:\n                pdf.title_image_content_page(title, image_url, content)\n            else:\n                pdf.content_pages(title, f\"[No image URL provided]\\n\\n{content}\")\n\n        elif ctype == \"image_content\":\n            if image_url:\n                pdf.image_content_page(image_url, content)\n            else:\n                pdf.content_pages(\"\", f\"[No image URL provided]\\n\\n{content}\")\n\n        elif ctype in (\"normal_bullets\", \"sequential_bullets\", \"icon_bullets\"):\n            bullets = (s or {}).get(\"bullet_points\", []) or []\n            bullet_lines = []\n            for b in bullets:\n                t = (b or {}).get(\"text\", \"\")\n                if t:\n                    bullet_lines.append(f\"• {t}\")\n            pdf.content_pages(title, \"\\n\\n\".join(bullet_lines) if bullet_lines else \"\")\n\n        else:\n            # Fallback behavior for unknown/legacy types\n            if image_url:\n                if title:\n                    pdf.title_image_page(title, image_url, description)\n                else:\n                    pdf.image_page(image_url, description)\n            elif subtitle:\n                pdf.title_page(title, subtitle)\n            else:\n                pdf.content_pages(title, content)\n\n\ndef generate_pdf_from_slides(slides_payload: dict):\n    \"\"\"\n    Generate a PDF file from slides and return as base64\n    \n    Args:\n        slides_payload: dict or list containing slide data with public URLs for images\n    \n    Returns:\n        dict with:\n            - base64: base64 encoded PDF string\n            - filename: suggested filename\n            - size: file size in bytes\n            - mime_type: MIME type of the document\n    \"\"\"\n    pdf = ImagePDF()\n    slides = _extract_slides(slides_payload)\n\n    print(f\"Processing {len(slides)} slides...\")\n    render_slides_into_pdf(pdf, slides)\n\n    current_datetime = datetime.now().strftime(\"%Y%m%d_%H%M%S\")\n    pdf_filename = f\"{current_datetime}_slides.pdf\"\n\n    # Generate PDF to bytes\n    pdf_bytes = pdf.output(dest='S')\n    file_size = len(pdf_bytes)\n\n    # Encode to base64\n    pdf_base64 = base64.b64encode(pdf_bytes).decode('utf-8')\n\n    print(f\"PDF generated: {pdf_filename} ({file_size} bytes)\")\n\n    return {\n        \"base64\": pdf_base64,\n        \"filename\": pdf_filename,\n        \"size\": file_size,\n        \"mime_type\": \"application/pdf\"\n    }\n\n\ndef _extract_slides(slides_payload):\n    \"\"\"Extract slides array from various input formats\"\"\"\n    if isinstance(slides_payload, list):\n        return slides_payload\n    if isinstance(slides_payload, dict):\n        if isinstance(slides_payload.get(\"slides\"), list):\n            return slides_payload[\"slides\"]\n        if isinstance(slides_payload.get(\"contentSlides\"), list):\n            return slides_payload[\"contentSlides\"]\n    return []\nresult = generate_pdf_from_slides(slides)\n",
				"isAsync":false,
				"python_version":"3.12",
				"outputFilesGlob":"*",
				"entityId":"e_691ae4cbb89ca466ebff0095",
				"captureStdOutput":false,
				"parameters":{
					"slides":{
						"items":"{{ n_Sj3k7.outputs.result.slides[0] }}",
						"source":"{{ n_Sj3k7.outputs.result.slides }}",
						"ua:type":"mappedArray"
					}
				}
			},
			"skip":false,
			"subTitle":"generatePDF",
			"title":"Execute Python script",
			"type":"ACTION"
		},
		{
			"context":{
				"appName":"files_by_unifyapps",
				"resourceVersion":0,
				"resourceName":"files_by_unifyapps_upload_file",
				"type":"APPLICATION"
			},
			"debug":false,
			"dirty":false,
			"fallbackMode":"STOP",
			"groupId":"_u2WlU@_ftk3g-1@y",
			"id":"n_Vv1JX",
			"index":14,
			"inputs":{
				"base64Content":"{{ n_le83X.outputs.result.base64 }}",
				"uploadPublic":false,
				"fileName":"{{ n_le83X.outputs.result.filename }}",
				"mimeType":"{{ n_le83X.outputs.result.mime_type }}",
				"option":"base64Content"
			},
			"skip":false,
			"subTitle":"Files by UnifyApps",
			"title":"Upload file",
			"type":"ACTION"
		},
		{
			"context":{
				"appName":"utility_by_unifyapps",
				"resourceVersion":654,
				"resourceName":"utility_by_unifyapps_generate_public_url",
				"type":"APPLICATION"
			},
			"debug":false,
			"dirty":false,
			"fallbackMode":"STOP",
			"groupId":"_u2WlU@_ftk3g-1@y",
			"id":"_mQJST",
			"index":15,
			"inputs":{
				"file":"{{ n_Vv1JX.outputs.fileDetails }}",
				"expiryTime":1
			},
			"skip":false,
			"subTitle":"pdf",
			"title":"Generate public file URL",
			"type":"ACTION"
		},
		{
			"context":{
				"appName":"callables",
				"resourceVersion":623,
				"resourceName":"callables_return_to_automation",
				"type":"APPLICATION"
			},
			"debug":false,
			"dirty":false,
			"fallbackMode":"STOP",
			"groupId":"_u2WlU@_ftk3g-1@y",
			"id":"_TRD1z",
			"index":16,
			"inputs":{
				"result":{
					"file":"{{ n_Vv1JX.outputs.fileDetails }}",
					"url":"{{ _mQJST.outputs.url }}"
				}
			},
			"skip":false,
			"subTitle":"Callable",
			"title":"Respond to automation",
			"type":"STOP"
		},
		{
			"additional":{
				"visibleOptionalFields":[
					"root.parameters.slides.items"
				]
			},
			"context":{
				"appName":"code_by_unifyapps",
				"resourceVersion":344,
				"resourceName":"code_by_unifyapps_python",
				"type":"APPLICATION"
			},
			"debug":false,
			"dirty":false,
			"fallbackMode":"STOP",
			"groupId":"_u2WlU@_ftk3g-1@n",
			"id":"n_3aisR",
			"index":17,
			"inputs":{
				"output":{
					"additionalProperties":false,
					"properties":{
						"base64":{
							"title":"base64",
							"type":"string"
						},
						"filename":{
							"title":"filename",
							"type":"string"
						},
						"mime_type":{
							"title":"mime_type",
							"type":"string"
						},
						"size":{
							"title":"size",
							"type":"number"
						}
					},
					"type":"object"
				},
				"configurationMode":"DEFAULT",
				"input":{
					"additionalProperties":false,
					"properties":{
						"slides":{
							"items":{
								"type":"object"
							},
							"title":"slides",
							"type":"array"
						}
					},
					"type":"object"
				},
				"code":"from docx import Document\nfrom docx.shared import Inches, Pt\nfrom docx.enum.text import WD_ALIGN_PARAGRAPH\nimport requests\nfrom io import BytesIO\nfrom PIL import Image\nimport tempfile\nimport os\nimport base64\nfrom datetime import datetime\n\nclass SlidesDocument:\n    def __init__(self):\n        self.doc = Document()\n        # Set default styles\n        style = self.doc.styles['Normal']\n        font = style.font\n        font.name = 'Arial'\n        font.size = Pt(11)\n    \n    def add_page_break(self):\n        \"\"\"Add a page break to start a new slide\"\"\"\n        self.doc.add_page_break()\n    \n    def title_page(self, title, subtitle):\n        \"\"\"Create a title page with title and subtitle\"\"\"\n        # Add some spacing from top\n        for _ in range(8):\n            self.doc.add_paragraph()\n        \n        # Title\n        title_para = self.doc.add_paragraph()\n        title_para.alignment = WD_ALIGN_PARAGRAPH.CENTER\n        title_run = title_para.add_run(title or \"\")\n        title_run.font.size = Pt(28)\n        title_run.font.bold = True\n        \n        # Spacing\n        self.doc.add_paragraph()\n        \n        # Subtitle\n        subtitle_para = self.doc.add_paragraph()\n        subtitle_para.alignment = WD_ALIGN_PARAGRAPH.CENTER\n        subtitle_run = subtitle_para.add_run(subtitle or \"\")\n        subtitle_run.font.size = Pt(18)\n        subtitle_run.font.italic = True\n    \n    def content_page(self, title, content):\n        \"\"\"Create a content page with title and paragraphs\"\"\"\n        # Title\n        title_para = self.doc.add_paragraph()\n        title_para.alignment = WD_ALIGN_PARAGRAPH.CENTER\n        title_run = title_para.add_run(title or \"\")\n        title_run.font.size = Pt(20)\n        title_run.font.bold = True\n        \n        # Spacing\n        self.doc.add_paragraph()\n        \n        # Content - split into paragraphs\n        paragraphs = self._split_paragraphs(content or \"\")\n        for para_text in paragraphs:\n            para = self.doc.add_paragraph(para_text)\n            para.alignment = WD_ALIGN_PARAGRAPH.LEFT\n            para_format = para.paragraph_format\n            para_format.space_after = Pt(12)\n    \n    def image_page(self, public_url, description=\"\", timeout=(5, 25)):\n        \"\"\"\n        Add a page with a centered image and description\n        Args:\n            public_url: Public URL to download the image from\n            description: Description text below the image\n        \"\"\"\n        temp_file = None\n        try:\n            # Download image from public URL\n            print(f\"Downloading image from: {public_url}\")\n            response = requests.get(public_url, timeout=timeout, verify=False)\n            response.raise_for_status()\n            img = Image.open(BytesIO(response.content))\n            \n            # Save to temp file\n            img_format = (img.format.lower() if img.format else 'png')\n            if img_format not in ['jpg', 'jpeg', 'png']:\n                img_format = 'png'\n            \n            temp_file = tempfile.NamedTemporaryFile(delete=False, suffix=f'.{img_format}')\n            img.save(temp_file.name, format=img.format or 'PNG')\n            temp_file.close()\n            \n            # Add some spacing\n            self.doc.add_paragraph()\n            \n            # Add image (centered)\n            # Calculate width to fit page (max 6 inches to leave margins)\n            img_width = min(6.0, img.width / 96)  # Convert pixels to inches (96 DPI)\n            \n            img_para = self.doc.add_paragraph()\n            img_para.alignment = WD_ALIGN_PARAGRAPH.CENTER\n            run = img_para.add_run()\n            run.add_picture(temp_file.name, width=Inches(img_width))\n            \n            # Add description\n            if description:\n                self.doc.add_paragraph()\n                desc_para = self.doc.add_paragraph(description)\n                desc_para.alignment = WD_ALIGN_PARAGRAPH.CENTER\n                desc_para.paragraph_format.space_before = Pt(12)\n        \n        except Exception as e:\n            print(f\"Error loading image from {public_url}: {str(e)}\")\n            # Add error message\n            error_para = self.doc.add_paragraph(f'Error loading image: {str(e)}')\n            error_para.alignment = WD_ALIGN_PARAGRAPH.CENTER\n            \n            if description:\n                self.doc.add_paragraph()\n                desc_para = self.doc.add_paragraph(description)\n                desc_para.alignment = WD_ALIGN_PARAGRAPH.CENTER\n        \n        finally:\n            if temp_file and os.path.exists(temp_file.name):\n                try:\n                    os.unlink(temp_file.name)\n                except:\n                    pass\n    \n    def title_image_page(self, title, public_url, description=\"\", timeout=(5, 25)):\n        \"\"\"\n        Create a page with title, image, and description\n        Args:\n            title: Page title\n            public_url: Public URL to download the image from\n            description: Description text below the image\n        \"\"\"\n        # Title\n        title_para = self.doc.add_paragraph()\n        title_para.alignment = WD_ALIGN_PARAGRAPH.CENTER\n        title_run = title_para.add_run(title or \"\")\n        title_run.font.size = Pt(20)\n        title_run.font.bold = True\n        \n        # Add spacing\n        self.doc.add_paragraph()\n        \n        # Add image\n        temp_file = None\n        try:\n            print(f\"Downloading image from: {public_url}\")\n            response = requests.get(public_url, timeout=timeout, verify=False)\n            response.raise_for_status()\n            img = Image.open(BytesIO(response.content))\n            \n            img_format = (img.format.lower() if img.format else 'png')\n            if img_format not in ['jpg', 'jpeg', 'png']:\n                img_format = 'png'\n            \n            temp_file = tempfile.NamedTemporaryFile(delete=False, suffix=f'.{img_format}')\n            img.save(temp_file.name, format=img.format or 'PNG')\n            temp_file.close()\n            \n            # Calculate width\n            img_width = min(6.0, img.width / 96)\n            \n            img_para = self.doc.add_paragraph()\n            img_para.alignment = WD_ALIGN_PARAGRAPH.CENTER\n            run = img_para.add_run()\n            run.add_picture(temp_file.name, width=Inches(img_width))\n            \n            # Description\n            if description:\n                self.doc.add_paragraph()\n                desc_para = self.doc.add_paragraph(description)\n                desc_para.alignment = WD_ALIGN_PARAGRAPH.CENTER\n        \n        except Exception as e:\n            print(f\"Error loading image from {public_url}: {str(e)}\")\n            error_para = self.doc.add_paragraph(f'Error loading image: {str(e)}')\n            error_para.alignment = WD_ALIGN_PARAGRAPH.CENTER\n            \n            if description:\n                self.doc.add_paragraph()\n                desc_para = self.doc.add_paragraph(description)\n                desc_para.alignment = WD_ALIGN_PARAGRAPH.CENTER\n        \n        finally:\n            if temp_file and os.path.exists(temp_file.name):\n                try:\n                    os.unlink(temp_file.name)\n                except:\n                    pass\n    \n    def image_content_page(self, public_url, content=\"\", timeout=(5, 25)):\n        \"\"\"\n        Add a page with a centered image and LEFT-aligned content paragraphs below\n        Args:\n            public_url: Public URL to download the image from\n            content: Body text rendered as normal left-aligned paragraphs\n        \"\"\"\n        temp_file = None\n        try:\n            print(f\"Downloading image from: {public_url}\")\n            response = requests.get(public_url, timeout=timeout, verify=False)\n            response.raise_for_status()\n            img = Image.open(BytesIO(response.content))\n\n            img_format = (img.format.lower() if img.format else 'png')\n            if img_format not in ['jpg', 'jpeg', 'png']:\n                img_format = 'png'\n\n            temp_file = tempfile.NamedTemporaryFile(delete=False, suffix=f'.{img_format}')\n            img.save(temp_file.name, format=img.format or 'PNG')\n            temp_file.close()\n\n            # Spacing\n            self.doc.add_paragraph()\n\n            # Image (centered)\n            img_width = min(6.0, img.width / 96)  # pixels to inches (96 DPI)\n            img_para = self.doc.add_paragraph()\n            img_para.alignment = WD_ALIGN_PARAGRAPH.CENTER\n            run = img_para.add_run()\n            run.add_picture(temp_file.name, width=Inches(img_width))\n\n            # Content (left-aligned paragraphs)\n            if content:\n                self.doc.add_paragraph()\n                for para_text in self._split_paragraphs(content):\n                    para = self.doc.add_paragraph(para_text)\n                    para.alignment = WD_ALIGN_PARAGRAPH.LEFT\n                    para.paragraph_format.space_after = Pt(12)\n\n        except Exception as e:\n            print(f\"Error loading image from {public_url}: {str(e)}\")\n            error_para = self.doc.add_paragraph(f'Error loading image: {str(e)}')\n            error_para.alignment = WD_ALIGN_PARAGRAPH.CENTER\n\n            if content:\n                self.doc.add_paragraph()\n                for para_text in self._split_paragraphs(content):\n                    para = self.doc.add_paragraph(para_text)\n                    para.alignment = WD_ALIGN_PARAGRAPH.LEFT\n                    para.paragraph_format.space_after = Pt(12)\n\n        finally:\n            if temp_file and os.path.exists(temp_file.name):\n                try:\n                    os.unlink(temp_file.name)\n                except:\n                    pass\n\n    def title_image_content_page(self, title, public_url, content=\"\", timeout=(5, 25)):\n        \"\"\"\n        Create a page with title, image, and LEFT-aligned content paragraphs\n        Args:\n            title: Page title\n            public_url: Public URL to download the image from\n            content: Body text rendered as normal left-aligned paragraphs\n        \"\"\"\n        # Title\n        title_para = self.doc.add_paragraph()\n        title_para.alignment = WD_ALIGN_PARAGRAPH.CENTER\n        title_run = title_para.add_run(title or \"\")\n        title_run.font.size = Pt(20)\n        title_run.font.bold = True\n\n        # Spacing\n        self.doc.add_paragraph()\n\n        temp_file = None\n        try:\n            print(f\"Downloading image from: {public_url}\")\n            response = requests.get(public_url, timeout=timeout, verify=False)\n            response.raise_for_status()\n            img = Image.open(BytesIO(response.content))\n\n            img_format = (img.format.lower() if img.format else 'png')\n            if img_format not in ['jpg', 'jpeg', 'png']:\n                img_format = 'png'\n\n            temp_file = tempfile.NamedTemporaryFile(delete=False, suffix=f'.{img_format}')\n            img.save(temp_file.name, format=img.format or 'PNG')\n            temp_file.close()\n\n            # Image (centered)\n            img_width = min(6.0, img.width / 96)\n            img_para = self.doc.add_paragraph()\n            img_para.alignment = WD_ALIGN_PARAGRAPH.CENTER\n            run = img_para.add_run()\n            run.add_picture(temp_file.name, width=Inches(img_width))\n\n            # Content (left-aligned paragraphs)\n            if content:\n                self.doc.add_paragraph()\n                for para_text in self._split_paragraphs(content):\n                    para = self.doc.add_paragraph(para_text)\n                    para.alignment = WD_ALIGN_PARAGRAPH.LEFT\n                    para.paragraph_format.space_after = Pt(12)\n\n        except Exception as e:\n            print(f\"Error loading image from {public_url}: {str(e)}\")\n            error_para = self.doc.add_paragraph(f'Error loading image: {str(e)}')\n            error_para.alignment = WD_ALIGN_PARAGRAPH.CENTER\n\n            if content:\n                self.doc.add_paragraph()\n                for para_text in self._split_paragraphs(content):\n                    para = self.doc.add_paragraph(para_text)\n                    para.alignment = WD_ALIGN_PARAGRAPH.LEFT\n                    para.paragraph_format.space_after = Pt(12)\n\n        finally:\n            if temp_file and os.path.exists(temp_file.name):\n                try:\n                    os.unlink(temp_file.name)\n                except:\n                    pass\n    \n    def ending_page(self, ending_text):\n        \"\"\"Add an ending page with text\"\"\"\n        # Add spacing from top\n        for _ in range(10):\n            self.doc.add_paragraph()\n        \n        ending_para = self.doc.add_paragraph(ending_text)\n        ending_para.alignment = WD_ALIGN_PARAGRAPH.CENTER\n        ending_run = ending_para.runs[0]\n        ending_run.font.size = Pt(16)\n    \n    def _split_paragraphs(self, content):\n        \"\"\"Split content into paragraphs\"\"\"\n        if not content:\n            return []\n        # Split on blank lines first\n        parts = [p.strip() for p in content.split('\\n\\n') if p and p.strip()]\n        if parts:\n            return parts\n        # Fall back to single newlines\n        return [p.strip() for p in content.split('\\n') if p and p.strip()]\n    \n    def save(self, filepath):\n        \"\"\"Save the document to a file\"\"\"\n        self.doc.save(filepath)\n    \n    def to_bytes(self):\n        \"\"\"Return document as bytes\"\"\"\n        bytes_io = BytesIO()\n        self.doc.save(bytes_io)\n        bytes_io.seek(0)\n        return bytes_io.getvalue()\n\n\ndef render_slides_into_docx(doc: SlidesDocument, slides: list):\n    \"\"\"\n    Renders a list of slide dicts into the DOCX document\n    Reads images from public URLs provided in the slides\n    \"\"\"\n    for idx, s in enumerate(slides or []):\n        if idx > 0:\n            doc.add_page_break()\n\n        ctype = (s or {}).get(\"content_type\", \"\")\n        title = (s or {}).get(\"title\", \"\") or \"\"\n        subtitle = (s or {}).get(\"subtitle\", \"\") or \"\"\n        content = (s or {}).get(\"content\", \"\") or (s or {}).get(\"paragraphs\", \"\") or \"\"\n        image_url = (s or {}).get(\"image_url\") or (s or {}).get(\"url\") or \"\"\n        description = (s or {}).get(\"description\", \"\") or \"\"\n\n        if ctype == \"title_subtitle\":\n            doc.title_page(title, subtitle)\n\n        elif ctype == \"title_content\":\n            doc.content_page(title, content)\n\n        elif ctype == \"title_image_description\":\n            if image_url:\n                doc.title_image_page(title, image_url, description)\n            else:\n                doc.content_page(title, f\"[No image URL provided]\\n\\n{description}\")\n\n        elif ctype == \"image_description\":\n            if image_url:\n                doc.image_page(image_url, description)\n            else:\n                doc.content_page(\"\", f\"[No image URL provided]\\n\\n{description}\")\n\n        elif ctype == \"title_image_content\":\n            if image_url:\n                doc.title_image_content_page(title, image_url, content)\n            else:\n                doc.content_page(title, f\"[No image URL provided]\\n\\n{content}\")\n\n        elif ctype == \"image_content\":\n            if image_url:\n                doc.image_content_page(image_url, content)\n            else:\n                doc.content_page(\"\", f\"[No image URL provided]\\n\\n{content}\")\n\n        elif ctype in (\"normal_bullets\", \"sequential_bullets\", \"icon_bullets\"):\n            bullets = (s or {}).get(\"bullet_points\", []) or []\n            bullet_lines = []\n            for b in bullets:\n                t = (b or {}).get(\"text\", \"\")\n                if t:\n                    bullet_lines.append(f\"• {t}\")\n            doc.content_page(title, \"\\n\\n\".join(bullet_lines) if bullet_lines else \"\")\n\n        else:\n            # Heuristic fallback\n            if image_url:\n                if title:\n                    doc.title_image_page(title, image_url, description)\n                else:\n                    doc.image_page(image_url, description)\n            elif subtitle:\n                doc.title_page(title, subtitle)\n            else:\n                doc.content_page(title, content)\n\n\ndef generate_docx_from_slides(slides_payload: dict):\n    \"\"\"\n    Generate a DOCX file from slides and return as base64\n    \n    Args:\n        slides_payload: dict or list containing slide data with public URLs for images\n    \n    Returns:\n        dict with:\n            - base64: base64 encoded DOCX string\n            - filename: suggested filename\n            - size: file size in bytes\n            - mime_type: MIME type of the document\n    \n    Example input:\n        {\n          \"slides\": [\n            {\n              \"content_type\": \"title_subtitle\",\n              \"title\": \"My Presentation\",\n              \"subtitle\": \"2024\"\n            },\n            {\n              \"content_type\": \"title_content\",\n              \"title\": \"Overview\",\n              \"content\": \"This is the content of the slide.\"\n            },\n            {\n              \"content_type\": \"title_image_description\",\n              \"title\": \"Chart\",\n              \"image_url\": \"https://example.com/public/chart.png\",\n              \"description\": \"Sales data visualization\"\n            },\n            {\n              \"content_type\": \"image_description\",\n              \"image_url\": \"https://example.com/public/photo.jpg\",\n              \"description\": \"Product photo\"\n            }\n          ]\n        }\n    \"\"\"\n    doc = SlidesDocument()\n    slides = _extract_slides(slides_payload)\n    \n    print(f\"Processing {len(slides)} slides...\")\n    render_slides_into_docx(doc, slides)\n    \n    # Generate filename\n    current_datetime = datetime.now().strftime(\"%Y%m%d_%H%M%S\")\n    docx_filename = f\"{current_datetime}_slides.docx\"\n    \n    # Get document as bytes\n    docx_bytes = doc.to_bytes()\n    file_size = len(docx_bytes)\n    \n    # Encode to base64\n    docx_base64 = base64.b64encode(docx_bytes).decode('utf-8')\n    \n    print(f\"DOCX generated: {docx_filename} ({file_size} bytes)\")\n    \n    return {\n        \"base64\": docx_base64,\n        \"filename\": docx_filename,\n        \"size\": file_size,\n        \"mime_type\": \"application/vnd.openxmlformats-officedocument.wordprocessingml.document\"\n    }\n\n\ndef _extract_slides(slides_payload):\n    \"\"\"Extract slides array from various input formats\"\"\"\n    if isinstance(slides_payload, list):\n        return slides_payload\n    if isinstance(slides_payload, dict):\n        if isinstance(slides_payload.get(\"slides\"), list):\n            return slides_payload[\"slides\"]\n        if isinstance(slides_payload.get(\"contentSlides\"), list):\n            return slides_payload[\"contentSlides\"]\n    return []\n\nresult = generate_docx_from_slides(slides)\n    \n",
				"isAsync":false,
				"python_version":"3.12",
				"outputFilesGlob":"*",
				"captureStdOutput":false,
				"parameters":{
					"slides":{
						"items":"{{ n_Sj3k7.outputs.result.slides[0] }}",
						"source":"{{ n_Sj3k7.outputs.result.slides }}",
						"ua:type":"mappedArray"
					}
				}
			},
			"skip":false,
			"subTitle":"generateDOC",
			"title":"Execute Python script",
			"type":"ACTION"
		},
		{
			"context":{
				"appName":"files_by_unifyapps",
				"resourceVersion":0,
				"resourceName":"files_by_unifyapps_upload_file",
				"type":"APPLICATION"
			},
			"debug":false,
			"dirty":false,
			"fallbackMode":"STOP",
			"groupId":"_u2WlU@_ftk3g-1@n",
			"id":"_C2vh5",
			"index":18,
			"inputs":{
				"base64Content":"{{ n_3aisR.outputs.result.base64 }}",
				"uploadPublic":false,
				"fileName":"{{ n_3aisR.outputs.result.filename }}",
				"mimeType":"{{ n_3aisR.outputs.result.mime_type }}",
				"option":"base64Content"
			},
			"skip":false,
			"subTitle":"Files by UnifyApps",
			"title":"Upload file",
			"type":"ACTION"
		},
		{
			"context":{
				"appName":"utility_by_unifyapps",
				"resourceVersion":654,
				"resourceName":"utility_by_unifyapps_generate_public_url",
				"type":"APPLICATION"
			},
			"debug":false,
			"dirty":false,
			"fallbackMode":"STOP",
			"groupId":"_u2WlU@_ftk3g-1@n",
			"id":"_3gRoV",
			"index":19,
			"inputs":{
				"file":"{{ _C2vh5.outputs.fileDetails }}",
				"expiryTime":1
			},
			"skip":false,
			"subTitle":"Docx",
			"title":"Generate public file URL",
			"type":"ACTION"
		},
		{
			"context":{
				"appName":"callables",
				"resourceVersion":623,
				"resourceName":"callables_return_to_automation",
				"type":"APPLICATION"
			},
			"debug":false,
			"dirty":false,
			"fallbackMode":"STOP",
			"groupId":"_u2WlU@_ftk3g-1@n",
			"id":"_FJh52",
			"index":20,
			"inputs":{
				"result":{
					"file":"{{ _C2vh5.outputs.fileDetails }}",
					"url":"{{ _3gRoV.outputs.url }}"
				}
			},
			"skip":false,
			"subTitle":"Callable",
			"title":"Respond to automation",
			"type":"STOP"
		}
	],
	"ownerUserId":-1,
	"paused":false,
	"schemaReferences":[
		
	],
	"settings":{
		"enableNodeLevelLogging":true,
		"enableRunLogging":true,
		"enableVariableLogging":true,
		"route":{
			"default":false,
			"tierName":"global"
		}
	},
	"standard":true,
	"version":0,
	"workflowDefinitionId":"6918efe56dee7e25571f641c",
	"workflowVersion":33
}